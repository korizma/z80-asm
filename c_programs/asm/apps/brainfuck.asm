; generated by sdcc-z80-bin
    JR __sdcc_z80_bin_start
    DB "brainfuck",0
__sdcc_z80_bin_start:
    LD SP,#0x7700
    CALL _main
__sdcc_z80_bin_halt:
    JR __sdcc_z80_bin_halt

;--------------------------------------------------------
; File Created by SDCC : free open source ANSI-C Compiler
; Version 4.1.0 #12072 (Linux)
;--------------------------------------------------------
	.module brainfuck
	.optsdcc -mz80
	
;--------------------------------------------------------
; Public variables in this module
;--------------------------------------------------------
	.globl _main
;--------------------------------------------------------
; special function registers
;--------------------------------------------------------
;--------------------------------------------------------
; ram data
;--------------------------------------------------------
	.area _DATA
_code_op:
	.ds 384
_code_arg:
	.ds 384
_code_jump:
	.ds 768
_compile_loop_stack:
	.ds 32
_tape:
	.ds 256
_code_len:
	.ds 2
_compile_error:
	.ds 1
_compile_message:
	.ds 2
_editor_view_start:
	.ds 2
_output_row:
	.ds 1
_output_col:
	.ds 1
;--------------------------------------------------------
; ram data
;--------------------------------------------------------
	.area _INITIALIZED
;--------------------------------------------------------
; absolute external ram data
;--------------------------------------------------------
	.area _DABS (ABS)
;--------------------------------------------------------
; global & static initialisations
;--------------------------------------------------------
	.area _HOME
	.area _GSINIT
	.area _GSFINAL
	.area _GSINIT
;--------------------------------------------------------
; Home
;--------------------------------------------------------
	.area _HOME
	.area _HOME
;--------------------------------------------------------
; code
;--------------------------------------------------------
	.area _CODE
;c_programs/apps/../common/glic80.h:29: static unsigned char glic_read_buttons(void) __naked {
;	---------------------------------
; Function glic_read_buttons
; ---------------------------------
_glic_read_buttons:
;c_programs/apps/../common/glic80.h:34: __endasm;
	in	a,(0)
	ld	l,a
	ret
;c_programs/apps/../common/glic80.h:35: }
;c_programs/apps/../common/glic80.h:37: static unsigned char glic_button_pressed(unsigned char buttons,
;	---------------------------------
; Function glic_button_pressed
; ---------------------------------
_glic_button_pressed:
;c_programs/apps/../common/glic80.h:39: return (buttons & mask) == 0u;
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	inc	iy
	and	a, 0 (iy)
	or	a, a
	ld	a, #0x01
	jr	Z, 00104$
	xor	a, a
00104$:
	ld	l, a
;c_programs/apps/../common/glic80.h:40: }
	ret
_glic_bit_masks:
	.db #0x01	; 1
	.db #0x02	; 2
	.db #0x04	; 4
	.db #0x08	; 8
	.db #0x10	; 16
	.db #0x20	; 32
	.db #0x40	; 64
	.db #0x80	; 128
;c_programs/apps/../common/glic80.h:42: static void glic_delay(unsigned int count) {
;	---------------------------------
; Function glic_delay
; ---------------------------------
_glic_delay:
;c_programs/apps/../common/glic80.h:43: while (count != 0u) {
	pop	de
	pop	bc
	push	bc
	push	de
00101$:
	ld	a, b
	or	a, c
	ret	Z
;c_programs/apps/../common/glic80.h:44: --count;
	dec	bc
;c_programs/apps/../common/glic80.h:47: __endasm;
	nop
;c_programs/apps/../common/glic80.h:49: }
	jr	00101$
;c_programs/apps/../common/glic80.h:51: static void glic_clear_text(void) {
;	---------------------------------
; Function glic_clear_text
; ---------------------------------
_glic_clear_text:
;c_programs/apps/../common/glic80.h:54: for (i = 0; i < GLIC_CVRAM_BYTES; ++i) {
	ld	bc, #0x0000
00102$:
;c_programs/apps/../common/glic80.h:55: GLIC_CVRAM[i] = 0x00u;
	ld	hl, #0x7700
	add	hl, bc
	ld	(hl), #0x00
;c_programs/apps/../common/glic80.h:54: for (i = 0; i < GLIC_CVRAM_BYTES; ++i) {
	inc	bc
	ld	a, b
	sub	a, #0x01
	jr	C, 00102$
;c_programs/apps/../common/glic80.h:57: }
	ret
;c_programs/apps/../common/glic80.h:59: static void glic_fill_graphics(unsigned char value) {
;	---------------------------------
; Function glic_fill_graphics
; ---------------------------------
_glic_fill_graphics:
;c_programs/apps/../common/glic80.h:62: for (i = 0; i < GLIC_GVRAM_BYTES; ++i) {
	ld	bc, #0x0000
00102$:
;c_programs/apps/../common/glic80.h:63: GLIC_GVRAM[i] = value;
	ld	hl, #0x7800
	add	hl, bc
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	ld	(hl), a
;c_programs/apps/../common/glic80.h:62: for (i = 0; i < GLIC_GVRAM_BYTES; ++i) {
	inc	bc
	ld	a, b
	sub	a, #0x08
	jr	C, 00102$
;c_programs/apps/../common/glic80.h:65: }
	ret
;c_programs/apps/../common/glic80.h:67: static void glic_plot(unsigned char x, unsigned char y, unsigned char value) {
;	---------------------------------
; Function glic_plot
; ---------------------------------
_glic_plot:
	push	ix
	ld	ix,#0
	add	ix,sp
;c_programs/apps/../common/glic80.h:71: if ((x >= GLIC_SCREEN_WIDTH) || (y >= 128u)) {
	ld	a, 4 (ix)
	sub	a, #0x80
	jr	NC, 00107$
	ld	a, 5 (ix)
	sub	a, #0x80
;c_programs/apps/../common/glic80.h:72: return;
	jr	NC, 00107$
;c_programs/apps/../common/glic80.h:75: offset = (((unsigned int)(y >> 3)) << 7) + x;
	ld	a, 5 (ix)
	rrca
	rrca
	rrca
	and	a, #0x1f
	ld	l, a
	ld	h, #0x00
	add	hl, hl
	add	hl, hl
	add	hl, hl
	add	hl, hl
	add	hl, hl
	add	hl, hl
	add	hl, hl
	ld	c, 4 (ix)
	ld	b, #0x00
	add	hl, bc
	ex	de, hl
;c_programs/apps/../common/glic80.h:76: mask = glic_bit_masks[y & 7u];
	ld	bc, #_glic_bit_masks+0
	ld	a, 5 (ix)
	and	a, #0x07
	ld	l, a
	ld	h, #0x00
	add	hl, bc
	ld	b, (hl)
;c_programs/apps/../common/glic80.h:78: GLIC_GVRAM[offset] |= mask;
	ld	hl, #0x7800
	add	hl, de
	ld	c, (hl)
;c_programs/apps/../common/glic80.h:77: if (value != 0u) {
	ld	a, 6 (ix)
	or	a, a
	jr	Z, 00105$
;c_programs/apps/../common/glic80.h:78: GLIC_GVRAM[offset] |= mask;
	ld	a, c
	or	a, b
	ld	(hl), a
	jr	00107$
00105$:
;c_programs/apps/../common/glic80.h:80: GLIC_GVRAM[offset] &= (unsigned char)~mask;
	ld	a, b
	cpl
	and	a, c
	ld	(hl), a
00107$:
;c_programs/apps/../common/glic80.h:82: }
	pop	ix
	ret
;c_programs/apps/../common/glic80.h:84: static void glic_draw_bitmap8(unsigned char x,
;	---------------------------------
; Function glic_draw_bitmap8
; ---------------------------------
_glic_draw_bitmap8:
	push	ix
	ld	ix,#0
	add	ix,sp
;c_programs/apps/../common/glic80.h:92: for (row = 0u; row < 8u; ++row) {
	ld	c, #0x00
00107$:
;c_programs/apps/../common/glic80.h:93: bits = rows[row];
	ld	l, 6 (ix)
	ld	h, 7 (ix)
	ld	b, #0x00
	add	hl, bc
	ld	l, (hl)
;c_programs/apps/../common/glic80.h:94: mask = 0x80u;
	ld	e, #0x80
;c_programs/apps/../common/glic80.h:95: for (col = 0u; col < 8u; ++col) {
	ld	a, 5 (ix)
	add	a, c
	ld	b, a
	ld	d, #0x00
00105$:
;c_programs/apps/../common/glic80.h:96: if ((bits & mask) != 0u) {
	ld	a, l
	and	a,e
	jr	Z, 00102$
;c_programs/apps/../common/glic80.h:97: glic_plot((unsigned char)(x + col), (unsigned char)(y + row), 1u);
	ld	a, 4 (ix)
	add	a, d
	push	hl
	push	bc
	push	de
	ld	h, #0x01
	ld	l, b
	push	hl
	push	af
	inc	sp
	call	_glic_plot
	pop	af
	inc	sp
	pop	de
	pop	bc
	pop	hl
00102$:
;c_programs/apps/../common/glic80.h:99: mask >>= 1;
	srl	e
;c_programs/apps/../common/glic80.h:95: for (col = 0u; col < 8u; ++col) {
	inc	d
	ld	a, d
	sub	a, #0x08
	jr	C, 00105$
;c_programs/apps/../common/glic80.h:92: for (row = 0u; row < 8u; ++row) {
	inc	c
	ld	a, c
	sub	a, #0x08
	jr	C, 00107$
;c_programs/apps/../common/glic80.h:102: }
	pop	ix
	ret
;c_programs/apps/../common/glic80.h:104: static void glic_draw_tile8(unsigned char cell_x,
;	---------------------------------
; Function glic_draw_tile8
; ---------------------------------
_glic_draw_tile8:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;c_programs/apps/../common/glic80.h:110: offset = (((unsigned int)cell_y) << 7) + (((unsigned int)cell_x) << 3);
	ld	c, 5 (ix)
	xor	a, a
	rr	a
	ld	b, c
	rr	b
	rra
	ld	c, a
	ld	l, 4 (ix)
	ld	h, #0x00
	add	hl, hl
	add	hl, hl
	add	hl, hl
	add	hl, bc
	ex	de, hl
;c_programs/apps/../common/glic80.h:111: for (col = 0u; col < 8u; ++col) {
	ld	c, #0x00
00102$:
;c_programs/apps/../common/glic80.h:112: GLIC_GVRAM[offset + col] = columns[col];
	ld	l, c
	ld	h, #0x00
	add	hl, de
	ld	-2 (ix), l
	ld	a, h
	add	a, #0x78
	ld	-1 (ix), a
	ld	l, 6 (ix)
	ld	h, 7 (ix)
	ld	b, #0x00
	add	hl, bc
	ld	a, (hl)
	pop	hl
	push	hl
	ld	(hl), a
;c_programs/apps/../common/glic80.h:111: for (col = 0u; col < 8u; ++col) {
	inc	c
	ld	a, c
	sub	a, #0x08
	jr	C, 00102$
;c_programs/apps/../common/glic80.h:114: }
	ld	sp, ix
	pop	ix
	ret
;c_programs/apps/../common/glic80.h:116: static void glic_prepare_screen(unsigned char value) {
;	---------------------------------
; Function glic_prepare_screen
; ---------------------------------
_glic_prepare_screen:
;c_programs/apps/../common/glic80.h:117: glic_clear_text();
	call	_glic_clear_text
;c_programs/apps/../common/glic80.h:118: glic_fill_graphics(value);
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_glic_fill_graphics
	inc	sp
;c_programs/apps/../common/glic80.h:119: }
	ret
;c_programs/apps/../common/glic80.h:121: static void glic_fill_page_run(unsigned char page,
;	---------------------------------
; Function glic_fill_page_run
; ---------------------------------
_glic_fill_page_run:
;c_programs/apps/../common/glic80.h:127: p = GLIC_GVRAM + (((unsigned int)page) << 7) + x;
	ld	iy, #2
	add	iy, sp
	ld	c, 0 (iy)
	ld	b, #0x00
	xor	a, a
	rr	b
	ld	b, c
	rr	b
	rra
	ld	c, a
	ld	a, b
	add	a, #0x78
	ld	b, a
	ld	a, c
	ld	hl, #3
	add	hl, sp
	add	a, (hl)
	ld	c, a
	jr	NC, 00117$
	inc	b
00117$:
;c_programs/apps/../common/glic80.h:128: while (width != 0u) {
	ld	e, 2 (iy)
00101$:
	ld	a, e
	or	a, a
	ret	Z
;c_programs/apps/../common/glic80.h:129: *p++ = value;
	ld	iy, #5
	add	iy, sp
	ld	a, 0 (iy)
	ld	(bc), a
	inc	bc
;c_programs/apps/../common/glic80.h:130: --width;
	dec	e
;c_programs/apps/../common/glic80.h:132: }
	jr	00101$
;c_programs/apps/../common/glic80.h:134: static void glic_fill_pages(unsigned char first_page,
;	---------------------------------
; Function glic_fill_pages
; ---------------------------------
_glic_fill_pages:
;c_programs/apps/../common/glic80.h:137: while (page_count != 0u) {
	ld	iy, #2
	add	iy, sp
	ld	b, 0 (iy)
	ld	c, 1 (iy)
00101$:
	ld	a, c
	or	a, a
	ret	Z
;c_programs/apps/../common/glic80.h:138: glic_fill_page_run(first_page, 0u, GLIC_SCREEN_WIDTH, value);
	push	bc
	ld	hl, #6
	add	hl, sp
	ld	d, (hl)
	ld	e,#0x80
	push	de
	xor	a, a
	push	af
	inc	sp
	push	bc
	inc	sp
	call	_glic_fill_page_run
	pop	af
	pop	af
	pop	bc
;c_programs/apps/../common/glic80.h:139: ++first_page;
	inc	b
;c_programs/apps/../common/glic80.h:140: --page_count;
	dec	c
;c_programs/apps/../common/glic80.h:142: }
	jr	00101$
;c_programs/apps/../common/glic80.h:144: static void glic_fill_columns(unsigned char x,
;	---------------------------------
; Function glic_fill_columns
; ---------------------------------
_glic_fill_columns:
;c_programs/apps/../common/glic80.h:149: for (page = 0u; page < GLIC_SCREEN_PAGES; ++page) {
	ld	b, #0x00
00102$:
;c_programs/apps/../common/glic80.h:150: glic_fill_page_run(page, x, width, value);
	push	bc
	ld	iy, #6
	add	iy, sp
	ld	a, 0 (iy)
	push	af
	inc	sp
	ld	a, -1 (iy)
	dec	iy
	push	af
	inc	sp
	ld	a, -1 (iy)
	push	af
	inc	sp
	push	bc
	inc	sp
	call	_glic_fill_page_run
	pop	af
	pop	af
	pop	bc
;c_programs/apps/../common/glic80.h:149: for (page = 0u; page < GLIC_SCREEN_PAGES; ++page) {
	inc	b
	ld	a, b
	sub	a, #0x10
	jr	C, 00102$
;c_programs/apps/../common/glic80.h:152: }
	ret
;c_programs/apps/brainfuck.c:68: static volatile unsigned char *text_cell(unsigned char row,
;	---------------------------------
; Function text_cell
; ---------------------------------
_text_cell:
;c_programs/apps/brainfuck.c:73: while (row != 0u) {
	ld	bc, #0x7700
	ld	iy, #2
	add	iy, sp
	ld	l, 0 (iy)
00101$:
	ld	a, l
	or	a, a
	jr	Z, 00103$
;c_programs/apps/brainfuck.c:74: p += 16u;
	ld	a, c
	add	a, #0x10
	ld	c, a
	jr	NC, 00117$
	inc	b
00117$:
;c_programs/apps/brainfuck.c:75: --row;
	dec	l
	jr	00101$
00103$:
;c_programs/apps/brainfuck.c:77: p += col;
	ld	a, c
	ld	hl, #3
	add	hl, sp
	add	a, (hl)
	ld	l, a
	jr	NC, 00118$
	inc	b
00118$:
	ld	h, b
;c_programs/apps/brainfuck.c:78: return p;
;c_programs/apps/brainfuck.c:79: }
	ret
_keyboard_keys:
	.db #0x2b	; 43
	.db #0x2d	; 45
	.db #0x3c	; 60
	.db #0x3e	; 62
	.db #0x2e	; 46
	.db #0x2c	; 44
	.db #0x5b	; 91
	.db #0x5d	; 93
	.db #0x41	; 65	'A'
	.db #0x42	; 66	'B'
	.db #0x43	; 67	'C'
	.db #0x44	; 68	'D'
	.db #0x45	; 69	'E'
	.db #0x46	; 70	'F'
	.db #0x47	; 71	'G'
	.db #0x48	; 72	'H'
	.db #0x49	; 73	'I'
	.db #0x4a	; 74	'J'
	.db #0x4b	; 75	'K'
	.db #0x4c	; 76	'L'
	.db #0x4d	; 77	'M'
	.db #0x4e	; 78	'N'
	.db #0x4f	; 79	'O'
	.db #0x50	; 80	'P'
	.db #0x51	; 81	'Q'
	.db #0x52	; 82	'R'
	.db #0x53	; 83	'S'
	.db #0x54	; 84	'T'
	.db #0x55	; 85	'U'
	.db #0x56	; 86	'V'
	.db #0x57	; 87	'W'
	.db #0x58	; 88	'X'
	.db #0x59	; 89	'Y'
	.db #0x5a	; 90	'Z'
	.db #0x30	; 48	'0'
	.db #0x31	; 49	'1'
	.db #0x32	; 50	'2'
	.db #0x33	; 51	'3'
	.db #0x34	; 52	'4'
	.db #0x35	; 53	'5'
	.db #0x36	; 54	'6'
	.db #0x37	; 55	'7'
	.db #0x38	; 56	'8'
	.db #0x39	; 57	'9'
	.db #0x5f	; 95
	.db #0x21	; 33
	.db #0x3f	; 63
	.db #0x0a	; 10
_demo_source:
	.ascii ",[.,]"
	.db 0x00
_demo_input:
	.ascii "HELLO"
	.db 0x0a
	.db 0x00
;c_programs/apps/brainfuck.c:81: static void text_put(unsigned char row, unsigned char col, unsigned char ch) {
;	---------------------------------
; Function text_put
; ---------------------------------
_text_put:
;c_programs/apps/brainfuck.c:82: if ((row >= 16u) || (col >= 16u)) {
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	sub	a, #0x10
	ret	NC
	ld	a, 1 (iy)
	sub	a, #0x10
;c_programs/apps/brainfuck.c:83: return;
	ret	NC
;c_programs/apps/brainfuck.c:85: *text_cell(row, col) = ch;
	ld	iy, #3
	add	iy, sp
	ld	h, 0 (iy)
	ld	l, -1 (iy)
	push	hl
	call	_text_cell
	pop	af
	ld	iy, #4
	add	iy, sp
	ld	a, 0 (iy)
	ld	(hl), a
;c_programs/apps/brainfuck.c:86: }
	ret
;c_programs/apps/brainfuck.c:88: static void text_clear_row(unsigned char row) {
;	---------------------------------
; Function text_clear_row
; ---------------------------------
_text_clear_row:
;c_programs/apps/brainfuck.c:91: for (col = 0u; col < 16u; ++col) {
	ld	b, #0x00
00102$:
;c_programs/apps/brainfuck.c:92: text_put(row, col, 0u);
	push	bc
	xor	a, a
	push	af
	inc	sp
	push	bc
	inc	sp
	ld	hl, #6
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
	pop	bc
;c_programs/apps/brainfuck.c:91: for (col = 0u; col < 16u; ++col) {
	inc	b
	ld	a, b
	sub	a, #0x10
	jr	C, 00102$
;c_programs/apps/brainfuck.c:94: }
	ret
;c_programs/apps/brainfuck.c:96: static void text_write(unsigned char row, unsigned char col, const char *text) {
;	---------------------------------
; Function text_write
; ---------------------------------
_text_write:
;c_programs/apps/brainfuck.c:97: while ((*text != 0) && (col < 16u)) {
	ld	iy, #4
	add	iy, sp
	ld	l, 0 (iy)
	ld	h, 1 (iy)
	ld	b, -1 (iy)
00102$:
	ld	d, (hl)
	ld	a, d
	or	a, a
	ret	Z
	ld	a, b
	sub	a, #0x10
	ret	NC
;c_programs/apps/brainfuck.c:98: text_put(row, col, (unsigned char)*text);
	push	hl
	push	bc
	ld	e, b
	push	de
	ld	hl, #8
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
	pop	bc
	pop	hl
;c_programs/apps/brainfuck.c:99: ++text;
	inc	hl
;c_programs/apps/brainfuck.c:100: ++col;
	inc	b
;c_programs/apps/brainfuck.c:102: }
	jr	00102$
;c_programs/apps/brainfuck.c:104: static void text_write_u16_3(unsigned char row,
;	---------------------------------
; Function text_write_u16_3
; ---------------------------------
_text_write_u16_3:
	push	ix
	ld	ix,#0
	add	ix,sp
;c_programs/apps/brainfuck.c:110: if (value > 999u) {
	ld	a, #0xe7
	cp	a, 6 (ix)
	ld	a, #0x03
	sbc	a, 7 (ix)
	jr	NC, 00102$
;c_programs/apps/brainfuck.c:111: value = 999u;
	ld	6 (ix), #0xe7
	ld	7 (ix), #0x03
00102$:
;c_programs/apps/brainfuck.c:115: while (value >= 100u) {
	ld	e, 6 (ix)
	ld	d, 7 (ix)
	ld	c, #0x00
00103$:
	ld	a, e
	sub	a, #0x64
	ld	a, d
	sbc	a, #0x00
	jr	C, 00116$
;c_programs/apps/brainfuck.c:116: value = (unsigned int)(value - 100u);
	ld	a, e
	add	a, #0x9c
	ld	e, a
	ld	a, d
	adc	a, #0xff
	ld	d, a
;c_programs/apps/brainfuck.c:117: ++hundreds;
	inc	c
	jr	00103$
00116$:
;c_programs/apps/brainfuck.c:120: while (value >= 10u) {
	ld	b, #0x00
00106$:
	ld	a, e
	sub	a, #0x0a
	ld	a, d
	sbc	a, #0x00
	jr	C, 00108$
;c_programs/apps/brainfuck.c:121: value = (unsigned int)(value - 10u);
	ld	a, e
	add	a, #0xf6
	ld	e, a
	ld	a, d
	adc	a, #0xff
	ld	d, a
;c_programs/apps/brainfuck.c:122: ++tens;
	inc	b
	jr	00106$
00108$:
;c_programs/apps/brainfuck.c:125: text_put(row, col, (unsigned char)('0' + hundreds));
	ld	a, c
	add	a, #0x30
	push	bc
	push	de
	push	af
	inc	sp
	ld	h, 5 (ix)
	ld	l, 4 (ix)
	push	hl
	call	_text_put
	pop	af
	inc	sp
	pop	de
	pop	bc
;c_programs/apps/brainfuck.c:126: text_put(row, (unsigned char)(col + 1u), (unsigned char)('0' + tens));
	ld	a, b
	add	a, #0x30
	ld	b, 5 (ix)
	inc	b
	push	de
	push	af
	inc	sp
	push	bc
	inc	sp
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
	pop	de
;c_programs/apps/brainfuck.c:128: (unsigned char)('0' + value));
	ld	a, e
	add	a, #0x30
;c_programs/apps/brainfuck.c:127: text_put(row, (unsigned char)(col + 2u),
	ld	b, 5 (ix)
	inc	b
	inc	b
	push	af
	inc	sp
	push	bc
	inc	sp
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
;c_programs/apps/brainfuck.c:129: }
	pop	ix
	ret
;c_programs/apps/brainfuck.c:131: static unsigned char button_edge(unsigned char buttons,
;	---------------------------------
; Function button_edge
; ---------------------------------
_button_edge:
;c_programs/apps/brainfuck.c:134: return (((last_buttons & mask) != 0u) && ((buttons & mask) == 0u));
	ld	iy, #3
	add	iy, sp
	ld	a, 0 (iy)
	inc	iy
	and	a, 0 (iy)
	or	a, a
	jr	Z, 00103$
	ld	a, -2 (iy)
	and	a, 0 (iy)
	or	a, a
	jr	Z, 00104$
00103$:
	ld	l, #0x00
	ret
00104$:
	ld	l, #0x01
;c_programs/apps/brainfuck.c:135: }
	ret
;c_programs/apps/brainfuck.c:137: static void wait_for_release(void) {
;	---------------------------------
; Function wait_for_release
; ---------------------------------
_wait_for_release:
;c_programs/apps/brainfuck.c:138: while (glic_read_buttons() != GLIC_BUTTONS_NONE) {
00101$:
	call	_glic_read_buttons
	inc	l
	ret	Z
;c_programs/apps/brainfuck.c:139: glic_delay(500u);
	ld	hl, #0x01f4
	push	hl
	call	_glic_delay
	pop	af
;c_programs/apps/brainfuck.c:141: }
	jr	00101$
;c_programs/apps/brainfuck.c:143: static void wait_for_any_key(void) {
;	---------------------------------
; Function wait_for_any_key
; ---------------------------------
_wait_for_any_key:
;c_programs/apps/brainfuck.c:144: wait_for_release();
	call	_wait_for_release
;c_programs/apps/brainfuck.c:145: while (glic_read_buttons() == GLIC_BUTTONS_NONE) {
00101$:
	call	_glic_read_buttons
	inc	l
	jp	NZ,_wait_for_release
;c_programs/apps/brainfuck.c:146: glic_delay(800u);
	ld	hl, #0x0320
	push	hl
	call	_glic_delay
	pop	af
;c_programs/apps/brainfuck.c:148: wait_for_release();
;c_programs/apps/brainfuck.c:149: }
	jr	00101$
;c_programs/apps/brainfuck.c:151: static unsigned int source_len(void) {
;	---------------------------------
; Function source_len
; ---------------------------------
_source_len:
;c_programs/apps/brainfuck.c:154: len = STORE_BASE[STORE_SRC_LEN_LO];
	ld	hl, #0x6004
	ld	c, (hl)
	ld	b, #0x00
;c_programs/apps/brainfuck.c:155: len |= ((unsigned int)STORE_BASE[STORE_SRC_LEN_HI]) << 8;
	ld	hl, #0x6005
	ld	l, (hl)
	ld	a,#0x00
	ld	h,a
	or	a, c
	ld	e, a
	ld	a, l
	or	a, b
	ld	d, a
;c_programs/apps/brainfuck.c:156: if (len > SOURCE_CAPACITY) {
	xor	a, a
	cp	a, e
	ld	a, #0x02
	sbc	a, d
	jr	NC, 00102$
;c_programs/apps/brainfuck.c:157: return 0u;
	ld	hl, #0x0000
	ret
00102$:
;c_programs/apps/brainfuck.c:159: return len;
	ex	de, hl
;c_programs/apps/brainfuck.c:160: }
	ret
;c_programs/apps/brainfuck.c:162: static void set_source_len(unsigned int len) {
;	---------------------------------
; Function set_source_len
; ---------------------------------
_set_source_len:
;c_programs/apps/brainfuck.c:163: if (len > SOURCE_CAPACITY) {
	xor	a, a
	ld	iy, #2
	add	iy, sp
	cp	a, 0 (iy)
	ld	a, #0x02
	sbc	a, 1 (iy)
	jr	NC, 00102$
;c_programs/apps/brainfuck.c:164: len = SOURCE_CAPACITY;
	ld	0 (iy), #0x00
	ld	1 (iy), #0x02
00102$:
;c_programs/apps/brainfuck.c:166: STORE_BASE[STORE_SRC_LEN_LO] = (unsigned char)(len & 0xffu);
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	ld	hl, #0x6004
	ld	(hl), a
;c_programs/apps/brainfuck.c:167: STORE_BASE[STORE_SRC_LEN_HI] = (unsigned char)(len >> 8);
	ld	c, 1 (iy)
	ld	l, #0x05
	ld	(hl), c
;c_programs/apps/brainfuck.c:168: }
	ret
;c_programs/apps/brainfuck.c:170: static unsigned int input_len(void) {
;	---------------------------------
; Function input_len
; ---------------------------------
_input_len:
;c_programs/apps/brainfuck.c:173: len = STORE_BASE[STORE_INPUT_LEN];
	ld	hl, #0x6006
	ld	l, (hl)
	ld	h, #0x00
;c_programs/apps/brainfuck.c:174: if (len > INPUT_CAPACITY) {
	ld	a, #0x40
	cp	a, l
	ld	a, #0x00
	sbc	a, h
	ret	NC
;c_programs/apps/brainfuck.c:175: return 0u;
	ld	hl, #0x0000
;c_programs/apps/brainfuck.c:177: return len;
;c_programs/apps/brainfuck.c:178: }
	ret
;c_programs/apps/brainfuck.c:180: static void set_input_len(unsigned int len) {
;	---------------------------------
; Function set_input_len
; ---------------------------------
_set_input_len:
;c_programs/apps/brainfuck.c:181: if (len > INPUT_CAPACITY) {
	ld	a, #0x40
	ld	iy, #2
	add	iy, sp
	cp	a, 0 (iy)
	ld	a, #0x00
	sbc	a, 1 (iy)
	jr	NC, 00102$
;c_programs/apps/brainfuck.c:182: len = INPUT_CAPACITY;
	ld	0 (iy), #0x40
	ld	1 (iy), #0
00102$:
;c_programs/apps/brainfuck.c:184: STORE_BASE[STORE_INPUT_LEN] = (unsigned char)len;
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	ld	hl, #0x6006
	ld	(hl), a
;c_programs/apps/brainfuck.c:185: }
	ret
;c_programs/apps/brainfuck.c:187: static volatile unsigned char *active_buffer(unsigned char kind) {
;	---------------------------------
; Function active_buffer
; ---------------------------------
_active_buffer:
;c_programs/apps/brainfuck.c:188: if (kind == EDIT_INPUT) {
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	dec	a
	jr	NZ, 00102$
;c_programs/apps/brainfuck.c:189: return INPUT_TEXT;
	ld	hl, #0x6208
	ret
00102$:
;c_programs/apps/brainfuck.c:191: return SOURCE_TEXT;
	ld	hl, #0x6008
;c_programs/apps/brainfuck.c:192: }
	ret
;c_programs/apps/brainfuck.c:194: static unsigned int active_len(unsigned char kind) {
;	---------------------------------
; Function active_len
; ---------------------------------
_active_len:
;c_programs/apps/brainfuck.c:195: if (kind == EDIT_INPUT) {
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	dec	a
	jp	Z,_input_len
;c_programs/apps/brainfuck.c:196: return input_len();
;c_programs/apps/brainfuck.c:198: return source_len();
;c_programs/apps/brainfuck.c:199: }
	jp	_source_len
;c_programs/apps/brainfuck.c:201: static void set_active_len(unsigned char kind, unsigned int len) {
;	---------------------------------
; Function set_active_len
; ---------------------------------
_set_active_len:
;c_programs/apps/brainfuck.c:202: if (kind == EDIT_INPUT) {
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	dec	a
	jr	NZ, 00102$
;c_programs/apps/brainfuck.c:203: set_input_len(len);
	ld	l, 1 (iy)
	ld	h, 2 (iy)
	push	hl
	call	_set_input_len
	pop	af
	ret
00102$:
;c_programs/apps/brainfuck.c:205: set_source_len(len);
	ld	hl, #3
	add	hl, sp
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	push	bc
	call	_set_source_len
	pop	af
;c_programs/apps/brainfuck.c:207: }
	ret
;c_programs/apps/brainfuck.c:209: static unsigned int active_capacity(unsigned char kind) {
;	---------------------------------
; Function active_capacity
; ---------------------------------
_active_capacity:
;c_programs/apps/brainfuck.c:210: if (kind == EDIT_INPUT) {
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	dec	a
	jr	NZ, 00102$
;c_programs/apps/brainfuck.c:211: return INPUT_CAPACITY;
	ld	hl, #0x0040
	ret
00102$:
;c_programs/apps/brainfuck.c:213: return SOURCE_CAPACITY;
	ld	hl, #0x0200
;c_programs/apps/brainfuck.c:214: }
	ret
;c_programs/apps/brainfuck.c:216: static void mark_storage_valid(void) {
;	---------------------------------
; Function mark_storage_valid
; ---------------------------------
_mark_storage_valid:
;c_programs/apps/brainfuck.c:217: STORE_BASE[0] = STORE_MAGIC0;
	ld	hl, #0x6000
	ld	(hl), #0x42
;c_programs/apps/brainfuck.c:218: STORE_BASE[1] = STORE_MAGIC1;
	ld	l, #0x01
	ld	(hl), #0x46
;c_programs/apps/brainfuck.c:219: STORE_BASE[2] = STORE_MAGIC2;
	ld	l, #0x02
	ld	(hl), #0x43
;c_programs/apps/brainfuck.c:220: STORE_BASE[3] = STORE_MAGIC3;
	ld	l, #0x03
	ld	(hl), #0x31
;c_programs/apps/brainfuck.c:221: }
	ret
;c_programs/apps/brainfuck.c:223: static unsigned int copy_literal(volatile unsigned char *dst,
;	---------------------------------
; Function copy_literal
; ---------------------------------
_copy_literal:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;c_programs/apps/brainfuck.c:229: while ((*src != 0) && (len < capacity)) {
	ld	c, 6 (ix)
	ld	b, 7 (ix)
	ld	de, #0x0000
00102$:
	ld	a, (bc)
	ld	-2 (ix), a
	ld	a, e
	sub	a, 8 (ix)
	ld	a, d
	sbc	a, 9 (ix)
	ld	a, #0x00
	rla
	ld	-1 (ix), a
;c_programs/apps/brainfuck.c:230: dst[len] = (unsigned char)*src;
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	add	hl, de
;c_programs/apps/brainfuck.c:229: while ((*src != 0) && (len < capacity)) {
	ld	a, -2 (ix)
	or	a, a
	jr	Z, 00113$
	ld	a, -1 (ix)
	or	a, a
	jr	Z, 00113$
;c_programs/apps/brainfuck.c:230: dst[len] = (unsigned char)*src;
	ld	a, -2 (ix)
	ld	(hl), a
;c_programs/apps/brainfuck.c:231: ++src;
	inc	bc
;c_programs/apps/brainfuck.c:232: ++len;
	inc	de
	jr	00102$
00113$:
;c_programs/apps/brainfuck.c:234: if (len < capacity) {
	ld	a, -1 (ix)
	or	a, a
	jr	Z, 00106$
;c_programs/apps/brainfuck.c:235: dst[len] = 0u;
	ld	(hl), #0x00
00106$:
;c_programs/apps/brainfuck.c:237: return len;
	ex	de, hl
;c_programs/apps/brainfuck.c:238: }
	ld	sp, ix
	pop	ix
	ret
;c_programs/apps/brainfuck.c:240: static void load_demo_program(void) {
;	---------------------------------
; Function load_demo_program
; ---------------------------------
_load_demo_program:
;c_programs/apps/brainfuck.c:243: mark_storage_valid();
	call	_mark_storage_valid
;c_programs/apps/brainfuck.c:244: len = copy_literal(SOURCE_TEXT, demo_source, SOURCE_CAPACITY);
	ld	hl, #0x0200
	push	hl
	ld	hl, #_demo_source
	push	hl
	ld	hl, #0x6008
	push	hl
	call	_copy_literal
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:245: set_source_len(len);
	ex	(sp),hl
	call	_set_source_len
;c_programs/apps/brainfuck.c:246: len = copy_literal(INPUT_TEXT, demo_input, INPUT_CAPACITY);
	ld	hl, #0x0040
	ex	(sp),hl
	ld	hl, #_demo_input
	push	hl
	ld	hl, #0x6208
	push	hl
	call	_copy_literal
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:247: set_input_len(len);
	ex	(sp),hl
	call	_set_input_len
	pop	af
;c_programs/apps/brainfuck.c:248: }
	ret
;c_programs/apps/brainfuck.c:250: static void storage_init(void) {
;	---------------------------------
; Function storage_init
; ---------------------------------
_storage_init:
;c_programs/apps/brainfuck.c:253: if ((STORE_BASE[0] != STORE_MAGIC0) ||
	ld	hl, #0x6000
	ld	a, (hl)
	sub	a, #0x42
	jp	NZ,_load_demo_program
;c_programs/apps/brainfuck.c:254: (STORE_BASE[1] != STORE_MAGIC1) ||
	ld	hl, #0x6001
	ld	a, (hl)
	sub	a, #0x46
	jp	NZ,_load_demo_program
;c_programs/apps/brainfuck.c:255: (STORE_BASE[2] != STORE_MAGIC2) ||
	ld	hl, #0x6002
	ld	a, (hl)
	sub	a, #0x43
	jp	NZ,_load_demo_program
;c_programs/apps/brainfuck.c:256: (STORE_BASE[3] != STORE_MAGIC3)) {
	ld	hl, #0x6003
	ld	a, (hl)
	sub	a, #0x31
;c_programs/apps/brainfuck.c:257: load_demo_program();
;c_programs/apps/brainfuck.c:258: return;
	jp	NZ,_load_demo_program
;c_programs/apps/brainfuck.c:261: len = source_len();
	call	_source_len
;c_programs/apps/brainfuck.c:262: if (len > SOURCE_CAPACITY) {
	xor	a, a
	cp	a, l
	ld	a, #0x02
	sbc	a, h
;c_programs/apps/brainfuck.c:263: load_demo_program();
;c_programs/apps/brainfuck.c:264: return;
	jp	C,_load_demo_program
;c_programs/apps/brainfuck.c:266: if (len < SOURCE_CAPACITY) {
	ld	a, h
	sub	a, #0x02
	jr	NC, 00109$
;c_programs/apps/brainfuck.c:267: SOURCE_TEXT[len] = 0u;
	ld	bc, #0x6008
	add	hl, bc
	ld	(hl), #0x00
00109$:
;c_programs/apps/brainfuck.c:270: len = input_len();
	call	_input_len
;c_programs/apps/brainfuck.c:271: if (len > INPUT_CAPACITY) {
	ld	a, #0x40
	cp	a, l
	ld	a, #0x00
	sbc	a, h
;c_programs/apps/brainfuck.c:272: load_demo_program();
;c_programs/apps/brainfuck.c:273: return;
	jp	C,_load_demo_program
;c_programs/apps/brainfuck.c:275: if (len < INPUT_CAPACITY) {
	ld	a, l
	sub	a, #0x40
	ld	a, h
	sbc	a, #0x00
	ret	NC
;c_programs/apps/brainfuck.c:276: INPUT_TEXT[len] = 0u;
	ld	bc, #0x6208
	add	hl, bc
	ld	(hl), #0x00
;c_programs/apps/brainfuck.c:278: }
	ret
;c_programs/apps/brainfuck.c:280: static unsigned char key_at(unsigned char row, unsigned char col) {
;	---------------------------------
; Function key_at
; ---------------------------------
_key_at:
;c_programs/apps/brainfuck.c:281: return keyboard_keys[((unsigned char)(row << 3)) + col];
	ld	bc, #_keyboard_keys+0
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	add	a, a
	add	a, a
	add	a, a
	ld	hl, #3
	add	hl, sp
	add	a, (hl)
	ld	l, a
	ld	h, #0x00
	add	hl, bc
	ld	l, (hl)
;c_programs/apps/brainfuck.c:282: }
	ret
;c_programs/apps/brainfuck.c:284: static unsigned char typed_key(unsigned char row, unsigned char col) {
;	---------------------------------
; Function typed_key
; ---------------------------------
_typed_key:
;c_programs/apps/brainfuck.c:287: ch = key_at(row, col);
	ld	iy, #3
	add	iy, sp
	ld	h, 0 (iy)
	ld	l, -1 (iy)
	push	hl
	call	_key_at
	pop	af
;c_programs/apps/brainfuck.c:288: if (ch == '_') {
	ld	a, l
	sub	a, #0x5f
	ret	NZ
;c_programs/apps/brainfuck.c:289: return ' ';
	ld	l, #0x20
;c_programs/apps/brainfuck.c:291: return ch;
;c_programs/apps/brainfuck.c:292: }
	ret
;c_programs/apps/brainfuck.c:294: static void key_label(unsigned char key,
;	---------------------------------
; Function key_label
; ---------------------------------
_key_label:
;c_programs/apps/brainfuck.c:298: *first = 'S';
	ld	iy, #3
	add	iy, sp
	ld	l, 0 (iy)
	ld	h, 1 (iy)
;c_programs/apps/brainfuck.c:299: *second = 'P';
	ld	c, 2 (iy)
	ld	b, 3 (iy)
	ld	a, -1 (iy)
	sub	a, #0x5f
	jr	NZ, 00102$
;c_programs/apps/brainfuck.c:298: *first = 'S';
	ld	(hl), #0x53
;c_programs/apps/brainfuck.c:299: *second = 'P';
	ld	a, #0x50
	ld	(bc), a
;c_programs/apps/brainfuck.c:300: return;
	ret
00102$:
;c_programs/apps/brainfuck.c:302: if (key == '\n') {
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	sub	a, #0x0a
	jr	NZ, 00104$
;c_programs/apps/brainfuck.c:303: *first = 'N';
	ld	(hl), #0x4e
;c_programs/apps/brainfuck.c:304: *second = 'L';
	ld	a, #0x4c
	ld	(bc), a
;c_programs/apps/brainfuck.c:305: return;
	ret
00104$:
;c_programs/apps/brainfuck.c:307: *first = key;
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	ld	(hl), a
;c_programs/apps/brainfuck.c:308: *second = ' ';
	ld	a, #0x20
	ld	(bc), a
;c_programs/apps/brainfuck.c:309: }
	ret
;c_programs/apps/brainfuck.c:311: static void draw_key_label(unsigned char row,
;	---------------------------------
; Function draw_key_label
; ---------------------------------
_draw_key_label:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;c_programs/apps/brainfuck.c:318: key_label(key, &first, &second);
	ld	hl, #1
	add	hl, sp
	ex	de, hl
	ld	hl, #0
	add	hl, sp
	push	de
	push	hl
	ld	a, 6 (ix)
	push	af
	inc	sp
	call	_key_label
	pop	af
;c_programs/apps/brainfuck.c:319: text_put(row, col, first);
	inc	sp
	ld	h, -2 (ix)
	ld	l, 5 (ix)
	ex	(sp),hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
;c_programs/apps/brainfuck.c:321: (selected != 0u) ? '<' : second);
	ld	a, 7 (ix)
	or	a, a
	jr	Z, 00103$
	ld	bc, #0x003c
	jr	00104$
00103$:
	ld	c, -1 (ix)
00104$:
	ld	a, c
;c_programs/apps/brainfuck.c:320: text_put(row, (unsigned char)(col + 1u),
	ld	b, 5 (ix)
	inc	b
	push	af
	inc	sp
	push	bc
	inc	sp
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_text_put
;c_programs/apps/brainfuck.c:322: }
	ld	sp,ix
	pop	ix
	ret
;c_programs/apps/brainfuck.c:324: static void draw_keyboard(unsigned char key_row, unsigned char key_col) {
;	---------------------------------
; Function draw_keyboard
; ---------------------------------
_draw_keyboard:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;c_programs/apps/brainfuck.c:328: for (row = 0u; row < KEY_ROWS; ++row) {
	ld	c, #0x00
00105$:
;c_programs/apps/brainfuck.c:329: text_clear_row((unsigned char)(KEY_TOP + row));
	ld	a, c
	add	a, #0x09
	ld	b, a
	push	bc
	push	bc
	inc	sp
	call	_text_clear_row
	inc	sp
	pop	bc
;c_programs/apps/brainfuck.c:330: for (col = 0u; col < KEY_COLS; ++col) {
	ld	a, 4 (ix)
	sub	a, c
	ld	a, #0x01
	jr	Z, 00141$
	xor	a, a
00141$:
	ld	-2 (ix), a
	ld	e, #0x00
00103$:
;c_programs/apps/brainfuck.c:334: ((row == key_row) && (col == key_col)) ? 1u : 0u);
	bit	0, -2 (ix)
	jr	Z, 00109$
	ld	a, 5 (ix)
	sub	a, e
	jr	NZ, 00109$
	ld	hl, #0x0001
	jr	00110$
00109$:
	ld	hl, #0x0000
00110$:
	ld	-1 (ix), l
;c_programs/apps/brainfuck.c:333: key_at(row, col),
	push	bc
	push	de
	ld	b, e
	push	bc
	call	_key_at
	pop	af
	pop	de
	pop	bc
;c_programs/apps/brainfuck.c:332: (unsigned char)(col << 1),
	ld	a, e
	add	a, a
;c_programs/apps/brainfuck.c:331: draw_key_label((unsigned char)(KEY_TOP + row),
	push	bc
	push	de
	ld	h, -1 (ix)
	push	hl
	inc	sp
	ld	h, l
	push	hl
	inc	sp
	push	af
	inc	sp
	push	bc
	inc	sp
	call	_draw_key_label
	pop	af
	pop	af
	pop	de
	pop	bc
;c_programs/apps/brainfuck.c:330: for (col = 0u; col < KEY_COLS; ++col) {
	inc	e
	ld	a, e
	sub	a, #0x08
	jr	C, 00103$
;c_programs/apps/brainfuck.c:328: for (row = 0u; row < KEY_ROWS; ++row) {
	inc	c
	ld	a, c
	sub	a, #0x06
	jr	C, 00105$
;c_programs/apps/brainfuck.c:337: }
	ld	sp, ix
	pop	ix
	ret
;c_programs/apps/brainfuck.c:339: static unsigned char buffer_starts_new_row(unsigned int pos,
;	---------------------------------
; Function buffer_starts_new_row
; ---------------------------------
_buffer_starts_new_row:
;c_programs/apps/brainfuck.c:344: *col = 0u;
	ld	iy, #7
	add	iy, sp
	ld	c, 0 (iy)
	ld	b, 1 (iy)
;c_programs/apps/brainfuck.c:343: if (ch == '\n') {
	ld	a, -1 (iy)
	sub	a, #0x0a
	jr	NZ, 00102$
;c_programs/apps/brainfuck.c:344: *col = 0u;
	xor	a, a
	ld	(bc), a
;c_programs/apps/brainfuck.c:345: return 1u;
	ld	l, #0x01
	ret
00102$:
;c_programs/apps/brainfuck.c:348: ++(*col);
	ld	a, (bc)
	inc	a
	ld	(bc), a
;c_programs/apps/brainfuck.c:349: if ((*col >= EDIT_VIEW_COLS) && (pos < len)) {
	sub	a, #0x0f
	jr	C, 00104$
	ld	hl, #4
	add	hl, sp
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	sub	a, (hl)
	ld	a, 1 (iy)
	inc	hl
	sbc	a, (hl)
	jr	NC, 00104$
;c_programs/apps/brainfuck.c:350: *col = 0u;
	xor	a, a
	ld	(bc), a
;c_programs/apps/brainfuck.c:351: return 1u;
	ld	l, #0x01
	ret
00104$:
;c_programs/apps/brainfuck.c:353: return 0u;
	ld	l, #0x00
;c_programs/apps/brainfuck.c:354: }
	ret
;c_programs/apps/brainfuck.c:356: static unsigned int buffer_max_window_start(volatile unsigned char *buffer,
;	---------------------------------
; Function buffer_max_window_start
; ---------------------------------
_buffer_max_window_start:
	push	ix
	ld	ix,#0
	add	ix,sp
	ld	hl, #-22
	add	hl, sp
	ld	sp, hl
;c_programs/apps/brainfuck.c:366: row_starts[0] = 0u;
	ld	hl, #0
	add	hl, sp
	ld	c,l
	ld	b,h
	xor	a, a
	ld	(hl), a
	inc	hl
	ld	(hl), a
;c_programs/apps/brainfuck.c:369: col = 0u;
	ld	-6 (ix), #0
;c_programs/apps/brainfuck.c:371: while (pos < len) {
	xor	a, a
	ld	-2 (ix), a
	ld	-1 (ix), a
	ld	de, #0x0000
00103$:
;c_programs/apps/brainfuck.c:375: ++row_no;
	ld	l, e
	ld	h, d
	inc	hl
	ld	-5 (ix), l
	ld	-4 (ix), h
;c_programs/apps/brainfuck.c:371: while (pos < len) {
	ld	a, -2 (ix)
	sub	a, 6 (ix)
	ld	a, -1 (ix)
	sbc	a, 7 (ix)
	jr	NC, 00105$
;c_programs/apps/brainfuck.c:372: ch = buffer[pos];
	ld	a, 4 (ix)
	add	a, -2 (ix)
	ld	l, a
	ld	a, 5 (ix)
	adc	a, -1 (ix)
	ld	h, a
	ld	a, (hl)
	ld	-3 (ix), a
;c_programs/apps/brainfuck.c:373: ++pos;
	inc	-2 (ix)
	jr	NZ, 00131$
	inc	-1 (ix)
00131$:
;c_programs/apps/brainfuck.c:374: if (buffer_starts_new_row(pos, len, ch, &col) != 0u) {
	ld	hl, #16
	add	hl, sp
	push	bc
	push	de
	push	hl
	ld	a, -3 (ix)
	push	af
	inc	sp
	ld	l, 6 (ix)
	ld	h, 7 (ix)
	push	hl
	ld	l, -2 (ix)
	ld	h, -1 (ix)
	push	hl
	call	_buffer_starts_new_row
	pop	af
	pop	af
	pop	af
	ld	a, l
	inc	sp
	pop	de
	pop	bc
	or	a, a
	jr	Z, 00103$
;c_programs/apps/brainfuck.c:375: ++row_no;
	ld	e, -5 (ix)
	ld	d, -4 (ix)
;c_programs/apps/brainfuck.c:376: slot = (unsigned char)(row_no & 7u);
	ld	a, -5 (ix)
	and	a, #0x07
;c_programs/apps/brainfuck.c:377: row_starts[slot] = pos;
	ld	h, #0x00
	ld	l, a
	add	hl, hl
	add	hl, bc
	ld	a, -2 (ix)
	ld	(hl), a
	inc	hl
	ld	a, -1 (ix)
	ld	(hl), a
	jr	00103$
00105$:
;c_programs/apps/brainfuck.c:381: if ((unsigned int)(row_no + 1u) <= EDIT_TEXT_ROWS) {
	ld	a, #0x07
	cp	a, -5 (ix)
	ld	a, #0x00
	sbc	a, -4 (ix)
	jr	C, 00107$
;c_programs/apps/brainfuck.c:382: return 0u;
	ld	hl, #0x0000
	jr	00108$
00107$:
;c_programs/apps/brainfuck.c:385: target_row = (unsigned int)(row_no + 1u - EDIT_TEXT_ROWS);
	ld	a, e
	add	a, #0xfa
;c_programs/apps/brainfuck.c:386: slot = (unsigned char)(target_row & 7u);
	and	a, #0x07
;c_programs/apps/brainfuck.c:387: return row_starts[slot];
	ld	l, a
	ld	h, #0x00
	add	hl, hl
	add	hl, bc
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	ex	de, hl
00108$:
;c_programs/apps/brainfuck.c:388: }
	ld	sp, ix
	pop	ix
	ret
;c_programs/apps/brainfuck.c:390: static unsigned int buffer_prev_window_start(volatile unsigned char *buffer,
;	---------------------------------
; Function buffer_prev_window_start
; ---------------------------------
_buffer_prev_window_start:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;c_programs/apps/brainfuck.c:399: prev = 0u;
	ld	de, #0x0000
;c_programs/apps/brainfuck.c:400: col = 0u;
	ld	-2 (ix), #0
;c_programs/apps/brainfuck.c:402: while ((pos < len) && (pos < start)) {
	ld	bc, #0x0000
00106$:
	ld	a, c
	sub	a, 6 (ix)
	ld	a, b
	sbc	a, 7 (ix)
	jr	NC, 00108$
	ld	a, c
	sub	a, 8 (ix)
	ld	a, b
	sbc	a, 9 (ix)
	jr	NC, 00108$
;c_programs/apps/brainfuck.c:403: ch = buffer[pos];
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	add	hl, bc
	ld	a, (hl)
	ld	-1 (ix), a
;c_programs/apps/brainfuck.c:404: ++pos;
	inc	bc
;c_programs/apps/brainfuck.c:405: if (buffer_starts_new_row(pos, len, ch, &col) != 0u) {
	ld	hl, #0
	add	hl, sp
	push	bc
	push	de
	push	hl
	ld	a, -1 (ix)
	push	af
	inc	sp
	ld	l, 6 (ix)
	ld	h, 7 (ix)
	push	hl
	push	bc
	call	_buffer_starts_new_row
	pop	af
	pop	af
	pop	af
	inc	sp
	ld	a, l
	pop	de
	pop	bc
	or	a, a
	jr	Z, 00106$
;c_programs/apps/brainfuck.c:406: if (pos >= start) {
	ld	a, c
	sub	a, 8 (ix)
	ld	a, b
	sbc	a, 9 (ix)
	jr	NC, 00108$
;c_programs/apps/brainfuck.c:409: prev = pos;
	ld	e, c
	ld	d, b
	jr	00106$
00108$:
;c_programs/apps/brainfuck.c:412: return prev;
	ex	de, hl
;c_programs/apps/brainfuck.c:413: }
	ld	sp, ix
	pop	ix
	ret
;c_programs/apps/brainfuck.c:415: static unsigned int buffer_next_window_start(volatile unsigned char *buffer,
;	---------------------------------
; Function buffer_next_window_start
; ---------------------------------
_buffer_next_window_start:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;c_programs/apps/brainfuck.c:423: max_start = buffer_max_window_start(buffer, len);
	ld	l, 6 (ix)
	ld	h, 7 (ix)
	push	hl
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_buffer_max_window_start
	pop	af
	pop	af
	ex	de, hl
;c_programs/apps/brainfuck.c:425: col = 0u;
	ld	-2 (ix), #0
;c_programs/apps/brainfuck.c:427: while (pos < len) {
	ld	bc, #0x0000
00107$:
	ld	a, c
	sub	a, 6 (ix)
	ld	a, b
	sbc	a, 7 (ix)
	jr	NC, 00109$
;c_programs/apps/brainfuck.c:428: ch = buffer[pos];
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	add	hl, bc
	ld	a, (hl)
	ld	-1 (ix), a
;c_programs/apps/brainfuck.c:429: ++pos;
	inc	bc
;c_programs/apps/brainfuck.c:430: if (buffer_starts_new_row(pos, len, ch, &col) != 0u) {
	ld	hl, #0
	add	hl, sp
	push	bc
	push	de
	push	hl
	ld	a, -1 (ix)
	push	af
	inc	sp
	ld	l, 6 (ix)
	ld	h, 7 (ix)
	push	hl
	push	bc
	call	_buffer_starts_new_row
	pop	af
	pop	af
	pop	af
	inc	sp
	ld	a, l
	pop	de
	pop	bc
	or	a, a
	jr	Z, 00107$
;c_programs/apps/brainfuck.c:431: if (pos > start) {
	ld	a, 8 (ix)
	sub	a, c
	ld	a, 9 (ix)
	sbc	a, b
	jr	NC, 00107$
;c_programs/apps/brainfuck.c:432: if (pos > max_start) {
	ld	a, e
	sub	a, c
	ld	a, d
	sbc	a, b
	jr	NC, 00102$
;c_programs/apps/brainfuck.c:433: return max_start;
	ex	de, hl
	jr	00110$
00102$:
;c_programs/apps/brainfuck.c:435: return pos;
	ld	l, c
	ld	h, b
	jr	00110$
00109$:
;c_programs/apps/brainfuck.c:439: return max_start;
	ex	de, hl
00110$:
;c_programs/apps/brainfuck.c:440: }
	ld	sp, ix
	pop	ix
	ret
;c_programs/apps/brainfuck.c:442: static void clamp_editor_view(unsigned char kind) {
;	---------------------------------
; Function clamp_editor_view
; ---------------------------------
_clamp_editor_view:
;c_programs/apps/brainfuck.c:446: buffer = active_buffer(kind);
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_active_buffer
	inc	sp
;c_programs/apps/brainfuck.c:447: max_start = buffer_max_window_start(buffer, active_len(kind));
	push	hl
	ld	hl, #4
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_active_len
	inc	sp
	pop	de
	push	hl
	push	de
	call	_buffer_max_window_start
	pop	af
	pop	af
	ex	de, hl
;c_programs/apps/brainfuck.c:448: if (editor_view_start > max_start) {
	ld	a, e
	ld	iy, #_editor_view_start
	sub	a, 0 (iy)
	ld	a, d
	sbc	a, 1 (iy)
	ret	NC
;c_programs/apps/brainfuck.c:449: editor_view_start = max_start;
	ld	(_editor_view_start), de
;c_programs/apps/brainfuck.c:451: }
	ret
;c_programs/apps/brainfuck.c:453: static void editor_view_to_end(unsigned char kind) {
;	---------------------------------
; Function editor_view_to_end
; ---------------------------------
_editor_view_to_end:
;c_programs/apps/brainfuck.c:456: buffer = active_buffer(kind);
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_active_buffer
	inc	sp
;c_programs/apps/brainfuck.c:457: editor_view_start = buffer_max_window_start(buffer, active_len(kind));
	push	hl
	ld	hl, #4
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_active_len
	inc	sp
	pop	de
	push	hl
	push	de
	call	_buffer_max_window_start
	pop	af
	pop	af
	ld	(_editor_view_start), hl
;c_programs/apps/brainfuck.c:458: }
	ret
;c_programs/apps/brainfuck.c:460: static void editor_scroll_up(unsigned char kind) {
;	---------------------------------
; Function editor_scroll_up
; ---------------------------------
_editor_scroll_up:
;c_programs/apps/brainfuck.c:463: buffer = active_buffer(kind);
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_active_buffer
	inc	sp
;c_programs/apps/brainfuck.c:465: active_len(kind),
	push	hl
	ld	hl, #4
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_active_len
	inc	sp
	ex	de, hl
	pop	bc
;c_programs/apps/brainfuck.c:464: editor_view_start = buffer_prev_window_start(buffer,
	ld	hl, (_editor_view_start)
	push	hl
	push	de
	push	bc
	call	_buffer_prev_window_start
	pop	af
	pop	af
	pop	af
	ld	(_editor_view_start), hl
;c_programs/apps/brainfuck.c:467: }
	ret
;c_programs/apps/brainfuck.c:469: static void editor_scroll_down(unsigned char kind) {
;	---------------------------------
; Function editor_scroll_down
; ---------------------------------
_editor_scroll_down:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;c_programs/apps/brainfuck.c:474: buffer = active_buffer(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_active_buffer
	inc	sp
	ex	(sp), hl
;c_programs/apps/brainfuck.c:475: len = active_len(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_active_len
	inc	sp
	ld	c, l
	ld	b, h
;c_programs/apps/brainfuck.c:476: max_start = buffer_max_window_start(buffer, len);
	push	bc
	push	bc
	ld	l, -2 (ix)
	ld	h, -1 (ix)
	push	hl
	call	_buffer_max_window_start
	pop	af
	pop	af
	ex	de, hl
	pop	bc
;c_programs/apps/brainfuck.c:477: if (editor_view_start < max_start) {
	ld	hl, (_editor_view_start)
	xor	a, a
	sbc	hl, de
	jr	NC, 00103$
;c_programs/apps/brainfuck.c:478: editor_view_start = buffer_next_window_start(buffer,
	ld	hl, (_editor_view_start)
	push	hl
	push	bc
	ld	l, -2 (ix)
	ld	h, -1 (ix)
	push	hl
	call	_buffer_next_window_start
	pop	af
	pop	af
	pop	af
	ld	(_editor_view_start), hl
00103$:
;c_programs/apps/brainfuck.c:482: }
	ld	sp, ix
	pop	ix
	ret
;c_programs/apps/brainfuck.c:484: static void draw_editor_scrollbar(unsigned char kind) {
;	---------------------------------
; Function draw_editor_scrollbar
; ---------------------------------
_draw_editor_scrollbar:
	push	ix
	ld	ix,#0
	add	ix,sp
	dec	sp
;c_programs/apps/brainfuck.c:494: buffer = active_buffer(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_active_buffer
	inc	sp
;c_programs/apps/brainfuck.c:495: len = active_len(kind);
	push	hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_active_len
	inc	sp
	pop	de
;c_programs/apps/brainfuck.c:496: max_start = buffer_max_window_start(buffer, len);
	push	hl
	push	de
	call	_buffer_max_window_start
	pop	af
	pop	af
	ld	c, l
	ld	b, h
;c_programs/apps/brainfuck.c:498: for (row = 0u; row < EDIT_TEXT_ROWS; ++row) {
	ld	d, #0x00
00116$:
;c_programs/apps/brainfuck.c:499: text_put((unsigned char)(EDIT_TEXT_FIRST_ROW + row), 15u, '.');
	inc	d
	push	bc
	push	de
	ld	hl, #0x2e0f
	push	hl
	push	de
	inc	sp
	call	_text_put
	pop	af
	inc	sp
	pop	de
	pop	bc
;c_programs/apps/brainfuck.c:498: for (row = 0u; row < EDIT_TEXT_ROWS; ++row) {
	ld	a, d
	sub	a, #0x07
	jr	C, 00116$
;c_programs/apps/brainfuck.c:502: if (max_start == 0u) {
	ld	a, b
;c_programs/apps/brainfuck.c:503: for (row = 0u; row < EDIT_TEXT_ROWS; ++row) {
	or	a,c
	jr	NZ, 00104$
	ld	b,a
00118$:
;c_programs/apps/brainfuck.c:504: text_put((unsigned char)(EDIT_TEXT_FIRST_ROW + row), 15u, '#');
	inc	b
	push	bc
	ld	de, #0x230f
	push	de
	push	bc
	inc	sp
	call	_text_put
	pop	af
	inc	sp
	pop	bc
;c_programs/apps/brainfuck.c:503: for (row = 0u; row < EDIT_TEXT_ROWS; ++row) {
	ld	a, b
	sub	a, #0x07
	jr	C, 00118$
;c_programs/apps/brainfuck.c:506: return;
	jr	00123$
00104$:
;c_programs/apps/brainfuck.c:509: if (editor_view_start == 0u) {
	ld	iy, #_editor_view_start
	ld	a, 1 (iy)
	or	a, 0 (iy)
	jr	NZ, 00114$
;c_programs/apps/brainfuck.c:510: knob_row = EDIT_TEXT_FIRST_ROW;
	ld	h, #0x01
	jr	00115$
00114$:
;c_programs/apps/brainfuck.c:511: } else if (editor_view_start >= max_start) {
	ld	hl, (_editor_view_start)
	xor	a, a
	sbc	hl, bc
	jr	C, 00111$
;c_programs/apps/brainfuck.c:512: knob_row = (unsigned char)(EDIT_TEXT_FIRST_ROW + EDIT_TEXT_ROWS - 1u);
	ld	h, #0x07
	jr	00115$
00111$:
;c_programs/apps/brainfuck.c:514: scaled = 0u;
	ld	de, #0x0000
;c_programs/apps/brainfuck.c:515: for (i = 0u; i < (unsigned char)(EDIT_TEXT_ROWS - 1u); ++i) {
	ld	-1 (ix), #0x06
00122$:
;c_programs/apps/brainfuck.c:516: scaled = (unsigned int)(scaled + editor_view_start);
	ld	a, e
	ld	hl, #_editor_view_start
	add	a, (hl)
	ld	e, a
	ld	a, d
	inc	hl
	adc	a, (hl)
	ld	d, a
	dec	-1 (ix)
;c_programs/apps/brainfuck.c:515: for (i = 0u; i < (unsigned char)(EDIT_TEXT_ROWS - 1u); ++i) {
	ld	a, -1 (ix)
;c_programs/apps/brainfuck.c:519: while ((scaled >= max_start) &&
	or	a,a
	jr	NZ, 00122$
	ld	l,a
00107$:
;c_programs/apps/brainfuck.c:522: ++knob_index;
	ld	h, l
	inc	h
;c_programs/apps/brainfuck.c:519: while ((scaled >= max_start) &&
	ld	a, e
	sub	a, c
	ld	a, d
	sbc	a, b
	jr	C, 00109$
;c_programs/apps/brainfuck.c:520: (knob_index < (unsigned char)(EDIT_TEXT_ROWS - 1u))) {
	ld	a, l
	sub	a, #0x06
	jr	NC, 00109$
;c_programs/apps/brainfuck.c:521: scaled = (unsigned int)(scaled - max_start);
	ld	a, e
	sub	a, c
	ld	e, a
	ld	a, d
	sbc	a, b
	ld	d, a
;c_programs/apps/brainfuck.c:522: ++knob_index;
	ld	l, h
	jr	00107$
00109$:
;c_programs/apps/brainfuck.c:524: knob_row = (unsigned char)(EDIT_TEXT_FIRST_ROW + knob_index);
00115$:
;c_programs/apps/brainfuck.c:526: text_put(knob_row, 15u, '#');
	ld	de, #0x230f
	push	de
	push	hl
	inc	sp
	call	_text_put
	pop	af
	inc	sp
00123$:
;c_programs/apps/brainfuck.c:527: }
	inc	sp
	pop	ix
	ret
;c_programs/apps/brainfuck.c:529: static void draw_editor_preview(unsigned char kind) {
;	---------------------------------
; Function draw_editor_preview
; ---------------------------------
_draw_editor_preview:
	push	ix
	ld	ix,#0
	add	ix,sp
	ld	hl, #-8
	add	hl, sp
	ld	sp, hl
;c_programs/apps/brainfuck.c:537: for (row = 0u; row < EDIT_TEXT_ROWS; ++row) {
	ld	b, #0x00
00117$:
;c_programs/apps/brainfuck.c:538: text_clear_row((unsigned char)(EDIT_TEXT_FIRST_ROW + row));
	inc	b
	push	bc
	push	bc
	inc	sp
	call	_text_clear_row
	inc	sp
	pop	bc
;c_programs/apps/brainfuck.c:537: for (row = 0u; row < EDIT_TEXT_ROWS; ++row) {
	ld	a, b
	sub	a, #0x07
	jr	C, 00117$
;c_programs/apps/brainfuck.c:541: buffer = active_buffer(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_active_buffer
	inc	sp
	ex	(sp), hl
;c_programs/apps/brainfuck.c:542: len = active_len(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_active_len
	ld	-6 (ix), l
	ld	-5 (ix), h
	inc	sp
;c_programs/apps/brainfuck.c:543: clamp_editor_view(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_clamp_editor_view
	inc	sp
;c_programs/apps/brainfuck.c:544: if (len == 0u) {
	ld	a, -5 (ix)
	or	a, -6 (ix)
	jr	NZ, 00103$
;c_programs/apps/brainfuck.c:545: text_write(4u, 5u, "EMPTY");
	ld	hl, #___str_2
	push	hl
	ld	de, #0x0504
	push	de
	call	_text_write
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:546: draw_editor_scrollbar(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_scrollbar
;c_programs/apps/brainfuck.c:547: return;
	jp	00119$
00103$:
;c_programs/apps/brainfuck.c:550: pos = editor_view_start;
	ld	hl, (_editor_view_start)
	ld	-3 (ix), l
	ld	-2 (ix), h
;c_programs/apps/brainfuck.c:551: row = EDIT_TEXT_FIRST_ROW;
	ld	-4 (ix), #0x01
;c_programs/apps/brainfuck.c:552: col = 0u;
	ld	-1 (ix), #0
;c_programs/apps/brainfuck.c:554: while ((pos < len) &&
00114$:
	ld	a, -3 (ix)
	sub	a, -6 (ix)
	ld	a, -2 (ix)
	sbc	a, -5 (ix)
	jr	NC, 00116$
;c_programs/apps/brainfuck.c:555: (row < (unsigned char)(EDIT_TEXT_FIRST_ROW + EDIT_TEXT_ROWS))) {
	ld	a, -4 (ix)
	sub	a, #0x08
	jr	NC, 00116$
;c_programs/apps/brainfuck.c:556: ch = buffer[pos];
	ld	a, -8 (ix)
	add	a, -3 (ix)
	ld	l, a
	ld	a, -7 (ix)
	adc	a, -2 (ix)
	ld	h, a
	ld	b, (hl)
;c_programs/apps/brainfuck.c:557: ++pos;
	inc	-3 (ix)
	jr	NZ, 00172$
	inc	-2 (ix)
00172$:
;c_programs/apps/brainfuck.c:538: text_clear_row((unsigned char)(EDIT_TEXT_FIRST_ROW + row));
	ld	c, -4 (ix)
	inc	c
;c_programs/apps/brainfuck.c:559: if (ch == '\n') {
	ld	a, b
	sub	a, #0x0a
	jr	NZ, 00107$
;c_programs/apps/brainfuck.c:560: if (col < EDIT_VIEW_COLS) {
	ld	a, -1 (ix)
	sub	a, #0x0f
	jr	NC, 00105$
;c_programs/apps/brainfuck.c:561: text_put(row, col, '\\');
	push	bc
	ld	h, #0x5c
	ld	l, -1 (ix)
	push	hl
	ld	a, -4 (ix)
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
	pop	bc
00105$:
;c_programs/apps/brainfuck.c:563: ++row;
	ld	-4 (ix), c
;c_programs/apps/brainfuck.c:564: col = 0u;
	ld	-1 (ix), #0
;c_programs/apps/brainfuck.c:565: continue;
	jr	00114$
00107$:
;c_programs/apps/brainfuck.c:567: if ((ch < 32u) || (ch > 126u)) {
	ld	a, b
	sub	a, #0x20
	jr	C, 00108$
	ld	a, #0x7e
	sub	a, b
	jr	NC, 00109$
00108$:
;c_programs/apps/brainfuck.c:568: ch = '?';
	ld	b, #0x3f
00109$:
;c_programs/apps/brainfuck.c:571: text_put(row, col, ch);
	push	bc
	push	bc
	inc	sp
	ld	h, -1 (ix)
	ld	l, -4 (ix)
	push	hl
	call	_text_put
	pop	af
	inc	sp
	pop	bc
;c_programs/apps/brainfuck.c:572: ++col;
	inc	-1 (ix)
;c_programs/apps/brainfuck.c:573: if (col >= EDIT_VIEW_COLS) {
	ld	a, -1 (ix)
	sub	a, #0x0f
	jr	C, 00114$
;c_programs/apps/brainfuck.c:574: col = 0u;
	ld	-1 (ix), #0
;c_programs/apps/brainfuck.c:575: ++row;
	ld	-4 (ix), c
	jp	00114$
00116$:
;c_programs/apps/brainfuck.c:578: draw_editor_scrollbar(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_scrollbar
00119$:
;c_programs/apps/brainfuck.c:579: }
	ld	sp, ix
	pop	ix
	ret
___str_2:
	.ascii "EMPTY"
	.db 0x00
;c_programs/apps/brainfuck.c:581: static void draw_editor_header(unsigned char kind,
;	---------------------------------
; Function draw_editor_header
; ---------------------------------
_draw_editor_header:
	push	ix
	ld	ix,#0
	add	ix,sp
;c_programs/apps/brainfuck.c:585: text_clear_row(0u);
	xor	a, a
	push	af
	inc	sp
	call	_text_clear_row
	inc	sp
;c_programs/apps/brainfuck.c:586: if (kind == EDIT_INPUT) {
	ld	a, 4 (ix)
	dec	a
	jr	NZ, 00102$
;c_programs/apps/brainfuck.c:587: text_write(0u, 0u, "INP ");
	ld	hl, #___str_3
	push	hl
	xor	a, a
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_write
	pop	af
	pop	af
	jr	00103$
00102$:
;c_programs/apps/brainfuck.c:589: text_write(0u, 0u, "SRC ");
	ld	hl, #___str_4
	push	hl
	xor	a, a
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_write
	pop	af
	pop	af
00103$:
;c_programs/apps/brainfuck.c:591: text_write_u16_3(0u, 4u, active_len(kind));
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_active_len
	inc	sp
	push	hl
	ld	a, #0x04
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_write_u16_3
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:592: text_put(0u, 7u, '/');
	ld	de, #0x2f07
	push	de
	xor	a, a
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
;c_programs/apps/brainfuck.c:593: text_write_u16_3(0u, 8u, active_capacity(kind));
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_active_capacity
	inc	sp
	push	hl
	ld	a, #0x08
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_write_u16_3
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:594: draw_key_label(0u, 12u, key_at(key_row, key_col), 0u);
	ld	h, 6 (ix)
	ld	l, 5 (ix)
	push	hl
	call	_key_at
	pop	af
	ld	a, l
	ld	h, #0x00
	push	hl
	inc	sp
	ld	d,a
	ld	e,#0x0c
	push	de
	xor	a, a
	push	af
	inc	sp
	call	_draw_key_label
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:595: text_write(0u, 14u, status);
	ld	l, 7 (ix)
	ld	h, 8 (ix)
	push	hl
	ld	a, #0x0e
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_write
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:596: }
	pop	ix
	ret
___str_3:
	.ascii "INP "
	.db 0x00
___str_4:
	.ascii "SRC "
	.db 0x00
;c_programs/apps/brainfuck.c:598: static void draw_editor_key(unsigned char key_row,
;	---------------------------------
; Function draw_editor_key
; ---------------------------------
_draw_editor_key:
;c_programs/apps/brainfuck.c:603: key_at(key_row, key_col),
	ld	iy, #3
	add	iy, sp
	ld	h, 0 (iy)
	ld	l, -1 (iy)
	push	hl
	call	_key_at
	pop	af
	ld	d, l
;c_programs/apps/brainfuck.c:602: (unsigned char)(key_col << 1),
	ld	iy, #3
	add	iy, sp
	ld	a, 0 (iy)
	add	a, a
	ld	b, a
;c_programs/apps/brainfuck.c:601: draw_key_label((unsigned char)(KEY_TOP + key_row),
	ld	a, -1 (iy)
	dec	iy
	add	a, #0x09
	ld	h, 2 (iy)
	ld	l, d
	push	hl
	push	bc
	inc	sp
	push	af
	inc	sp
	call	_draw_key_label
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:605: }
	ret
;c_programs/apps/brainfuck.c:607: static void move_editor_key(unsigned char old_row,
;	---------------------------------
; Function move_editor_key
; ---------------------------------
_move_editor_key:
	push	ix
	ld	ix,#0
	add	ix,sp
;c_programs/apps/brainfuck.c:611: if ((old_row == new_row) && (old_col == new_col)) {
	ld	a, 4 (ix)
	sub	a, 6 (ix)
	jr	NZ, 00102$
	ld	a, 5 (ix)
	sub	a, 7 (ix)
	jr	Z, 00104$
;c_programs/apps/brainfuck.c:612: return;
	jr	00102$
00102$:
;c_programs/apps/brainfuck.c:614: draw_editor_key(old_row, old_col, 0u);
	xor	a, a
	push	af
	inc	sp
	ld	h, 5 (ix)
	ld	l, 4 (ix)
	push	hl
	call	_draw_editor_key
	pop	af
	inc	sp
;c_programs/apps/brainfuck.c:615: draw_editor_key(new_row, new_col, 1u);
	ld	h, #0x01
	ld	l, 7 (ix)
	push	hl
	ld	a, 6 (ix)
	push	af
	inc	sp
	call	_draw_editor_key
	pop	af
	inc	sp
00104$:
;c_programs/apps/brainfuck.c:616: }
	pop	ix
	ret
;c_programs/apps/brainfuck.c:618: static void draw_editor_shell(unsigned char kind,
;	---------------------------------
; Function draw_editor_shell
; ---------------------------------
_draw_editor_shell:
	push	ix
	ld	ix,#0
	add	ix,sp
;c_programs/apps/brainfuck.c:622: glic_prepare_screen(GLIC_BLACK);
	xor	a, a
	push	af
	inc	sp
	call	_glic_prepare_screen
	inc	sp
;c_programs/apps/brainfuck.c:623: draw_editor_header(kind, key_row, key_col, status);
	ld	l, 7 (ix)
	ld	h, 8 (ix)
	push	hl
	ld	h, 6 (ix)
	ld	l, 5 (ix)
	push	hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_header
	pop	af
	pop	af
	inc	sp
;c_programs/apps/brainfuck.c:624: draw_editor_preview(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_preview
	inc	sp
;c_programs/apps/brainfuck.c:625: text_write(8u, 0u, "B DEL C MENU");
	ld	hl, #___str_5
	push	hl
	xor	a, a
	ld	d,a
	ld	e,#0x08
	push	de
	call	_text_write
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:626: draw_keyboard(key_row, key_col);
	ld	h, 6 (ix)
	ld	l, 5 (ix)
	push	hl
	call	_draw_keyboard
;c_programs/apps/brainfuck.c:627: text_write(15u, 0u, "AA RUN A UD SCR");
	ld	hl, #___str_6
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x0f
	push	de
	call	_text_write
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:628: }
	pop	ix
	ret
___str_5:
	.ascii "B DEL C MENU"
	.db 0x00
___str_6:
	.ascii "AA RUN A UD SCR"
	.db 0x00
;c_programs/apps/brainfuck.c:630: static void insert_char(unsigned char kind,
;	---------------------------------
; Function insert_char
; ---------------------------------
_insert_char:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;c_programs/apps/brainfuck.c:637: buffer = active_buffer(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_active_buffer
	inc	sp
	ex	(sp), hl
;c_programs/apps/brainfuck.c:638: len = active_len(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_active_len
	inc	sp
;c_programs/apps/brainfuck.c:639: capacity = active_capacity(kind);
	push	hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_active_capacity
	inc	sp
	ex	de, hl
	pop	bc
;c_programs/apps/brainfuck.c:640: if (len >= capacity) {
	ld	a, c
	sub	a, e
	ld	a, b
	sbc	a, d
;c_programs/apps/brainfuck.c:641: return;
	jr	NC, 00105$
;c_programs/apps/brainfuck.c:644: buffer[len] = typed_key(key_row, key_col);
	pop	hl
	push	hl
	add	hl, bc
	push	hl
	push	bc
	push	de
	ld	h, 6 (ix)
	ld	l, 5 (ix)
	push	hl
	call	_typed_key
	pop	af
	ld	a, l
	pop	de
	pop	bc
	pop	hl
	ld	(hl), a
;c_programs/apps/brainfuck.c:645: ++len;
	inc	bc
;c_programs/apps/brainfuck.c:646: set_active_len(kind, len);
	push	bc
	push	de
	push	bc
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_set_active_len
	pop	af
	inc	sp
	pop	de
	pop	bc
;c_programs/apps/brainfuck.c:647: if (len < capacity) {
	ld	a, c
	sub	a, e
	ld	a, b
	sbc	a, d
	jr	NC, 00105$
;c_programs/apps/brainfuck.c:648: buffer[len] = 0u;
	pop	hl
	push	hl
	add	hl, bc
	ld	(hl), #0x00
00105$:
;c_programs/apps/brainfuck.c:650: }
	ld	sp, ix
	pop	ix
	ret
;c_programs/apps/brainfuck.c:652: static void delete_char(unsigned char kind) {
;	---------------------------------
; Function delete_char
; ---------------------------------
_delete_char:
	push	ix
	ld	ix,#0
	add	ix,sp
;c_programs/apps/brainfuck.c:656: len = active_len(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_active_len
	inc	sp
	ex	de, hl
;c_programs/apps/brainfuck.c:657: if (len == 0u) {
	ld	a, d
	or	a, e
;c_programs/apps/brainfuck.c:658: return;
	jr	Z, 00103$
;c_programs/apps/brainfuck.c:661: buffer = active_buffer(kind);
	push	de
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_active_buffer
	inc	sp
	pop	de
;c_programs/apps/brainfuck.c:662: --len;
	dec	de
;c_programs/apps/brainfuck.c:663: set_active_len(kind, len);
	push	hl
	push	de
	push	de
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_set_active_len
	pop	af
	inc	sp
	pop	de
	pop	hl
;c_programs/apps/brainfuck.c:664: buffer[len] = 0u;
	add	hl, de
	ld	(hl), #0x00
00103$:
;c_programs/apps/brainfuck.c:665: }
	pop	ix
	ret
;c_programs/apps/brainfuck.c:667: static void copy_text_row(unsigned char from_row, unsigned char to_row) {
;	---------------------------------
; Function copy_text_row
; ---------------------------------
_copy_text_row:
;c_programs/apps/brainfuck.c:670: for (col = 0u; col < 16u; ++col) {
	ld	b, #0x00
00102$:
;c_programs/apps/brainfuck.c:671: text_put(to_row, col, *text_cell(from_row, col));
	push	bc
	push	bc
	inc	sp
	ld	hl, #5
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_text_cell
	pop	af
	pop	bc
	ld	a, (hl)
	push	bc
	push	af
	inc	sp
	push	bc
	inc	sp
	ld	hl, #7
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
	pop	bc
;c_programs/apps/brainfuck.c:670: for (col = 0u; col < 16u; ++col) {
	inc	b
	ld	a, b
	sub	a, #0x10
	jr	C, 00102$
;c_programs/apps/brainfuck.c:673: }
	ret
;c_programs/apps/brainfuck.c:675: static void output_scroll(void) {
;	---------------------------------
; Function output_scroll
; ---------------------------------
_output_scroll:
;c_programs/apps/brainfuck.c:678: for (row = 1u; row < 14u; ++row) {
	ld	a, #0x01
00102$:
;c_programs/apps/brainfuck.c:679: copy_text_row((unsigned char)(row + 1u), row);
	ld	b, a
	inc	b
	push	bc
	push	af
	inc	sp
	push	bc
	inc	sp
	call	_copy_text_row
	pop	af
	pop	bc
;c_programs/apps/brainfuck.c:678: for (row = 1u; row < 14u; ++row) {
	ld	a, b
	cp	a, #0x0e
	jr	C, 00102$
;c_programs/apps/brainfuck.c:681: text_clear_row(14u);
	ld	a, #0x0e
	push	af
	inc	sp
	call	_text_clear_row
	inc	sp
;c_programs/apps/brainfuck.c:682: output_row = 14u;
	ld	a, #0x0e
	ld	(#_output_row), a
;c_programs/apps/brainfuck.c:683: output_col = 0u;
	ld	a, #0x00
	ld	(#_output_col), a
;c_programs/apps/brainfuck.c:684: }
	ret
;c_programs/apps/brainfuck.c:686: static void output_newline(void) {
;	---------------------------------
; Function output_newline
; ---------------------------------
_output_newline:
;c_programs/apps/brainfuck.c:687: output_col = 0u;
	ld	a, #0x00
	ld	(#_output_col), a
;c_programs/apps/brainfuck.c:688: if (output_row < 14u) {
	ld	iy, #_output_row
	ld	a, 0 (iy)
	sub	a, #0x0e
	jp	NC,_output_scroll
;c_programs/apps/brainfuck.c:689: ++output_row;
	inc	0 (iy)
;c_programs/apps/brainfuck.c:691: output_scroll();
	ret
;c_programs/apps/brainfuck.c:693: }
	ret
;c_programs/apps/brainfuck.c:695: static void output_char(unsigned char ch) {
;	---------------------------------
; Function output_char
; ---------------------------------
_output_char:
;c_programs/apps/brainfuck.c:696: if (ch == '\n') {
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	sub	a, #0x0a
	jp	Z,_output_newline
;c_programs/apps/brainfuck.c:697: output_newline();
;c_programs/apps/brainfuck.c:698: return;
	jr	00102$
00102$:
;c_programs/apps/brainfuck.c:700: if ((ch < 32u) || (ch > 126u)) {
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	sub	a, #0x20
	jr	C, 00103$
	ld	a, #0x7e
	sub	a, 0 (iy)
	jr	NC, 00104$
00103$:
;c_programs/apps/brainfuck.c:701: ch = '?';
	ld	iy, #2
	add	iy, sp
	ld	0 (iy), #0x3f
00104$:
;c_programs/apps/brainfuck.c:703: if (output_col >= 16u) {
	ld	a,(#_output_col + 0)
	sub	a, #0x10
	jr	C, 00107$
;c_programs/apps/brainfuck.c:704: output_newline();
	call	_output_newline
00107$:
;c_programs/apps/brainfuck.c:706: text_put(output_row, output_col, ch);
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	ld	a,(#_output_col + 0)
	push	af
	inc	sp
	ld	a,(#_output_row + 0)
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
;c_programs/apps/brainfuck.c:707: ++output_col;
	ld	iy, #_output_col
	inc	0 (iy)
;c_programs/apps/brainfuck.c:708: }
	ret
;c_programs/apps/brainfuck.c:710: static void output_text(const char *text) {
;	---------------------------------
; Function output_text
; ---------------------------------
_output_text:
;c_programs/apps/brainfuck.c:711: while (*text != 0) {
	pop	de
	pop	bc
	push	bc
	push	de
00101$:
	ld	a, (bc)
	or	a, a
	ret	Z
;c_programs/apps/brainfuck.c:712: output_char((unsigned char)*text);
	push	bc
	push	af
	inc	sp
	call	_output_char
	inc	sp
	pop	bc
;c_programs/apps/brainfuck.c:713: ++text;
	inc	bc
;c_programs/apps/brainfuck.c:715: }
	jr	00101$
;c_programs/apps/brainfuck.c:717: static void output_clear(void) {
;	---------------------------------
; Function output_clear
; ---------------------------------
_output_clear:
;c_programs/apps/brainfuck.c:718: glic_prepare_screen(GLIC_BLACK);
	xor	a, a
	push	af
	inc	sp
	call	_glic_prepare_screen
	inc	sp
;c_programs/apps/brainfuck.c:719: text_write(0u, 0u, "BF RUN");
	ld	hl, #___str_7
	push	hl
	xor	a, a
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:720: text_write(15u, 0u, "C STOP ANY BACK");
	ld	hl, #___str_8
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x0f
	push	de
	call	_text_write
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:721: output_row = 1u;
	ld	a, #0x01
	ld	(#_output_row), a
;c_programs/apps/brainfuck.c:722: output_col = 0u;
	ld	a, #0x00
	ld	(#_output_col), a
;c_programs/apps/brainfuck.c:723: }
	ret
___str_7:
	.ascii "BF RUN"
	.db 0x00
___str_8:
	.ascii "C STOP ANY BACK"
	.db 0x00
;c_programs/apps/brainfuck.c:725: static void fail_compile(const char *message) {
;	---------------------------------
; Function fail_compile
; ---------------------------------
_fail_compile:
;c_programs/apps/brainfuck.c:726: if (compile_error == 0u) {
	ld	iy, #_compile_error
	ld	a, 0 (iy)
	or	a, a
	ret	NZ
;c_programs/apps/brainfuck.c:727: compile_error = 1u;
	ld	0 (iy), #0x01
;c_programs/apps/brainfuck.c:728: compile_message = message;
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	ld	(_compile_message+0), a
	ld	a, 1 (iy)
	ld	(_compile_message+1), a
;c_programs/apps/brainfuck.c:730: }
	ret
;c_programs/apps/brainfuck.c:732: static unsigned char emit_op(unsigned char op,
;	---------------------------------
; Function emit_op
; ---------------------------------
_emit_op:
	push	ix
	ld	ix,#0
	add	ix,sp
;c_programs/apps/brainfuck.c:735: if (code_len >= MAX_CODE) {
	ld	iy, #_code_len
	ld	a, 0 (iy)
	sub	a, #0x80
	ld	a, 1 (iy)
	sbc	a, #0x01
	jr	C, 00102$
;c_programs/apps/brainfuck.c:736: fail_compile("CODE FULL");
	ld	hl, #___str_9
	push	hl
	call	_fail_compile
	pop	af
;c_programs/apps/brainfuck.c:737: return 0u;
	ld	l, #0x00
	jr	00103$
00102$:
;c_programs/apps/brainfuck.c:740: code_op[code_len] = op;
	ld	bc, #_code_op+0
	ld	hl, (_code_len)
	add	hl, bc
	ld	a, 4 (ix)
	ld	(hl), a
;c_programs/apps/brainfuck.c:741: code_arg[code_len] = arg;
	ld	bc, #_code_arg+0
	ld	hl, (_code_len)
	add	hl, bc
	ld	a, 5 (ix)
	ld	(hl), a
;c_programs/apps/brainfuck.c:742: code_jump[code_len] = jump;
	ld	bc, #_code_jump+0
	ld	hl, (_code_len)
	add	hl, hl
	add	hl, bc
	ld	a, 6 (ix)
	ld	(hl), a
	inc	hl
	ld	a, 7 (ix)
	ld	(hl), a
;c_programs/apps/brainfuck.c:743: ++code_len;
	ld	hl, (_code_len)
	inc	hl
	ld	(_code_len), hl
;c_programs/apps/brainfuck.c:744: return 1u;
	ld	l, #0x01
00103$:
;c_programs/apps/brainfuck.c:745: }
	pop	ix
	ret
___str_9:
	.ascii "CODE FULL"
	.db 0x00
;c_programs/apps/brainfuck.c:747: static unsigned char emit_delta(unsigned char op, unsigned char delta) {
;	---------------------------------
; Function emit_delta
; ---------------------------------
_emit_delta:
	push	ix
	ld	ix,#0
	add	ix,sp
;c_programs/apps/brainfuck.c:750: if (code_len != 0u) {
	ld	iy, #_code_len
	ld	a, 1 (iy)
	or	a, 0 (iy)
	jr	Z, 00104$
;c_programs/apps/brainfuck.c:751: last = (unsigned int)(code_len - 1u);
	ld	bc, (_code_len)
	dec	bc
;c_programs/apps/brainfuck.c:752: if (code_op[last] == op) {
	ld	hl, #_code_op
	add	hl, bc
	ld	a,4 (ix)
	sub	a,(hl)
	jr	NZ, 00104$
;c_programs/apps/brainfuck.c:753: code_arg[last] = (unsigned char)(code_arg[last] + delta);
	ld	hl, #_code_arg
	add	hl, bc
	ld	a, (hl)
	add	a, 5 (ix)
	ld	(hl), a
;c_programs/apps/brainfuck.c:754: return 1u;
	ld	l, #0x01
	jr	00105$
00104$:
;c_programs/apps/brainfuck.c:757: return emit_op(op, delta, 0u);
	ld	hl, #0x0000
	push	hl
	ld	h, 5 (ix)
	ld	l, 4 (ix)
	push	hl
	call	_emit_op
	pop	af
	pop	af
00105$:
;c_programs/apps/brainfuck.c:758: }
	pop	ix
	ret
;c_programs/apps/brainfuck.c:760: static unsigned char compile_program(void) {
;	---------------------------------
; Function compile_program
; ---------------------------------
_compile_program:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
	push	af
	dec	sp
;c_programs/apps/brainfuck.c:767: code_len = 0u;
	ld	hl, #0x0000
	ld	(_code_len), hl
;c_programs/apps/brainfuck.c:768: compile_error = 0u;
	ld	hl, #_compile_error
	ld	(hl), #0x00
;c_programs/apps/brainfuck.c:769: compile_message = "";
	ld	hl, #___str_10+0
	ld	(_compile_message), hl
;c_programs/apps/brainfuck.c:770: loop_depth = 0u;
	ld	-3 (ix), #0
;c_programs/apps/brainfuck.c:771: len = source_len();
	call	_source_len
	ex	(sp), hl
;c_programs/apps/brainfuck.c:773: for (pos = 0u; pos < len; ++pos) {
	xor	a, a
	ld	-2 (ix), a
	ld	-1 (ix), a
00148$:
	ld	a, -2 (ix)
	sub	a, -5 (ix)
	ld	a, -1 (ix)
	sbc	a, -4 (ix)
	jp	NC, 00144$
;c_programs/apps/brainfuck.c:774: ch = SOURCE_TEXT[pos];
	ld	l, -2 (ix)
	ld	h, -1 (ix)
	ld	de, #0x6008
	add	hl, de
	ld	a, (hl)
;c_programs/apps/brainfuck.c:775: if (ch == '+') {
	cp	a, #0x2b
	jr	NZ, 00142$
;c_programs/apps/brainfuck.c:776: if (emit_delta(OP_ADD, 1u) == 0u) {
	ld	de, #0x0101
	push	de
	call	_emit_delta
	pop	af
	ld	a, l
;c_programs/apps/brainfuck.c:777: return 0u;
	or	a,a
	jp	NZ,00149$
	ld	l,a
	jp	00150$
00142$:
;c_programs/apps/brainfuck.c:779: } else if (ch == '-') {
	cp	a, #0x2d
	jr	NZ, 00139$
;c_programs/apps/brainfuck.c:780: if (emit_delta(OP_ADD, 255u) == 0u) {
	ld	de, #0xff01
	push	de
	call	_emit_delta
	pop	af
	ld	a, l
;c_programs/apps/brainfuck.c:781: return 0u;
	or	a,a
	jp	NZ,00149$
	ld	l,a
	jp	00150$
00139$:
;c_programs/apps/brainfuck.c:783: } else if (ch == '>') {
	cp	a, #0x3e
	jr	NZ, 00136$
;c_programs/apps/brainfuck.c:784: if (emit_delta(OP_MOVE, 1u) == 0u) {
	ld	de, #0x0102
	push	de
	call	_emit_delta
	pop	af
	ld	a, l
;c_programs/apps/brainfuck.c:785: return 0u;
	or	a,a
	jp	NZ,00149$
	ld	l,a
	jp	00150$
00136$:
;c_programs/apps/brainfuck.c:787: } else if (ch == '<') {
	cp	a, #0x3c
	jr	NZ, 00133$
;c_programs/apps/brainfuck.c:788: if (emit_delta(OP_MOVE, 255u) == 0u) {
	ld	de, #0xff02
	push	de
	call	_emit_delta
	pop	af
	ld	a, l
;c_programs/apps/brainfuck.c:789: return 0u;
	or	a,a
	jp	NZ,00149$
	ld	l,a
	jp	00150$
00133$:
;c_programs/apps/brainfuck.c:791: } else if (ch == '.') {
	cp	a, #0x2e
	jr	NZ, 00130$
;c_programs/apps/brainfuck.c:792: if (emit_op(OP_OUT, 0u, 0u) == 0u) {
	ld	hl, #0x0000
	push	hl
	xor	a, a
	ld	d,a
	ld	e,#0x03
	push	de
	call	_emit_op
	pop	af
	pop	af
	ld	a, l
;c_programs/apps/brainfuck.c:793: return 0u;
	or	a,a
	jp	NZ,00149$
	ld	l,a
	jp	00150$
00130$:
;c_programs/apps/brainfuck.c:795: } else if (ch == ',') {
	cp	a, #0x2c
	jr	NZ, 00127$
;c_programs/apps/brainfuck.c:796: if (emit_op(OP_IN, 0u, 0u) == 0u) {
	ld	hl, #0x0000
	push	hl
	xor	a, a
	ld	d,a
	ld	e,#0x04
	push	de
	call	_emit_op
	pop	af
	pop	af
	ld	a, l
;c_programs/apps/brainfuck.c:797: return 0u;
	or	a,a
	jp	NZ,00149$
	ld	l,a
	jp	00150$
00127$:
;c_programs/apps/brainfuck.c:799: } else if (ch == '[') {
	cp	a, #0x5b
	jr	NZ, 00124$
;c_programs/apps/brainfuck.c:800: if (loop_depth >= MAX_LOOP_STACK) {
	ld	a, -3 (ix)
	sub	a, #0x10
	jr	C, 00114$
;c_programs/apps/brainfuck.c:801: fail_compile("LOOP DEEP");
	ld	hl, #___str_11
	push	hl
	call	_fail_compile
	pop	af
;c_programs/apps/brainfuck.c:802: return 0u;
	ld	l, #0x00
	jp	00150$
00114$:
;c_programs/apps/brainfuck.c:804: if (emit_op(OP_JZ, 0u, 0u) == 0u) {
	ld	hl, #0x0000
	push	hl
	xor	a, a
	ld	d,a
	ld	e,#0x05
	push	de
	call	_emit_op
	pop	af
	pop	af
	ld	a, l
;c_programs/apps/brainfuck.c:805: return 0u;
	or	a,a
	jr	NZ, 00116$
	ld	l,a
	jp	00150$
00116$:
;c_programs/apps/brainfuck.c:807: compile_loop_stack[loop_depth] = (unsigned int)(code_len - 1u);
	ld	e, -3 (ix)
	ld	d, #0x00
	ex	de, hl
	add	hl, hl
	ex	de, hl
	ld	hl, #_compile_loop_stack
	add	hl, de
	ex	de, hl
	ld	bc, (_code_len)
	dec	bc
	ld	a, c
	ld	(de), a
	inc	de
	ld	a, b
	ld	(de), a
;c_programs/apps/brainfuck.c:808: ++loop_depth;
	inc	-3 (ix)
	jr	00149$
00124$:
;c_programs/apps/brainfuck.c:809: } else if (ch == ']') {
	sub	a, #0x5d
	jr	NZ, 00149$
;c_programs/apps/brainfuck.c:810: if (loop_depth == 0u) {
	ld	a, -3 (ix)
	or	a, a
	jr	NZ, 00118$
;c_programs/apps/brainfuck.c:811: fail_compile("EXTRA ]");
	ld	hl, #___str_12
	push	hl
	call	_fail_compile
	pop	af
;c_programs/apps/brainfuck.c:812: return 0u;
	ld	l, #0x00
	jr	00150$
00118$:
;c_programs/apps/brainfuck.c:814: --loop_depth;
	dec	-3 (ix)
;c_programs/apps/brainfuck.c:815: open_pc = compile_loop_stack[loop_depth];
	ld	l, -3 (ix)
	ld	h, #0x00
	add	hl, hl
	ld	de, #_compile_loop_stack
	add	hl, de
	ld	a, (hl)
	inc	hl
	ld	h, (hl)
	ld	l, a
;c_programs/apps/brainfuck.c:816: if (emit_op(OP_JNZ, 0u, open_pc) == 0u) {
	push	hl
	push	hl
	xor	a, a
	ld	d,a
	ld	e,#0x06
	push	de
	call	_emit_op
	pop	af
	pop	af
	ld	a, l
	pop	hl
;c_programs/apps/brainfuck.c:817: return 0u;
	or	a,a
	jr	NZ, 00120$
	ld	l,a
	jr	00150$
00120$:
;c_programs/apps/brainfuck.c:819: code_jump[open_pc] = code_len;
	add	hl, hl
	ld	de, #_code_jump
	add	hl, de
	ld	iy, #_code_len
	ld	a, 0 (iy)
	ld	(hl), a
	inc	hl
	ld	a, 1 (iy)
	ld	(hl), a
00149$:
;c_programs/apps/brainfuck.c:773: for (pos = 0u; pos < len; ++pos) {
	inc	-2 (ix)
	jp	NZ,00148$
	inc	-1 (ix)
	jp	00148$
00144$:
;c_programs/apps/brainfuck.c:823: if (loop_depth != 0u) {
	ld	a, -3 (ix)
	or	a, a
	jr	Z, 00146$
;c_programs/apps/brainfuck.c:824: fail_compile("OPEN [");
	ld	hl, #___str_13
	push	hl
	call	_fail_compile
	pop	af
;c_programs/apps/brainfuck.c:825: return 0u;
	ld	l, #0x00
	jr	00150$
00146$:
;c_programs/apps/brainfuck.c:827: return 1u;
	ld	l, #0x01
00150$:
;c_programs/apps/brainfuck.c:828: }
	ld	sp, ix
	pop	ix
	ret
___str_10:
	.db 0x00
___str_11:
	.ascii "LOOP DEEP"
	.db 0x00
___str_12:
	.ascii "EXTRA ]"
	.db 0x00
___str_13:
	.ascii "OPEN ["
	.db 0x00
;c_programs/apps/brainfuck.c:830: static void clear_tape(void) {
;	---------------------------------
; Function clear_tape
; ---------------------------------
_clear_tape:
;c_programs/apps/brainfuck.c:833: for (i = 0u; i < 256u; ++i) {
	ld	bc, #0x0000
00102$:
;c_programs/apps/brainfuck.c:834: tape[i] = 0u;
	ld	hl, #_tape
	add	hl, bc
	ld	(hl), #0x00
;c_programs/apps/brainfuck.c:833: for (i = 0u; i < 256u; ++i) {
	inc	bc
	ld	a, b
	sub	a, #0x01
	jr	C, 00102$
;c_programs/apps/brainfuck.c:836: }
	ret
;c_programs/apps/brainfuck.c:838: static unsigned char read_input_byte(unsigned int *input_pos,
;	---------------------------------
; Function read_input_byte
; ---------------------------------
_read_input_byte:
	push	ix
	ld	ix,#0
	add	ix,sp
;c_programs/apps/brainfuck.c:842: if (*input_pos >= len) {
	ld	b, 4 (ix)
	ld	c, 5 (ix)
	ld	l, b
	ld	h, c
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	ld	a, e
	sub	a, 6 (ix)
	ld	a, d
	sbc	a, 7 (ix)
	jr	C, 00102$
;c_programs/apps/brainfuck.c:843: return 0u;
	ld	l, #0x00
	jr	00103$
00102$:
;c_programs/apps/brainfuck.c:845: ch = INPUT_TEXT[*input_pos];
	ld	hl, #0x6208
	add	hl, de
	ld	a, (hl)
;c_programs/apps/brainfuck.c:846: ++(*input_pos);
	inc	de
	ld	l, b
	ld	h, c
	ld	(hl), e
	inc	hl
	ld	(hl), d
;c_programs/apps/brainfuck.c:847: return ch;
	ld	l, a
00103$:
;c_programs/apps/brainfuck.c:848: }
	pop	ix
	ret
;c_programs/apps/brainfuck.c:850: static void execute_program(void) {
;	---------------------------------
; Function execute_program
; ---------------------------------
_execute_program:
	push	ix
	ld	ix,#0
	add	ix,sp
	ld	hl, #-8
	add	hl, sp
	ld	sp, hl
;c_programs/apps/brainfuck.c:858: pc = 0u;
	ld	bc, #0x0000
;c_programs/apps/brainfuck.c:859: input_pos = 0u;
	ld	hl, #0x0000
	ex	(sp), hl
;c_programs/apps/brainfuck.c:860: input_size = input_len();
	push	bc
	call	_input_len
	ex	de, hl
	pop	bc
;c_programs/apps/brainfuck.c:861: data_ptr = 0u;
	ld	-2 (ix), #0
;c_programs/apps/brainfuck.c:862: stopped = 0u;
	ld	-6 (ix), #0
;c_programs/apps/brainfuck.c:864: clear_tape();
	push	bc
	push	de
	call	_clear_tape
	pop	de
	pop	bc
;c_programs/apps/brainfuck.c:866: while (pc < code_len) {
	ld	-1 (ix), #0
00129$:
	ld	hl, #_code_len
	ld	a, c
	sub	a, (hl)
	ld	a, b
	inc	hl
	sbc	a, (hl)
	jp	NC, 00131$
;c_programs/apps/brainfuck.c:867: ++poll;
	inc	-1 (ix)
;c_programs/apps/brainfuck.c:868: if ((poll & 31u) == 0u) {
	ld	a, -1 (ix)
	and	a, #0x1f
	jr	NZ, 00104$
;c_programs/apps/brainfuck.c:869: if (glic_button_pressed(glic_read_buttons(), GLIC_BTN_C) != 0u) {
	push	bc
	push	de
	call	_glic_read_buttons
	ld	a, l
	ld	h, #0x20
	push	hl
	inc	sp
	push	af
	inc	sp
	call	_glic_button_pressed
	pop	af
	ld	a, l
	pop	de
	pop	bc
	or	a, a
	jr	Z, 00104$
;c_programs/apps/brainfuck.c:870: stopped = 1u;
	ld	-6 (ix), #0x01
;c_programs/apps/brainfuck.c:871: break;
	jp	00131$
00104$:
;c_programs/apps/brainfuck.c:875: if (code_op[pc] == OP_ADD) {
	ld	hl, #_code_op
	add	hl, bc
	ld	l, (hl)
;c_programs/apps/brainfuck.c:877: ++pc;
	ld	a, c
	add	a, #0x01
	ld	-5 (ix), a
	ld	a, b
	adc	a, #0x00
	ld	-4 (ix), a
;c_programs/apps/brainfuck.c:875: if (code_op[pc] == OP_ADD) {
	ld	a, l
	dec	a
	jr	NZ, 00127$
;c_programs/apps/brainfuck.c:876: tape[data_ptr] = (unsigned char)(tape[data_ptr] + code_arg[pc]);
	ld	a, #<(_tape)
	add	a, -2 (ix)
	ld	l, a
	ld	a, #>(_tape)
	adc	a, #0x00
	ld	h, a
	ld	a, (hl)
	ld	-3 (ix), a
	ld	a, #<(_code_arg)
	add	a, c
	ld	c, a
	ld	a, #>(_code_arg)
	adc	a, b
	ld	b, a
	ld	a, (bc)
	add	a, -3 (ix)
	ld	(hl), a
;c_programs/apps/brainfuck.c:877: ++pc;
	ld	c, -5 (ix)
	ld	b, -4 (ix)
	jr	00129$
00127$:
;c_programs/apps/brainfuck.c:878: } else if (code_op[pc] == OP_MOVE) {
	ld	a, l
	sub	a, #0x02
	jr	NZ, 00124$
;c_programs/apps/brainfuck.c:879: data_ptr = (unsigned char)(data_ptr + code_arg[pc]);
	ld	hl, #_code_arg
	add	hl, bc
	ld	a, (hl)
	add	a, -2 (ix)
	ld	-2 (ix), a
;c_programs/apps/brainfuck.c:880: ++pc;
	ld	c, -5 (ix)
	ld	b, -4 (ix)
	jp	00129$
00124$:
;c_programs/apps/brainfuck.c:881: } else if (code_op[pc] == OP_OUT) {
	ld	a, l
	sub	a, #0x03
	jr	NZ, 00121$
;c_programs/apps/brainfuck.c:882: output_char(tape[data_ptr]);
	ld	a, #<(_tape)
	add	a, -2 (ix)
	ld	c, a
	ld	a, #>(_tape)
	adc	a, #0x00
	ld	b, a
	ld	a, (bc)
	push	de
	push	af
	inc	sp
	call	_output_char
	inc	sp
	pop	de
;c_programs/apps/brainfuck.c:883: ++pc;
	ld	c, -5 (ix)
	ld	b, -4 (ix)
	jp	00129$
00121$:
;c_programs/apps/brainfuck.c:884: } else if (code_op[pc] == OP_IN) {
	ld	a, l
	sub	a, #0x04
	jr	NZ, 00118$
;c_programs/apps/brainfuck.c:885: tape[data_ptr] = read_input_byte(&input_pos, input_size);
	ld	a, #<(_tape)
	add	a, -2 (ix)
	ld	c, a
	ld	a, #>(_tape)
	adc	a, #0x00
	ld	b, a
	ld	hl, #0
	add	hl, sp
	push	bc
	push	de
	push	de
	push	hl
	call	_read_input_byte
	pop	af
	pop	af
	ld	a, l
	pop	de
	pop	bc
	ld	(bc), a
;c_programs/apps/brainfuck.c:886: ++pc;
	ld	c, -5 (ix)
	ld	b, -4 (ix)
	jp	00129$
00118$:
;c_programs/apps/brainfuck.c:889: pc = code_jump[pc];
	sla	c
	rl	b
;c_programs/apps/brainfuck.c:887: } else if (code_op[pc] == OP_JZ) {
	ld	a, l
	sub	a, #0x05
	jr	NZ, 00115$
;c_programs/apps/brainfuck.c:888: if (tape[data_ptr] == 0u) {
	ld	a, #<(_tape)
	add	a, -2 (ix)
	ld	l, a
	ld	a, #>(_tape)
	adc	a, #0x00
	ld	h, a
	ld	a, (hl)
	or	a, a
	jr	NZ, 00106$
;c_programs/apps/brainfuck.c:889: pc = code_jump[pc];
	ld	hl, #_code_jump
	add	hl, bc
	ld	-4 (ix), l
	ld	-3 (ix), h
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	jp	00129$
00106$:
;c_programs/apps/brainfuck.c:891: ++pc;
	ld	c, -5 (ix)
	ld	b, -4 (ix)
	jp	00129$
00115$:
;c_programs/apps/brainfuck.c:893: } else if (code_op[pc] == OP_JNZ) {
	ld	a, l
	sub	a, #0x06
	jr	NZ, 00112$
;c_programs/apps/brainfuck.c:894: if (tape[data_ptr] != 0u) {
	ld	a, #<(_tape)
	add	a, -2 (ix)
	ld	l, a
	ld	a, #>(_tape)
	adc	a, #0x00
	ld	h, a
	ld	a, (hl)
	or	a, a
	jr	Z, 00109$
;c_programs/apps/brainfuck.c:895: pc = code_jump[pc];
	ld	hl, #_code_jump
	add	hl, bc
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	jp	00129$
00109$:
;c_programs/apps/brainfuck.c:897: ++pc;
	ld	c, -5 (ix)
	ld	b, -4 (ix)
	jp	00129$
00112$:
;c_programs/apps/brainfuck.c:900: ++pc;
	ld	c, -5 (ix)
	ld	b, -4 (ix)
	jp	00129$
00131$:
;c_programs/apps/brainfuck.c:904: output_newline();
	call	_output_newline
;c_programs/apps/brainfuck.c:905: if (stopped != 0u) {
	ld	a, -6 (ix)
	or	a, a
	jr	Z, 00133$
;c_programs/apps/brainfuck.c:906: output_text("STOP");
	ld	hl, #___str_14
	push	hl
	call	_output_text
	pop	af
	jr	00134$
00133$:
;c_programs/apps/brainfuck.c:908: output_text("DONE");
	ld	hl, #___str_15
	push	hl
	call	_output_text
	pop	af
00134$:
;c_programs/apps/brainfuck.c:910: wait_for_any_key();
	call	_wait_for_any_key
;c_programs/apps/brainfuck.c:911: }
	ld	sp, ix
	pop	ix
	ret
___str_14:
	.ascii "STOP"
	.db 0x00
___str_15:
	.ascii "DONE"
	.db 0x00
;c_programs/apps/brainfuck.c:913: static void run_program(void) {
;	---------------------------------
; Function run_program
; ---------------------------------
_run_program:
;c_programs/apps/brainfuck.c:914: wait_for_release();
	call	_wait_for_release
;c_programs/apps/brainfuck.c:915: output_clear();
	call	_output_clear
;c_programs/apps/brainfuck.c:916: text_write(1u, 0u, "COMPILING");
	ld	hl, #___str_16
	push	hl
	xor	a, a
	ld	d,a
	ld	e,#0x01
	push	de
	call	_text_write
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:917: if (compile_program() == 0u) {
	call	_compile_program
	ld	a, l
	or	a, a
	jr	NZ, 00102$
;c_programs/apps/brainfuck.c:918: text_clear_row(1u);
	ld	a, #0x01
	push	af
	inc	sp
	call	_text_clear_row
	inc	sp
;c_programs/apps/brainfuck.c:919: output_row = 1u;
	ld	hl, #_output_row
	ld	(hl), #0x01
;c_programs/apps/brainfuck.c:920: output_col = 0u;
	ld	hl, #_output_col
	ld	(hl), #0x00
;c_programs/apps/brainfuck.c:921: output_text("COMPILE ERR");
	ld	hl, #___str_17
	push	hl
	call	_output_text
	pop	af
;c_programs/apps/brainfuck.c:922: output_newline();
	call	_output_newline
;c_programs/apps/brainfuck.c:923: output_text(compile_message);
	ld	hl, (_compile_message)
	push	hl
	call	_output_text
	pop	af
;c_programs/apps/brainfuck.c:924: wait_for_any_key();
;c_programs/apps/brainfuck.c:925: return;
	jp	_wait_for_any_key
00102$:
;c_programs/apps/brainfuck.c:928: text_clear_row(1u);
	ld	a, #0x01
	push	af
	inc	sp
	call	_text_clear_row
	inc	sp
;c_programs/apps/brainfuck.c:929: output_row = 1u;
	ld	iy, #_output_row
	ld	0 (iy), #0x01
;c_programs/apps/brainfuck.c:930: output_col = 0u;
	ld	iy, #_output_col
	ld	0 (iy), #0x00
;c_programs/apps/brainfuck.c:931: execute_program();
;c_programs/apps/brainfuck.c:932: }
	jp	_execute_program
___str_16:
	.ascii "COMPILING"
	.db 0x00
___str_17:
	.ascii "COMPILE ERR"
	.db 0x00
;c_programs/apps/brainfuck.c:934: static void run_editor(unsigned char kind) {
;	---------------------------------
; Function run_editor
; ---------------------------------
_run_editor:
	push	ix
	ld	ix,#0
	add	ix,sp
	ld	hl, #-6
	add	hl, sp
	ld	sp, hl
;c_programs/apps/brainfuck.c:944: key_row = 0u;
	ld	-2 (ix), #0
;c_programs/apps/brainfuck.c:945: key_col = 0u;
	ld	-1 (ix), #0
;c_programs/apps/brainfuck.c:946: a_prefix = 0u;
	ld	-6 (ix), #0
;c_programs/apps/brainfuck.c:947: editor_view_to_end(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_editor_view_to_end
	inc	sp
;c_programs/apps/brainfuck.c:948: status = "";
;c_programs/apps/brainfuck.c:949: draw_editor_shell(kind, key_row, key_col, status);
	ld	hl, #___str_18
	push	hl
	xor	a, a
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_shell
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:950: last_buttons = GLIC_BUTTONS_NONE;
	ld	-4 (ix), #0xff
	inc	sp
;c_programs/apps/brainfuck.c:952: while (1) {
00147$:
;c_programs/apps/brainfuck.c:953: buttons = glic_read_buttons();
	call	_glic_read_buttons
	ld	-5 (ix), l
;c_programs/apps/brainfuck.c:954: if (button_edge(buttons, last_buttons, GLIC_BTN_UP) != 0u) {
	ld	h, #0x02
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	-3 (ix), l
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00144$
;c_programs/apps/brainfuck.c:955: if (a_prefix != 0u) {
	ld	a, -6 (ix)
	or	a, a
	jr	Z, 00105$
;c_programs/apps/brainfuck.c:956: editor_scroll_up(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_editor_scroll_up
;c_programs/apps/brainfuck.c:957: a_prefix = 0u;
	ld	-6 (ix), #0
	inc	sp
;c_programs/apps/brainfuck.c:958: status = "SC";
;c_programs/apps/brainfuck.c:959: draw_editor_header(kind, key_row, key_col, status);
	ld	hl, #___str_19
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_header
	pop	af
	pop	af
	inc	sp
;c_programs/apps/brainfuck.c:960: draw_editor_preview(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_preview
	inc	sp
	jp	00145$
00105$:
;c_programs/apps/brainfuck.c:962: old_row = key_row;
	ld	a, -2 (ix)
	ld	-4 (ix), a
;c_programs/apps/brainfuck.c:963: old_col = key_col;
	ld	a, -1 (ix)
	ld	-3 (ix), a
;c_programs/apps/brainfuck.c:964: if (key_row == 0u) {
	ld	a, -2 (ix)
	or	a, a
	jr	NZ, 00102$
;c_programs/apps/brainfuck.c:965: key_row = (unsigned char)(KEY_ROWS - 1u);
	ld	-2 (ix), #0x05
	jr	00103$
00102$:
;c_programs/apps/brainfuck.c:967: --key_row;
	dec	-2 (ix)
00103$:
;c_programs/apps/brainfuck.c:969: status = "";
;c_programs/apps/brainfuck.c:970: draw_editor_header(kind, key_row, key_col, status);
	ld	hl, #___str_18
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_header
	pop	af
;c_programs/apps/brainfuck.c:971: move_editor_key(old_row, old_col, key_row, key_col);
	inc	sp
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	ex	(sp),hl
	ld	h, -3 (ix)
	ld	l, -4 (ix)
	push	hl
	call	_move_editor_key
	pop	af
	pop	af
	jp	00145$
00144$:
;c_programs/apps/brainfuck.c:973: } else if (button_edge(buttons, last_buttons, GLIC_BTN_DOWN) != 0u) {
	ld	h, #0x08
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00141$
;c_programs/apps/brainfuck.c:974: if (a_prefix != 0u) {
	ld	a, -6 (ix)
	or	a, a
	jr	Z, 00110$
;c_programs/apps/brainfuck.c:975: editor_scroll_down(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_editor_scroll_down
;c_programs/apps/brainfuck.c:976: a_prefix = 0u;
	ld	-6 (ix), #0
	inc	sp
;c_programs/apps/brainfuck.c:977: status = "SC";
;c_programs/apps/brainfuck.c:978: draw_editor_header(kind, key_row, key_col, status);
	ld	hl, #___str_19
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_header
	pop	af
	pop	af
	inc	sp
;c_programs/apps/brainfuck.c:979: draw_editor_preview(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_preview
	inc	sp
	jp	00145$
00110$:
;c_programs/apps/brainfuck.c:981: old_row = key_row;
	ld	a, -2 (ix)
	ld	-4 (ix), a
;c_programs/apps/brainfuck.c:982: old_col = key_col;
	ld	a, -1 (ix)
	ld	-3 (ix), a
;c_programs/apps/brainfuck.c:983: ++key_row;
	inc	-2 (ix)
;c_programs/apps/brainfuck.c:984: if (key_row >= KEY_ROWS) {
	ld	a, -2 (ix)
	sub	a, #0x06
	jr	C, 00108$
;c_programs/apps/brainfuck.c:985: key_row = 0u;
	ld	-2 (ix), #0
00108$:
;c_programs/apps/brainfuck.c:987: status = "";
;c_programs/apps/brainfuck.c:988: draw_editor_header(kind, key_row, key_col, status);
	ld	hl, #___str_18
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_header
	pop	af
;c_programs/apps/brainfuck.c:989: move_editor_key(old_row, old_col, key_row, key_col);
	inc	sp
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	ex	(sp),hl
	ld	h, -3 (ix)
	ld	l, -4 (ix)
	push	hl
	call	_move_editor_key
	pop	af
	pop	af
	jp	00145$
00141$:
;c_programs/apps/brainfuck.c:991: } else if (button_edge(buttons, last_buttons, GLIC_BTN_LEFT) != 0u) {
	ld	h, #0x10
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00138$
;c_programs/apps/brainfuck.c:992: a_prefix = 0u;
	ld	-6 (ix), #0
;c_programs/apps/brainfuck.c:993: old_row = key_row;
	ld	a, -2 (ix)
	ld	-3 (ix), a
;c_programs/apps/brainfuck.c:994: old_col = key_col;
	ld	c, -1 (ix)
;c_programs/apps/brainfuck.c:995: if (key_col == 0u) {
	ld	a, -1 (ix)
	or	a, a
	jr	NZ, 00113$
;c_programs/apps/brainfuck.c:996: key_col = (unsigned char)(KEY_COLS - 1u);
	ld	-1 (ix), #0x07
	jr	00114$
00113$:
;c_programs/apps/brainfuck.c:998: --key_col;
	dec	-1 (ix)
00114$:
;c_programs/apps/brainfuck.c:1000: status = "";
;c_programs/apps/brainfuck.c:1001: draw_editor_header(kind, key_row, key_col, status);
	push	bc
	ld	hl, #___str_18
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_header
	pop	af
	pop	af
	inc	sp
	pop	bc
;c_programs/apps/brainfuck.c:1002: move_editor_key(old_row, old_col, key_row, key_col);
	ld	a, -1 (ix)
	push	af
	inc	sp
	ld	b, -2 (ix)
	push	bc
	ld	a, -3 (ix)
	push	af
	inc	sp
	call	_move_editor_key
	pop	af
	pop	af
	jp	00145$
00138$:
;c_programs/apps/brainfuck.c:1003: } else if (button_edge(buttons, last_buttons, GLIC_BTN_RIGHT) != 0u) {
	ld	h, #0x04
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00135$
;c_programs/apps/brainfuck.c:1004: a_prefix = 0u;
	ld	-6 (ix), #0
;c_programs/apps/brainfuck.c:1005: old_row = key_row;
	ld	b, -2 (ix)
;c_programs/apps/brainfuck.c:1006: old_col = key_col;
	ld	d, -1 (ix)
;c_programs/apps/brainfuck.c:1007: ++key_col;
	inc	-1 (ix)
;c_programs/apps/brainfuck.c:1008: if (key_col >= KEY_COLS) {
	ld	a, -1 (ix)
	sub	a, #0x08
	jr	C, 00116$
;c_programs/apps/brainfuck.c:1009: key_col = 0u;
	ld	-1 (ix), #0
00116$:
;c_programs/apps/brainfuck.c:1011: status = "";
;c_programs/apps/brainfuck.c:1012: draw_editor_header(kind, key_row, key_col, status);
	push	bc
	push	de
	ld	hl, #___str_18
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_header
	pop	af
	pop	af
	inc	sp
	pop	de
	pop	bc
;c_programs/apps/brainfuck.c:1013: move_editor_key(old_row, old_col, key_row, key_col);
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	ld	e, b
	push	de
	call	_move_editor_key
	pop	af
	pop	af
	jp	00145$
00135$:
;c_programs/apps/brainfuck.c:1014: } else if (button_edge(buttons, last_buttons, GLIC_BTN_CENTER) != 0u) {
	ld	h, #0x01
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00132$
;c_programs/apps/brainfuck.c:1015: a_prefix = 0u;
	ld	-6 (ix), #0
;c_programs/apps/brainfuck.c:1016: if (active_len(kind) < active_capacity(kind)) {
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_active_len
	inc	sp
	push	hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_active_capacity
	inc	sp
	ex	de, hl
	pop	hl
	xor	a, a
	sbc	hl, de
	jr	NC, 00118$
;c_programs/apps/brainfuck.c:1017: insert_char(kind, key_row, key_col);
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_insert_char
	pop	af
	inc	sp
;c_programs/apps/brainfuck.c:1018: editor_view_to_end(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_editor_view_to_end
	inc	sp
;c_programs/apps/brainfuck.c:1019: status = "";
	ld	bc, #___str_18
	jr	00119$
00118$:
;c_programs/apps/brainfuck.c:1021: status = "FL";
	ld	bc, #___str_20
00119$:
;c_programs/apps/brainfuck.c:1023: draw_editor_header(kind, key_row, key_col, status);
	push	bc
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_header
	pop	af
	pop	af
	inc	sp
;c_programs/apps/brainfuck.c:1024: draw_editor_preview(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_preview
	inc	sp
	jp	00145$
00132$:
;c_programs/apps/brainfuck.c:1025: } else if (button_edge(buttons, last_buttons, GLIC_BTN_B) != 0u) {
	ld	h, #0x40
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00129$
;c_programs/apps/brainfuck.c:1026: a_prefix = 0u;
	ld	-6 (ix), #0
;c_programs/apps/brainfuck.c:1027: delete_char(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_delete_char
	inc	sp
;c_programs/apps/brainfuck.c:1028: editor_view_to_end(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_editor_view_to_end
	inc	sp
;c_programs/apps/brainfuck.c:1029: status = "";
;c_programs/apps/brainfuck.c:1030: draw_editor_header(kind, key_row, key_col, status);
	ld	hl, #___str_18
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_header
	pop	af
	pop	af
	inc	sp
;c_programs/apps/brainfuck.c:1031: draw_editor_preview(kind);
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_preview
	inc	sp
	jr	00145$
00129$:
;c_programs/apps/brainfuck.c:1032: } else if (button_edge(buttons, last_buttons, GLIC_BTN_A) != 0u) {
	ld	h, #0x80
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00126$
;c_programs/apps/brainfuck.c:1033: if (a_prefix != 0u) {
	ld	a, -6 (ix)
	or	a, a
	jr	Z, 00121$
;c_programs/apps/brainfuck.c:1034: a_prefix = 0u;
	ld	-6 (ix), #0
;c_programs/apps/brainfuck.c:1035: run_program();
	call	_run_program
;c_programs/apps/brainfuck.c:1036: status = "";
;c_programs/apps/brainfuck.c:1037: draw_editor_shell(kind, key_row, key_col, status);
	ld	hl, #___str_18
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_shell
	pop	af
	pop	af
	inc	sp
;c_programs/apps/brainfuck.c:1038: last_buttons = GLIC_BUTTONS_NONE;
	jr	00145$
00121$:
;c_programs/apps/brainfuck.c:1040: a_prefix = 1u;
	ld	-6 (ix), #0x01
;c_programs/apps/brainfuck.c:1041: status = "A?";
;c_programs/apps/brainfuck.c:1042: draw_editor_header(kind, key_row, key_col, status);
	ld	hl, #___str_21
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_draw_editor_header
	pop	af
	pop	af
	inc	sp
	jr	00145$
00126$:
;c_programs/apps/brainfuck.c:1044: } else if (button_edge(buttons, last_buttons, GLIC_BTN_C) != 0u) {
	ld	h, #0x20
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00145$
;c_programs/apps/brainfuck.c:1045: wait_for_release();
	call	_wait_for_release
;c_programs/apps/brainfuck.c:1046: return;
	jr	00149$
00145$:
;c_programs/apps/brainfuck.c:1049: last_buttons = buttons;
	ld	a, -5 (ix)
	ld	-4 (ix), a
;c_programs/apps/brainfuck.c:1050: glic_delay(LOOP_DELAY);
	ld	hl, #0x0384
	push	hl
	call	_glic_delay
	pop	af
	jp	00147$
00149$:
;c_programs/apps/brainfuck.c:1052: }
	ld	sp, ix
	pop	ix
	ret
___str_18:
	.db 0x00
___str_19:
	.ascii "SC"
	.db 0x00
___str_20:
	.ascii "FL"
	.db 0x00
___str_21:
	.ascii "A?"
	.db 0x00
;c_programs/apps/brainfuck.c:1054: static void draw_menu_item(unsigned char index,
;	---------------------------------
; Function draw_menu_item
; ---------------------------------
_draw_menu_item:
;c_programs/apps/brainfuck.c:1059: row = (unsigned char)(4u + (unsigned char)(index << 1));
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	add	a, a
	add	a, #0x04
	ld	b, a
;c_programs/apps/brainfuck.c:1060: text_clear_row(row);
	push	bc
	push	bc
	inc	sp
	call	_text_clear_row
	inc	sp
	pop	bc
;c_programs/apps/brainfuck.c:1061: text_put(row, 0u, (selected != 0u) ? '>' : ' ');
	ld	hl, #3
	add	hl, sp
	ld	a, (hl)
	or	a, a
	jr	Z, 00103$
	ld	de, #0x003e
	jr	00104$
00103$:
	ld	de, #0x0020
00104$:
	ld	a, e
	push	bc
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	push	bc
	inc	sp
	call	_text_put
	pop	af
	inc	sp
	pop	bc
;c_programs/apps/brainfuck.c:1062: text_write(row, 2u, label);
	ld	hl, #4
	add	hl, sp
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	push	de
	ld	a, #0x02
	push	af
	inc	sp
	push	bc
	inc	sp
	call	_text_write
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:1063: }
	ret
;c_programs/apps/brainfuck.c:1065: static void draw_menu(unsigned char selected, const char *status) {
;	---------------------------------
; Function draw_menu
; ---------------------------------
_draw_menu:
;c_programs/apps/brainfuck.c:1066: glic_prepare_screen(GLIC_BLACK);
	xor	a, a
	push	af
	inc	sp
	call	_glic_prepare_screen
	inc	sp
;c_programs/apps/brainfuck.c:1067: text_write(0u, 3u, "BRAINFUCK");
	ld	hl, #___str_22
	push	hl
	ld	a, #0x03
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:1068: text_write(1u, 0u, "COMPILE AND RUN");
	ld	hl, #___str_23
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x01
	push	de
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:1069: text_write(2u, 0u, "SRC ");
	ld	hl, #___str_24
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x02
	push	de
	call	_text_write
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:1070: text_write_u16_3(2u, 4u, source_len());
	call	_source_len
	push	hl
	ld	de, #0x0402
	push	de
	call	_text_write_u16_3
	pop	af
;c_programs/apps/brainfuck.c:1071: text_write(2u, 8u, "IN ");
	ld	hl, #___str_25
	ex	(sp),hl
	ld	de, #0x0802
	push	de
	call	_text_write
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:1072: text_write_u16_3(2u, 11u, input_len());
	call	_input_len
	push	hl
	ld	de, #0x0b02
	push	de
	call	_text_write_u16_3
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:1075: "EDIT PROGRAM");
	ld	bc, #___str_26+0
;c_programs/apps/brainfuck.c:1074: (selected == MENU_EDIT_SOURCE) ? 1u : 0u,
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	or	a, a
	jr	NZ, 00103$
	ld	de, #0x0001
	jr	00104$
00103$:
	ld	de, #0x0000
00104$:
	ld	a, e
;c_programs/apps/brainfuck.c:1073: draw_menu_item(MENU_EDIT_SOURCE,
	push	bc
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_draw_menu_item
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:1078: "EDIT INPUT");
	ld	bc, #___str_27+0
;c_programs/apps/brainfuck.c:1077: (selected == MENU_EDIT_INPUT) ? 1u : 0u,
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	dec	a
	jr	NZ, 00105$
	ld	de, #0x0001
	jr	00106$
00105$:
	ld	de, #0x0000
00106$:
	ld	a, e
;c_programs/apps/brainfuck.c:1076: draw_menu_item(MENU_EDIT_INPUT,
	push	bc
	ld	d,a
	ld	e,#0x01
	push	de
	call	_draw_menu_item
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:1081: "RUN");
	ld	bc, #___str_28+0
;c_programs/apps/brainfuck.c:1080: (selected == MENU_RUN) ? 1u : 0u,
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	sub	a, #0x02
	jr	NZ, 00107$
	ld	de, #0x0001
	jr	00108$
00107$:
	ld	de, #0x0000
00108$:
	ld	a, e
;c_programs/apps/brainfuck.c:1079: draw_menu_item(MENU_RUN,
	push	bc
	ld	d,a
	ld	e,#0x02
	push	de
	call	_draw_menu_item
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:1084: "HELP");
	ld	bc, #___str_29+0
;c_programs/apps/brainfuck.c:1083: (selected == MENU_HELP) ? 1u : 0u,
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	sub	a, #0x03
	jr	NZ, 00109$
	ld	de, #0x0001
	jr	00110$
00109$:
	ld	de, #0x0000
00110$:
	ld	a, e
;c_programs/apps/brainfuck.c:1082: draw_menu_item(MENU_HELP,
	push	bc
	ld	d,a
	ld	e,#0x03
	push	de
	call	_draw_menu_item
	pop	af
;c_programs/apps/brainfuck.c:1085: text_write(12u, 0u, "B LOAD DEMO");
	ld	hl, #___str_30
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x0c
	push	de
	call	_text_write
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:1086: text_write(13u, 0u, status);
	ld	hl, #3
	add	hl, sp
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	push	bc
	xor	a, a
	ld	d,a
	ld	e,#0x0d
	push	de
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:1087: text_write(15u, 0u, "A/CENTER SELECT");
	ld	hl, #___str_31
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x0f
	push	de
	call	_text_write
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:1088: }
	ret
___str_22:
	.ascii "BRAINFUCK"
	.db 0x00
___str_23:
	.ascii "COMPILE AND RUN"
	.db 0x00
___str_24:
	.ascii "SRC "
	.db 0x00
___str_25:
	.ascii "IN "
	.db 0x00
___str_26:
	.ascii "EDIT PROGRAM"
	.db 0x00
___str_27:
	.ascii "EDIT INPUT"
	.db 0x00
___str_28:
	.ascii "RUN"
	.db 0x00
___str_29:
	.ascii "HELP"
	.db 0x00
___str_30:
	.ascii "B LOAD DEMO"
	.db 0x00
___str_31:
	.ascii "A/CENTER SELECT"
	.db 0x00
;c_programs/apps/brainfuck.c:1090: static void show_help(void) {
;	---------------------------------
; Function show_help
; ---------------------------------
_show_help:
;c_programs/apps/brainfuck.c:1091: glic_prepare_screen(GLIC_BLACK);
	xor	a, a
	push	af
	inc	sp
	call	_glic_prepare_screen
	inc	sp
;c_programs/apps/brainfuck.c:1092: text_write(0u, 1u, "BRAINFUCK HELP");
	ld	hl, #___str_32
	push	hl
	ld	a, #0x01
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:1093: text_write(2u, 0u, "+- CELL VALUE");
	ld	hl, #___str_33
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x02
	push	de
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:1094: text_write(3u, 0u, "<> TAPE POINTER");
	ld	hl, #___str_34
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x03
	push	de
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:1095: text_write(4u, 0u, ". OUTPUT CHAR");
	ld	hl, #___str_35
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x04
	push	de
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:1096: text_write(5u, 0u, ", INPUT BYTE");
	ld	hl, #___str_36
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x05
	push	de
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:1097: text_write(6u, 0u, "[] LOOP WHILE");
	ld	hl, #___str_37
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x06
	push	de
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:1098: text_write(7u, 0u, "CELL NONZERO");
	ld	hl, #___str_38
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x07
	push	de
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:1099: text_write(9u, 0u, "INPUT EOF IS 0");
	ld	hl, #___str_39
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x09
	push	de
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:1100: text_write(10u, 0u, "256 BYTE TAPE");
	ld	hl, #___str_40
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x0a
	push	de
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:1101: text_write(11u, 0u, "384 BYTECODE");
	ld	hl, #___str_41
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x0b
	push	de
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:1102: text_write(12u, 0u, "NON BF IGNORED");
	ld	hl, #___str_42
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x0c
	push	de
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:1103: text_write(13u, 0u, "C STOPS RUN");
	ld	hl, #___str_43
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x0d
	push	de
	call	_text_write
	pop	af
;c_programs/apps/brainfuck.c:1104: text_write(15u, 0u, "ANY KEY BACK");
	ld	hl, #___str_44
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x0f
	push	de
	call	_text_write
	pop	af
	pop	af
;c_programs/apps/brainfuck.c:1105: wait_for_any_key();
;c_programs/apps/brainfuck.c:1106: }
	jp	_wait_for_any_key
___str_32:
	.ascii "BRAINFUCK HELP"
	.db 0x00
___str_33:
	.ascii "+- CELL VALUE"
	.db 0x00
___str_34:
	.ascii "<> TAPE POINTER"
	.db 0x00
___str_35:
	.ascii ". OUTPUT CHAR"
	.db 0x00
___str_36:
	.ascii ", INPUT BYTE"
	.db 0x00
___str_37:
	.ascii "[] LOOP WHILE"
	.db 0x00
___str_38:
	.ascii "CELL NONZERO"
	.db 0x00
___str_39:
	.ascii "INPUT EOF IS 0"
	.db 0x00
___str_40:
	.ascii "256 BYTE TAPE"
	.db 0x00
___str_41:
	.ascii "384 BYTECODE"
	.db 0x00
___str_42:
	.ascii "NON BF IGNORED"
	.db 0x00
___str_43:
	.ascii "C STOPS RUN"
	.db 0x00
___str_44:
	.ascii "ANY KEY BACK"
	.db 0x00
;c_programs/apps/brainfuck.c:1108: void main(void) {
;	---------------------------------
; Function main
; ---------------------------------
_main::
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;c_programs/apps/brainfuck.c:1114: storage_init();
	call	_storage_init
;c_programs/apps/brainfuck.c:1115: selected = MENU_EDIT_SOURCE;
	ld	-1 (ix), #0
;c_programs/apps/brainfuck.c:1116: status = "";
;c_programs/apps/brainfuck.c:1117: draw_menu(selected, status);
	ld	hl, #___str_45
	push	hl
	xor	a, a
	push	af
	inc	sp
	call	_draw_menu
	pop	af
	inc	sp
;c_programs/apps/brainfuck.c:1118: last_buttons = GLIC_BUTTONS_NONE;
	ld	e, #0xff
;c_programs/apps/brainfuck.c:1120: while (1) {
00131$:
;c_programs/apps/brainfuck.c:1121: buttons = glic_read_buttons();
	push	de
	call	_glic_read_buttons
	pop	de
	ld	-2 (ix), l
;c_programs/apps/brainfuck.c:1122: if (button_edge(buttons, last_buttons, GLIC_BTN_UP) != 0u) {
	push	de
	ld	d, #0x02
	push	de
	ld	a, -2 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	c, l
	inc	sp
	pop	de
	ld	a, c
	or	a, a
	jr	Z, 00128$
;c_programs/apps/brainfuck.c:1123: if (selected == 0u) {
	ld	a, -1 (ix)
	or	a, a
	jr	NZ, 00102$
;c_programs/apps/brainfuck.c:1124: selected = (unsigned char)(MENU_COUNT - 1u);
	ld	-1 (ix), #0x03
	jr	00103$
00102$:
;c_programs/apps/brainfuck.c:1126: --selected;
	dec	-1 (ix)
00103$:
;c_programs/apps/brainfuck.c:1128: status = "";
;c_programs/apps/brainfuck.c:1129: draw_menu(selected, status);
	ld	hl, #___str_45
	push	hl
	ld	a, -1 (ix)
	push	af
	inc	sp
	call	_draw_menu
	pop	af
	inc	sp
	jp	00129$
00128$:
;c_programs/apps/brainfuck.c:1130: } else if (button_edge(buttons, last_buttons, GLIC_BTN_DOWN) != 0u) {
	push	de
	ld	d, #0x08
	push	de
	ld	a, -2 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	de
	or	a, a
	jr	Z, 00125$
;c_programs/apps/brainfuck.c:1131: ++selected;
	inc	-1 (ix)
;c_programs/apps/brainfuck.c:1132: if (selected >= MENU_COUNT) {
	ld	a, -1 (ix)
	sub	a, #0x04
	jr	C, 00105$
;c_programs/apps/brainfuck.c:1133: selected = 0u;
	ld	-1 (ix), #0
00105$:
;c_programs/apps/brainfuck.c:1135: status = "";
;c_programs/apps/brainfuck.c:1136: draw_menu(selected, status);
	ld	hl, #___str_45
	push	hl
	ld	a, -1 (ix)
	push	af
	inc	sp
	call	_draw_menu
	pop	af
	inc	sp
	jp	00129$
00125$:
;c_programs/apps/brainfuck.c:1137: } else if ((button_edge(buttons, last_buttons, GLIC_BTN_A) != 0u) ||
	push	de
	ld	d, #0x80
	push	de
	ld	a, -2 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	de
	or	a, a
	jr	NZ, 00120$
;c_programs/apps/brainfuck.c:1138: (button_edge(buttons,
	push	de
	ld	d, #0x01
	push	de
	ld	a, -2 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	de
	or	a, a
	jr	Z, 00121$
00120$:
;c_programs/apps/brainfuck.c:1141: wait_for_release();
	call	_wait_for_release
;c_programs/apps/brainfuck.c:1142: if (selected == MENU_EDIT_SOURCE) {
	ld	a, -1 (ix)
	or	a, a
	jr	NZ, 00113$
;c_programs/apps/brainfuck.c:1143: run_editor(EDIT_SOURCE);
	xor	a, a
	push	af
	inc	sp
	call	_run_editor
	inc	sp
	jr	00114$
00113$:
;c_programs/apps/brainfuck.c:1144: } else if (selected == MENU_EDIT_INPUT) {
	ld	a, -1 (ix)
	dec	a
	jr	NZ, 00110$
;c_programs/apps/brainfuck.c:1145: run_editor(EDIT_INPUT);
	ld	a, #0x01
	push	af
	inc	sp
	call	_run_editor
	inc	sp
	jr	00114$
00110$:
;c_programs/apps/brainfuck.c:1146: } else if (selected == MENU_RUN) {
	ld	a, -1 (ix)
	sub	a, #0x02
	jr	NZ, 00107$
;c_programs/apps/brainfuck.c:1147: run_program();
	call	_run_program
	jr	00114$
00107$:
;c_programs/apps/brainfuck.c:1149: show_help();
	call	_show_help
00114$:
;c_programs/apps/brainfuck.c:1151: status = "";
;c_programs/apps/brainfuck.c:1152: draw_menu(selected, status);
	ld	hl, #___str_45
	push	hl
	ld	a, -1 (ix)
	push	af
	inc	sp
	call	_draw_menu
	pop	af
	inc	sp
;c_programs/apps/brainfuck.c:1153: last_buttons = GLIC_BUTTONS_NONE;
	jr	00129$
00121$:
;c_programs/apps/brainfuck.c:1154: } else if (button_edge(buttons, last_buttons, GLIC_BTN_B) != 0u) {
	push	de
	ld	d, #0x40
	push	de
	ld	a, -2 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	de
	or	a, a
	jr	Z, 00118$
;c_programs/apps/brainfuck.c:1155: load_demo_program();
	call	_load_demo_program
;c_programs/apps/brainfuck.c:1156: status = "DEMO LOADED";
;c_programs/apps/brainfuck.c:1157: draw_menu(selected, status);
	ld	hl, #___str_46
	push	hl
	ld	a, -1 (ix)
	push	af
	inc	sp
	call	_draw_menu
	pop	af
	inc	sp
	jr	00129$
00118$:
;c_programs/apps/brainfuck.c:1158: } else if (button_edge(buttons, last_buttons, GLIC_BTN_C) != 0u) {
	ld	d, #0x20
	push	de
	ld	a, -2 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00129$
;c_programs/apps/brainfuck.c:1159: show_help();
	call	_show_help
;c_programs/apps/brainfuck.c:1160: status = "";
	ld	bc, #___str_45
;c_programs/apps/brainfuck.c:1161: draw_menu(selected, status);
	push	bc
	ld	a, -1 (ix)
	push	af
	inc	sp
	call	_draw_menu
	pop	af
	inc	sp
;c_programs/apps/brainfuck.c:1162: last_buttons = GLIC_BUTTONS_NONE;
00129$:
;c_programs/apps/brainfuck.c:1165: last_buttons = buttons;
	ld	e, -2 (ix)
;c_programs/apps/brainfuck.c:1166: glic_delay(LOOP_DELAY);
	push	de
	ld	hl, #0x0384
	push	hl
	call	_glic_delay
	pop	af
	pop	de
;c_programs/apps/brainfuck.c:1168: }
	jp	00131$
___str_45:
	.db 0x00
___str_46:
	.ascii "DEMO LOADED"
	.db 0x00
	.area _CODE
	.area _INITIALIZER
	.area _CABS (ABS)
