; generated by sdcc-z80-bin
    JR __sdcc_z80_bin_start
    DB "oneaddr_basic",0
__sdcc_z80_bin_start:
    LD SP,#0x7700
    CALL _main
__sdcc_z80_bin_halt:
    JR __sdcc_z80_bin_halt

;--------------------------------------------------------
; File Created by SDCC : free open source ANSI-C Compiler
; Version 4.1.0 #12072 (Linux)
;--------------------------------------------------------
	.module oneaddr_basic
	.optsdcc -mz80
	
;--------------------------------------------------------
; Public variables in this module
;--------------------------------------------------------
	.globl _main
;--------------------------------------------------------
; special function registers
;--------------------------------------------------------
;--------------------------------------------------------
; ram data
;--------------------------------------------------------
	.area _DATA
_variables:
	.ds 52
_loop_stack:
	.ds 28
_loop_depth:
	.ds 1
_run_pc:
	.ds 2
_run_len:
	.ds 2
_run_error:
	.ds 1
_run_stop:
	.ds 1
_run_break:
	.ds 1
_run_done:
	.ds 1
_output_row:
	.ds 1
_output_col:
	.ds 1
_editor_view_start:
	.ds 2
;--------------------------------------------------------
; ram data
;--------------------------------------------------------
	.area _INITIALIZED
;--------------------------------------------------------
; absolute external ram data
;--------------------------------------------------------
	.area _DABS (ABS)
;--------------------------------------------------------
; global & static initialisations
;--------------------------------------------------------
	.area _HOME
	.area _GSINIT
	.area _GSFINAL
	.area _GSINIT
;--------------------------------------------------------
; Home
;--------------------------------------------------------
	.area _HOME
	.area _HOME
;--------------------------------------------------------
; code
;--------------------------------------------------------
	.area _CODE
;apps/../common/glic80.h:29: static unsigned char glic_read_buttons(void) __naked {
;	---------------------------------
; Function glic_read_buttons
; ---------------------------------
_glic_read_buttons:
;apps/../common/glic80.h:34: __endasm;
	in	a,(0)
	ld	l,a
	ret
;apps/../common/glic80.h:35: }
;apps/../common/glic80.h:37: static unsigned char glic_button_pressed(unsigned char buttons,
;	---------------------------------
; Function glic_button_pressed
; ---------------------------------
_glic_button_pressed:
;apps/../common/glic80.h:39: return (buttons & mask) == 0u;
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	inc	iy
	and	a, 0 (iy)
	or	a, a
	ld	a, #0x01
	jr	Z, 00104$
	xor	a, a
00104$:
	ld	l, a
;apps/../common/glic80.h:40: }
	ret
_glic_bit_masks:
	.db #0x01	; 1
	.db #0x02	; 2
	.db #0x04	; 4
	.db #0x08	; 8
	.db #0x10	; 16
	.db #0x20	; 32
	.db #0x40	; 64
	.db #0x80	; 128
;apps/../common/glic80.h:42: static void glic_delay(unsigned int count) {
;	---------------------------------
; Function glic_delay
; ---------------------------------
_glic_delay:
;apps/../common/glic80.h:43: while (count != 0u) {
	pop	de
	pop	bc
	push	bc
	push	de
00101$:
	ld	a, b
	or	a, c
	ret	Z
;apps/../common/glic80.h:44: --count;
	dec	bc
;apps/../common/glic80.h:47: __endasm;
	nop
;apps/../common/glic80.h:49: }
	jr	00101$
;apps/../common/glic80.h:51: static void glic_clear_text(void) {
;	---------------------------------
; Function glic_clear_text
; ---------------------------------
_glic_clear_text:
;apps/../common/glic80.h:54: for (i = 0; i < GLIC_CVRAM_BYTES; ++i) {
	ld	bc, #0x0000
00102$:
;apps/../common/glic80.h:55: GLIC_CVRAM[i] = 0x00u;
	ld	hl, #0x7700
	add	hl, bc
	ld	(hl), #0x00
;apps/../common/glic80.h:54: for (i = 0; i < GLIC_CVRAM_BYTES; ++i) {
	inc	bc
	ld	a, b
	sub	a, #0x01
	jr	C, 00102$
;apps/../common/glic80.h:57: }
	ret
;apps/../common/glic80.h:59: static void glic_fill_graphics(unsigned char value) {
;	---------------------------------
; Function glic_fill_graphics
; ---------------------------------
_glic_fill_graphics:
;apps/../common/glic80.h:62: for (i = 0; i < GLIC_GVRAM_BYTES; ++i) {
	ld	bc, #0x0000
00102$:
;apps/../common/glic80.h:63: GLIC_GVRAM[i] = value;
	ld	hl, #0x7800
	add	hl, bc
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	ld	(hl), a
;apps/../common/glic80.h:62: for (i = 0; i < GLIC_GVRAM_BYTES; ++i) {
	inc	bc
	ld	a, b
	sub	a, #0x08
	jr	C, 00102$
;apps/../common/glic80.h:65: }
	ret
;apps/../common/glic80.h:67: static void glic_plot(unsigned char x, unsigned char y, unsigned char value) {
;	---------------------------------
; Function glic_plot
; ---------------------------------
_glic_plot:
	push	ix
	ld	ix,#0
	add	ix,sp
;apps/../common/glic80.h:71: if ((x >= GLIC_SCREEN_WIDTH) || (y >= 128u)) {
	ld	a, 4 (ix)
	sub	a, #0x80
	jr	NC, 00107$
	ld	a, 5 (ix)
	sub	a, #0x80
;apps/../common/glic80.h:72: return;
	jr	NC, 00107$
;apps/../common/glic80.h:75: offset = (((unsigned int)(y >> 3)) << 7) + x;
	ld	a, 5 (ix)
	rrca
	rrca
	rrca
	and	a, #0x1f
	ld	l, a
	ld	h, #0x00
	add	hl, hl
	add	hl, hl
	add	hl, hl
	add	hl, hl
	add	hl, hl
	add	hl, hl
	add	hl, hl
	ld	c, 4 (ix)
	ld	b, #0x00
	add	hl, bc
	ex	de, hl
;apps/../common/glic80.h:76: mask = glic_bit_masks[y & 7u];
	ld	bc, #_glic_bit_masks+0
	ld	a, 5 (ix)
	and	a, #0x07
	ld	l, a
	ld	h, #0x00
	add	hl, bc
	ld	b, (hl)
;apps/../common/glic80.h:78: GLIC_GVRAM[offset] |= mask;
	ld	hl, #0x7800
	add	hl, de
	ld	c, (hl)
;apps/../common/glic80.h:77: if (value != 0u) {
	ld	a, 6 (ix)
	or	a, a
	jr	Z, 00105$
;apps/../common/glic80.h:78: GLIC_GVRAM[offset] |= mask;
	ld	a, c
	or	a, b
	ld	(hl), a
	jr	00107$
00105$:
;apps/../common/glic80.h:80: GLIC_GVRAM[offset] &= (unsigned char)~mask;
	ld	a, b
	cpl
	and	a, c
	ld	(hl), a
00107$:
;apps/../common/glic80.h:82: }
	pop	ix
	ret
;apps/../common/glic80.h:84: static void glic_draw_bitmap8(unsigned char x,
;	---------------------------------
; Function glic_draw_bitmap8
; ---------------------------------
_glic_draw_bitmap8:
	push	ix
	ld	ix,#0
	add	ix,sp
;apps/../common/glic80.h:92: for (row = 0u; row < 8u; ++row) {
	ld	c, #0x00
00107$:
;apps/../common/glic80.h:93: bits = rows[row];
	ld	l, 6 (ix)
	ld	h, 7 (ix)
	ld	b, #0x00
	add	hl, bc
	ld	l, (hl)
;apps/../common/glic80.h:94: mask = 0x80u;
	ld	e, #0x80
;apps/../common/glic80.h:95: for (col = 0u; col < 8u; ++col) {
	ld	a, 5 (ix)
	add	a, c
	ld	b, a
	ld	d, #0x00
00105$:
;apps/../common/glic80.h:96: if ((bits & mask) != 0u) {
	ld	a, l
	and	a,e
	jr	Z, 00102$
;apps/../common/glic80.h:97: glic_plot((unsigned char)(x + col), (unsigned char)(y + row), 1u);
	ld	a, 4 (ix)
	add	a, d
	push	hl
	push	bc
	push	de
	ld	h, #0x01
	ld	l, b
	push	hl
	push	af
	inc	sp
	call	_glic_plot
	pop	af
	inc	sp
	pop	de
	pop	bc
	pop	hl
00102$:
;apps/../common/glic80.h:99: mask >>= 1;
	srl	e
;apps/../common/glic80.h:95: for (col = 0u; col < 8u; ++col) {
	inc	d
	ld	a, d
	sub	a, #0x08
	jr	C, 00105$
;apps/../common/glic80.h:92: for (row = 0u; row < 8u; ++row) {
	inc	c
	ld	a, c
	sub	a, #0x08
	jr	C, 00107$
;apps/../common/glic80.h:102: }
	pop	ix
	ret
;apps/../common/glic80.h:104: static void glic_draw_tile8(unsigned char cell_x,
;	---------------------------------
; Function glic_draw_tile8
; ---------------------------------
_glic_draw_tile8:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;apps/../common/glic80.h:110: offset = (((unsigned int)cell_y) << 7) + (((unsigned int)cell_x) << 3);
	ld	c, 5 (ix)
	xor	a, a
	rr	a
	ld	b, c
	rr	b
	rra
	ld	c, a
	ld	l, 4 (ix)
	ld	h, #0x00
	add	hl, hl
	add	hl, hl
	add	hl, hl
	add	hl, bc
	ex	de, hl
;apps/../common/glic80.h:111: for (col = 0u; col < 8u; ++col) {
	ld	c, #0x00
00102$:
;apps/../common/glic80.h:112: GLIC_GVRAM[offset + col] = columns[col];
	ld	l, c
	ld	h, #0x00
	add	hl, de
	ld	-2 (ix), l
	ld	a, h
	add	a, #0x78
	ld	-1 (ix), a
	ld	l, 6 (ix)
	ld	h, 7 (ix)
	ld	b, #0x00
	add	hl, bc
	ld	a, (hl)
	pop	hl
	push	hl
	ld	(hl), a
;apps/../common/glic80.h:111: for (col = 0u; col < 8u; ++col) {
	inc	c
	ld	a, c
	sub	a, #0x08
	jr	C, 00102$
;apps/../common/glic80.h:114: }
	ld	sp, ix
	pop	ix
	ret
;apps/../common/glic80.h:116: static void glic_prepare_screen(unsigned char value) {
;	---------------------------------
; Function glic_prepare_screen
; ---------------------------------
_glic_prepare_screen:
;apps/../common/glic80.h:117: glic_clear_text();
	call	_glic_clear_text
;apps/../common/glic80.h:118: glic_fill_graphics(value);
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_glic_fill_graphics
	inc	sp
;apps/../common/glic80.h:119: }
	ret
;apps/../common/glic80.h:121: static void glic_fill_page_run(unsigned char page,
;	---------------------------------
; Function glic_fill_page_run
; ---------------------------------
_glic_fill_page_run:
;apps/../common/glic80.h:127: p = GLIC_GVRAM + (((unsigned int)page) << 7) + x;
	ld	iy, #2
	add	iy, sp
	ld	c, 0 (iy)
	ld	b, #0x00
	xor	a, a
	rr	b
	ld	b, c
	rr	b
	rra
	ld	c, a
	ld	a, b
	add	a, #0x78
	ld	b, a
	ld	a, c
	ld	hl, #3
	add	hl, sp
	add	a, (hl)
	ld	c, a
	jr	NC, 00117$
	inc	b
00117$:
;apps/../common/glic80.h:128: while (width != 0u) {
	ld	e, 2 (iy)
00101$:
	ld	a, e
	or	a, a
	ret	Z
;apps/../common/glic80.h:129: *p++ = value;
	ld	iy, #5
	add	iy, sp
	ld	a, 0 (iy)
	ld	(bc), a
	inc	bc
;apps/../common/glic80.h:130: --width;
	dec	e
;apps/../common/glic80.h:132: }
	jr	00101$
;apps/../common/glic80.h:134: static void glic_fill_pages(unsigned char first_page,
;	---------------------------------
; Function glic_fill_pages
; ---------------------------------
_glic_fill_pages:
;apps/../common/glic80.h:137: while (page_count != 0u) {
	ld	iy, #2
	add	iy, sp
	ld	b, 0 (iy)
	ld	c, 1 (iy)
00101$:
	ld	a, c
	or	a, a
	ret	Z
;apps/../common/glic80.h:138: glic_fill_page_run(first_page, 0u, GLIC_SCREEN_WIDTH, value);
	push	bc
	ld	hl, #6
	add	hl, sp
	ld	d, (hl)
	ld	e,#0x80
	push	de
	xor	a, a
	push	af
	inc	sp
	push	bc
	inc	sp
	call	_glic_fill_page_run
	pop	af
	pop	af
	pop	bc
;apps/../common/glic80.h:139: ++first_page;
	inc	b
;apps/../common/glic80.h:140: --page_count;
	dec	c
;apps/../common/glic80.h:142: }
	jr	00101$
;apps/../common/glic80.h:144: static void glic_fill_columns(unsigned char x,
;	---------------------------------
; Function glic_fill_columns
; ---------------------------------
_glic_fill_columns:
;apps/../common/glic80.h:149: for (page = 0u; page < GLIC_SCREEN_PAGES; ++page) {
	ld	b, #0x00
00102$:
;apps/../common/glic80.h:150: glic_fill_page_run(page, x, width, value);
	push	bc
	ld	iy, #6
	add	iy, sp
	ld	a, 0 (iy)
	push	af
	inc	sp
	ld	a, -1 (iy)
	dec	iy
	push	af
	inc	sp
	ld	a, -1 (iy)
	push	af
	inc	sp
	push	bc
	inc	sp
	call	_glic_fill_page_run
	pop	af
	pop	af
	pop	bc
;apps/../common/glic80.h:149: for (page = 0u; page < GLIC_SCREEN_PAGES; ++page) {
	inc	b
	ld	a, b
	sub	a, #0x10
	jr	C, 00102$
;apps/../common/glic80.h:152: }
	ret
;apps/oneaddr_basic.c:68: static volatile unsigned char *text_cell(unsigned char row,
;	---------------------------------
; Function text_cell
; ---------------------------------
_text_cell:
;apps/oneaddr_basic.c:73: while (row != 0u) {
	ld	bc, #0x7700
	ld	iy, #2
	add	iy, sp
	ld	l, 0 (iy)
00101$:
	ld	a, l
	or	a, a
	jr	Z, 00103$
;apps/oneaddr_basic.c:74: p += 16u;
	ld	a, c
	add	a, #0x10
	ld	c, a
	jr	NC, 00117$
	inc	b
00117$:
;apps/oneaddr_basic.c:75: --row;
	dec	l
	jr	00101$
00103$:
;apps/oneaddr_basic.c:77: p += col;
	ld	a, c
	ld	hl, #3
	add	hl, sp
	add	a, (hl)
	ld	l, a
	jr	NC, 00118$
	inc	b
00118$:
	ld	h, b
;apps/oneaddr_basic.c:78: return p;
;apps/oneaddr_basic.c:79: }
	ret
_keyboard_keys:
	.db #0x41	; 65	'A'
	.db #0x42	; 66	'B'
	.db #0x43	; 67	'C'
	.db #0x44	; 68	'D'
	.db #0x45	; 69	'E'
	.db #0x46	; 70	'F'
	.db #0x47	; 71	'G'
	.db #0x48	; 72	'H'
	.db #0x49	; 73	'I'
	.db #0x4a	; 74	'J'
	.db #0x4b	; 75	'K'
	.db #0x4c	; 76	'L'
	.db #0x4d	; 77	'M'
	.db #0x4e	; 78	'N'
	.db #0x4f	; 79	'O'
	.db #0x50	; 80	'P'
	.db #0x51	; 81	'Q'
	.db #0x52	; 82	'R'
	.db #0x53	; 83	'S'
	.db #0x54	; 84	'T'
	.db #0x55	; 85	'U'
	.db #0x56	; 86	'V'
	.db #0x57	; 87	'W'
	.db #0x58	; 88	'X'
	.db #0x59	; 89	'Y'
	.db #0x5a	; 90	'Z'
	.db #0x30	; 48	'0'
	.db #0x31	; 49	'1'
	.db #0x32	; 50	'2'
	.db #0x33	; 51	'3'
	.db #0x34	; 52	'4'
	.db #0x35	; 53	'5'
	.db #0x36	; 54	'6'
	.db #0x37	; 55	'7'
	.db #0x38	; 56	'8'
	.db #0x39	; 57	'9'
	.db #0x5f	; 95
	.db #0x3d	; 61
	.db #0x2b	; 43
	.db #0x2d	; 45
	.db #0x2a	; 42
	.db #0x2f	; 47
	.db #0x3c	; 60
	.db #0x3e	; 62
	.db #0x28	; 40
	.db #0x29	; 41
	.db #0x22	; 34
	.db #0x0a	; 10
_demo_source:
	.ascii "PRINT "
	.db 0x22
	.ascii "ONEADDR"
	.db 0x22
	.db 0x0a
	.ascii "LET A=0"
	.db 0x0a
	.ascii "FOR I=1 TO 5"
	.db 0x0a
	.ascii "LET A=A+I"
	.db 0x0a
	.ascii "PRINT A"
	.db 0x0a
	.ascii "NEXT"
	.db 0x0a
	.ascii "IF A=15 THEN PRINT "
	.db 0x22
	.ascii "OK"
	.db 0x22
	.db 0x0a
	.ascii "END"
	.db 0x0a
	.db 0x00
;apps/oneaddr_basic.c:81: static void text_put(unsigned char row, unsigned char col, unsigned char ch) {
;	---------------------------------
; Function text_put
; ---------------------------------
_text_put:
;apps/oneaddr_basic.c:82: if ((row >= 16u) || (col >= 16u)) {
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	sub	a, #0x10
	ret	NC
	ld	a, 1 (iy)
	sub	a, #0x10
;apps/oneaddr_basic.c:83: return;
	ret	NC
;apps/oneaddr_basic.c:85: *text_cell(row, col) = ch;
	ld	iy, #3
	add	iy, sp
	ld	h, 0 (iy)
	ld	l, -1 (iy)
	push	hl
	call	_text_cell
	pop	af
	ld	iy, #4
	add	iy, sp
	ld	a, 0 (iy)
	ld	(hl), a
;apps/oneaddr_basic.c:86: }
	ret
;apps/oneaddr_basic.c:88: static void text_clear_row(unsigned char row) {
;	---------------------------------
; Function text_clear_row
; ---------------------------------
_text_clear_row:
;apps/oneaddr_basic.c:91: for (col = 0u; col < 16u; ++col) {
	ld	b, #0x00
00102$:
;apps/oneaddr_basic.c:92: text_put(row, col, 0u);
	push	bc
	xor	a, a
	push	af
	inc	sp
	push	bc
	inc	sp
	ld	hl, #6
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
	pop	bc
;apps/oneaddr_basic.c:91: for (col = 0u; col < 16u; ++col) {
	inc	b
	ld	a, b
	sub	a, #0x10
	jr	C, 00102$
;apps/oneaddr_basic.c:94: }
	ret
;apps/oneaddr_basic.c:96: static void text_write(unsigned char row, unsigned char col, const char *text) {
;	---------------------------------
; Function text_write
; ---------------------------------
_text_write:
;apps/oneaddr_basic.c:97: while ((*text != 0) && (col < 16u)) {
	ld	iy, #4
	add	iy, sp
	ld	l, 0 (iy)
	ld	h, 1 (iy)
	ld	b, -1 (iy)
00102$:
	ld	d, (hl)
	ld	a, d
	or	a, a
	ret	Z
	ld	a, b
	sub	a, #0x10
	ret	NC
;apps/oneaddr_basic.c:98: text_put(row, col, (unsigned char)*text);
	push	hl
	push	bc
	ld	e, b
	push	de
	ld	hl, #8
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
	pop	bc
	pop	hl
;apps/oneaddr_basic.c:99: ++text;
	inc	hl
;apps/oneaddr_basic.c:100: ++col;
	inc	b
;apps/oneaddr_basic.c:102: }
	jr	00102$
;apps/oneaddr_basic.c:104: static void text_write_u16_3(unsigned char row,
;	---------------------------------
; Function text_write_u16_3
; ---------------------------------
_text_write_u16_3:
	push	ix
	ld	ix,#0
	add	ix,sp
;apps/oneaddr_basic.c:110: if (value > 999u) {
	ld	a, #0xe7
	cp	a, 6 (ix)
	ld	a, #0x03
	sbc	a, 7 (ix)
	jr	NC, 00102$
;apps/oneaddr_basic.c:111: value = 999u;
	ld	6 (ix), #0xe7
	ld	7 (ix), #0x03
00102$:
;apps/oneaddr_basic.c:115: while (value >= 100u) {
	ld	e, 6 (ix)
	ld	d, 7 (ix)
	ld	c, #0x00
00103$:
	ld	a, e
	sub	a, #0x64
	ld	a, d
	sbc	a, #0x00
	jr	C, 00116$
;apps/oneaddr_basic.c:116: value = (unsigned int)(value - 100u);
	ld	a, e
	add	a, #0x9c
	ld	e, a
	ld	a, d
	adc	a, #0xff
	ld	d, a
;apps/oneaddr_basic.c:117: ++hundreds;
	inc	c
	jr	00103$
00116$:
;apps/oneaddr_basic.c:120: while (value >= 10u) {
	ld	b, #0x00
00106$:
	ld	a, e
	sub	a, #0x0a
	ld	a, d
	sbc	a, #0x00
	jr	C, 00108$
;apps/oneaddr_basic.c:121: value = (unsigned int)(value - 10u);
	ld	a, e
	add	a, #0xf6
	ld	e, a
	ld	a, d
	adc	a, #0xff
	ld	d, a
;apps/oneaddr_basic.c:122: ++tens;
	inc	b
	jr	00106$
00108$:
;apps/oneaddr_basic.c:125: text_put(row, col, (unsigned char)('0' + hundreds));
	ld	a, c
	add	a, #0x30
	push	bc
	push	de
	push	af
	inc	sp
	ld	h, 5 (ix)
	ld	l, 4 (ix)
	push	hl
	call	_text_put
	pop	af
	inc	sp
	pop	de
	pop	bc
;apps/oneaddr_basic.c:126: text_put(row, (unsigned char)(col + 1u), (unsigned char)('0' + tens));
	ld	a, b
	add	a, #0x30
	ld	b, 5 (ix)
	inc	b
	push	de
	push	af
	inc	sp
	push	bc
	inc	sp
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
	pop	de
;apps/oneaddr_basic.c:127: text_put(row, (unsigned char)(col + 2u), (unsigned char)('0' + value));
	ld	a, e
	add	a, #0x30
	ld	b, 5 (ix)
	inc	b
	inc	b
	push	af
	inc	sp
	push	bc
	inc	sp
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
;apps/oneaddr_basic.c:128: }
	pop	ix
	ret
;apps/oneaddr_basic.c:130: static unsigned char button_edge(unsigned char buttons,
;	---------------------------------
; Function button_edge
; ---------------------------------
_button_edge:
;apps/oneaddr_basic.c:133: return (((last_buttons & mask) != 0u) && ((buttons & mask) == 0u));
	ld	iy, #3
	add	iy, sp
	ld	a, 0 (iy)
	inc	iy
	and	a, 0 (iy)
	or	a, a
	jr	Z, 00103$
	ld	a, -2 (iy)
	and	a, 0 (iy)
	or	a, a
	jr	Z, 00104$
00103$:
	ld	l, #0x00
	ret
00104$:
	ld	l, #0x01
;apps/oneaddr_basic.c:134: }
	ret
;apps/oneaddr_basic.c:136: static void wait_for_release(void) {
;	---------------------------------
; Function wait_for_release
; ---------------------------------
_wait_for_release:
;apps/oneaddr_basic.c:137: while (glic_read_buttons() != GLIC_BUTTONS_NONE) {
00101$:
	call	_glic_read_buttons
	inc	l
	ret	Z
;apps/oneaddr_basic.c:138: glic_delay(500u);
	ld	hl, #0x01f4
	push	hl
	call	_glic_delay
	pop	af
;apps/oneaddr_basic.c:140: }
	jr	00101$
;apps/oneaddr_basic.c:142: static unsigned int program_len(void) {
;	---------------------------------
; Function program_len
; ---------------------------------
_program_len:
;apps/oneaddr_basic.c:145: len = STORE_BASE[STORE_LEN_LO];
	ld	hl, #0x6004
	ld	c, (hl)
	ld	b, #0x00
;apps/oneaddr_basic.c:146: len |= ((unsigned int)STORE_BASE[STORE_LEN_HI]) << 8;
	ld	hl, #0x6005
	ld	l, (hl)
	ld	a,#0x00
	ld	h,a
	or	a, c
	ld	e, a
	ld	a, l
	or	a, b
	ld	d, a
;apps/oneaddr_basic.c:147: if (len > PROGRAM_CAPACITY) {
	xor	a, a
	cp	a, e
	ld	a, #0x02
	sbc	a, d
	jr	NC, 00102$
;apps/oneaddr_basic.c:148: return 0u;
	ld	hl, #0x0000
	ret
00102$:
;apps/oneaddr_basic.c:150: return len;
	ex	de, hl
;apps/oneaddr_basic.c:151: }
	ret
;apps/oneaddr_basic.c:153: static void set_program_len(unsigned int len) {
;	---------------------------------
; Function set_program_len
; ---------------------------------
_set_program_len:
;apps/oneaddr_basic.c:154: if (len > PROGRAM_CAPACITY) {
	xor	a, a
	ld	iy, #2
	add	iy, sp
	cp	a, 0 (iy)
	ld	a, #0x02
	sbc	a, 1 (iy)
	jr	NC, 00102$
;apps/oneaddr_basic.c:155: len = PROGRAM_CAPACITY;
	ld	0 (iy), #0x00
	ld	1 (iy), #0x02
00102$:
;apps/oneaddr_basic.c:157: STORE_BASE[STORE_LEN_LO] = (unsigned char)(len & 0xffu);
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	ld	hl, #0x6004
	ld	(hl), a
;apps/oneaddr_basic.c:158: STORE_BASE[STORE_LEN_HI] = (unsigned char)(len >> 8);
	ld	c, 1 (iy)
	ld	l, #0x05
	ld	(hl), c
;apps/oneaddr_basic.c:159: }
	ret
;apps/oneaddr_basic.c:161: static void mark_storage_valid(void) {
;	---------------------------------
; Function mark_storage_valid
; ---------------------------------
_mark_storage_valid:
;apps/oneaddr_basic.c:162: STORE_BASE[0] = STORE_MAGIC0;
	ld	hl, #0x6000
	ld	(hl), #0x4f
;apps/oneaddr_basic.c:163: STORE_BASE[1] = STORE_MAGIC1;
	ld	l, #0x01
	ld	(hl), #0x41
;apps/oneaddr_basic.c:164: STORE_BASE[2] = STORE_MAGIC2;
	ld	l, #0x02
	ld	(hl), #0x42
;apps/oneaddr_basic.c:165: STORE_BASE[3] = STORE_MAGIC3;
	ld	l, #0x03
	ld	(hl), #0x31
;apps/oneaddr_basic.c:166: }
	ret
;apps/oneaddr_basic.c:168: static void load_demo_program(void) {
;	---------------------------------
; Function load_demo_program
; ---------------------------------
_load_demo_program:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;apps/oneaddr_basic.c:172: mark_storage_valid();
	call	_mark_storage_valid
;apps/oneaddr_basic.c:173: src = demo_source;
	ld	bc, #_demo_source+0
;apps/oneaddr_basic.c:175: while ((*src != 0) && (len < PROGRAM_CAPACITY)) {
	ld	de, #0x0000
00102$:
	ld	a, (bc)
	ld	-2 (ix), a
	ld	a, d
	sub	a, #0x02
	ld	a, #0x00
	rla
	ld	-1 (ix), a
;apps/oneaddr_basic.c:176: PROGRAM_TEXT[len] = (unsigned char)*src;
	ld	hl, #0x6006
	add	hl, de
;apps/oneaddr_basic.c:175: while ((*src != 0) && (len < PROGRAM_CAPACITY)) {
	ld	a, -2 (ix)
	or	a, a
	jr	Z, 00104$
	ld	a, -1 (ix)
	or	a, a
	jr	Z, 00104$
;apps/oneaddr_basic.c:176: PROGRAM_TEXT[len] = (unsigned char)*src;
	ld	a, -2 (ix)
	ld	(hl), a
;apps/oneaddr_basic.c:177: ++src;
	inc	bc
;apps/oneaddr_basic.c:178: ++len;
	inc	de
	jr	00102$
00104$:
;apps/oneaddr_basic.c:180: set_program_len(len);
	push	hl
	push	de
	call	_set_program_len
	pop	af
	pop	hl
;apps/oneaddr_basic.c:181: if (len < PROGRAM_CAPACITY) {
	ld	a, -1 (ix)
	or	a, a
	jr	Z, 00107$
;apps/oneaddr_basic.c:182: PROGRAM_TEXT[len] = 0u;
	ld	(hl), #0x00
00107$:
;apps/oneaddr_basic.c:184: }
	ld	sp, ix
	pop	ix
	ret
;apps/oneaddr_basic.c:186: static void storage_init(void) {
;	---------------------------------
; Function storage_init
; ---------------------------------
_storage_init:
;apps/oneaddr_basic.c:189: if ((STORE_BASE[0] != STORE_MAGIC0) ||
	ld	hl, #0x6000
	ld	a, (hl)
	sub	a, #0x4f
	jp	NZ,_load_demo_program
;apps/oneaddr_basic.c:190: (STORE_BASE[1] != STORE_MAGIC1) ||
	ld	hl, #0x6001
	ld	a, (hl)
	sub	a, #0x41
	jp	NZ,_load_demo_program
;apps/oneaddr_basic.c:191: (STORE_BASE[2] != STORE_MAGIC2) ||
	ld	hl, #0x6002
	ld	a, (hl)
	sub	a, #0x42
	jp	NZ,_load_demo_program
;apps/oneaddr_basic.c:192: (STORE_BASE[3] != STORE_MAGIC3)) {
	ld	hl, #0x6003
	ld	a, (hl)
	sub	a, #0x31
;apps/oneaddr_basic.c:193: load_demo_program();
;apps/oneaddr_basic.c:194: return;
	jp	NZ,_load_demo_program
;apps/oneaddr_basic.c:197: len = program_len();
	call	_program_len
;apps/oneaddr_basic.c:198: if (len > PROGRAM_CAPACITY) {
	xor	a, a
	cp	a, l
	ld	a, #0x02
	sbc	a, h
;apps/oneaddr_basic.c:199: load_demo_program();
;apps/oneaddr_basic.c:200: return;
	jp	C,_load_demo_program
;apps/oneaddr_basic.c:202: if (len < PROGRAM_CAPACITY) {
	ld	a, h
	sub	a, #0x02
	ret	NC
;apps/oneaddr_basic.c:203: PROGRAM_TEXT[len] = 0u;
	ld	bc, #0x6006
	add	hl, bc
	ld	(hl), #0x00
;apps/oneaddr_basic.c:205: }
	ret
;apps/oneaddr_basic.c:207: static unsigned char key_at(unsigned char row, unsigned char col) {
;	---------------------------------
; Function key_at
; ---------------------------------
_key_at:
;apps/oneaddr_basic.c:208: return keyboard_keys[((unsigned char)(row << 3)) + col];
	ld	bc, #_keyboard_keys+0
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	add	a, a
	add	a, a
	add	a, a
	ld	hl, #3
	add	hl, sp
	add	a, (hl)
	ld	l, a
	ld	h, #0x00
	add	hl, bc
	ld	l, (hl)
;apps/oneaddr_basic.c:209: }
	ret
;apps/oneaddr_basic.c:211: static unsigned char typed_key(unsigned char row, unsigned char col) {
;	---------------------------------
; Function typed_key
; ---------------------------------
_typed_key:
;apps/oneaddr_basic.c:214: ch = key_at(row, col);
	ld	iy, #3
	add	iy, sp
	ld	h, 0 (iy)
	ld	l, -1 (iy)
	push	hl
	call	_key_at
	pop	af
;apps/oneaddr_basic.c:215: if (ch == '_') {
	ld	a, l
	sub	a, #0x5f
	ret	NZ
;apps/oneaddr_basic.c:216: return ' ';
	ld	l, #0x20
;apps/oneaddr_basic.c:218: return ch;
;apps/oneaddr_basic.c:219: }
	ret
;apps/oneaddr_basic.c:221: static void key_label(unsigned char key,
;	---------------------------------
; Function key_label
; ---------------------------------
_key_label:
;apps/oneaddr_basic.c:225: *first = 'S';
	ld	iy, #3
	add	iy, sp
	ld	l, 0 (iy)
	ld	h, 1 (iy)
;apps/oneaddr_basic.c:226: *second = 'P';
	ld	c, 2 (iy)
	ld	b, 3 (iy)
	ld	a, -1 (iy)
	sub	a, #0x5f
	jr	NZ, 00102$
;apps/oneaddr_basic.c:225: *first = 'S';
	ld	(hl), #0x53
;apps/oneaddr_basic.c:226: *second = 'P';
	ld	a, #0x50
	ld	(bc), a
;apps/oneaddr_basic.c:227: return;
	ret
00102$:
;apps/oneaddr_basic.c:229: if (key == '\n') {
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	sub	a, #0x0a
	jr	NZ, 00104$
;apps/oneaddr_basic.c:230: *first = 'N';
	ld	(hl), #0x4e
;apps/oneaddr_basic.c:231: *second = 'L';
	ld	a, #0x4c
	ld	(bc), a
;apps/oneaddr_basic.c:232: return;
	ret
00104$:
;apps/oneaddr_basic.c:234: *first = key;
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	ld	(hl), a
;apps/oneaddr_basic.c:235: *second = ' ';
	ld	a, #0x20
	ld	(bc), a
;apps/oneaddr_basic.c:236: }
	ret
;apps/oneaddr_basic.c:238: static void draw_key_label(unsigned char row,
;	---------------------------------
; Function draw_key_label
; ---------------------------------
_draw_key_label:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;apps/oneaddr_basic.c:245: key_label(key, &first, &second);
	ld	hl, #1
	add	hl, sp
	ex	de, hl
	ld	hl, #0
	add	hl, sp
	push	de
	push	hl
	ld	a, 6 (ix)
	push	af
	inc	sp
	call	_key_label
	pop	af
;apps/oneaddr_basic.c:246: text_put(row, col, first);
	inc	sp
	ld	h, -2 (ix)
	ld	l, 5 (ix)
	ex	(sp),hl
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
;apps/oneaddr_basic.c:248: (selected != 0u) ? '<' : second);
	ld	a, 7 (ix)
	or	a, a
	jr	Z, 00103$
	ld	bc, #0x003c
	jr	00104$
00103$:
	ld	c, -1 (ix)
00104$:
	ld	a, c
;apps/oneaddr_basic.c:247: text_put(row, (unsigned char)(col + 1u),
	ld	b, 5 (ix)
	inc	b
	push	af
	inc	sp
	push	bc
	inc	sp
	ld	a, 4 (ix)
	push	af
	inc	sp
	call	_text_put
;apps/oneaddr_basic.c:249: }
	ld	sp,ix
	pop	ix
	ret
;apps/oneaddr_basic.c:251: static void draw_keyboard(unsigned char key_row, unsigned char key_col) {
;	---------------------------------
; Function draw_keyboard
; ---------------------------------
_draw_keyboard:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;apps/oneaddr_basic.c:256: for (row = 0u; row < KEY_ROWS; ++row) {
	ld	c, #0x00
00105$:
;apps/oneaddr_basic.c:257: text_clear_row((unsigned char)(KEY_TOP + row));
	ld	a, c
	add	a, #0x09
	ld	b, a
	push	bc
	push	bc
	inc	sp
	call	_text_clear_row
	inc	sp
	pop	bc
;apps/oneaddr_basic.c:258: for (col = 0u; col < KEY_COLS; ++col) {
	ld	a, 4 (ix)
	sub	a, c
	ld	a, #0x01
	jr	Z, 00141$
	xor	a, a
00141$:
	ld	-2 (ix), a
	ld	e, #0x00
00103$:
;apps/oneaddr_basic.c:259: screen_col = (unsigned char)(col << 1);
	ld	a, e
	add	a, a
	ld	-1 (ix), a
;apps/oneaddr_basic.c:263: ((row == key_row) && (col == key_col)) ? 1u : 0u);
	bit	0, -2 (ix)
	jr	Z, 00109$
	ld	a, 5 (ix)
	sub	a, e
	jr	NZ, 00109$
	ld	hl, #0x0001
	jr	00110$
00109$:
	ld	hl, #0x0000
00110$:
;apps/oneaddr_basic.c:262: key_at(row, col),
	push	hl
	push	bc
	push	de
	ld	b, e
	push	bc
	call	_key_at
	pop	af
	ld	a, l
	pop	de
	pop	bc
	pop	hl
;apps/oneaddr_basic.c:260: draw_key_label((unsigned char)(KEY_TOP + row),
	push	bc
	push	de
	ld	h, l
	push	hl
	inc	sp
	push	af
	inc	sp
	ld	a, -1 (ix)
	push	af
	inc	sp
	push	bc
	inc	sp
	call	_draw_key_label
	pop	af
	pop	af
	pop	de
	pop	bc
;apps/oneaddr_basic.c:258: for (col = 0u; col < KEY_COLS; ++col) {
	inc	e
	ld	a, e
	sub	a, #0x08
	jr	C, 00103$
;apps/oneaddr_basic.c:256: for (row = 0u; row < KEY_ROWS; ++row) {
	inc	c
	ld	a, c
	sub	a, #0x06
	jr	C, 00105$
;apps/oneaddr_basic.c:266: }
	ld	sp, ix
	pop	ix
	ret
;apps/oneaddr_basic.c:268: static unsigned char source_starts_new_row(unsigned int pos,
;	---------------------------------
; Function source_starts_new_row
; ---------------------------------
_source_starts_new_row:
;apps/oneaddr_basic.c:273: *col = 0u;
	ld	iy, #7
	add	iy, sp
	ld	c, 0 (iy)
	ld	b, 1 (iy)
;apps/oneaddr_basic.c:272: if (ch == '\n') {
	ld	a, -1 (iy)
	sub	a, #0x0a
	jr	NZ, 00102$
;apps/oneaddr_basic.c:273: *col = 0u;
	xor	a, a
	ld	(bc), a
;apps/oneaddr_basic.c:274: return 1u;
	ld	l, #0x01
	ret
00102$:
;apps/oneaddr_basic.c:277: ++(*col);
	ld	a, (bc)
	inc	a
	ld	(bc), a
;apps/oneaddr_basic.c:278: if ((*col >= EDIT_VIEW_COLS) && (pos < len)) {
	sub	a, #0x0f
	jr	C, 00104$
	ld	hl, #4
	add	hl, sp
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	sub	a, (hl)
	ld	a, 1 (iy)
	inc	hl
	sbc	a, (hl)
	jr	NC, 00104$
;apps/oneaddr_basic.c:279: *col = 0u;
	xor	a, a
	ld	(bc), a
;apps/oneaddr_basic.c:280: return 1u;
	ld	l, #0x01
	ret
00104$:
;apps/oneaddr_basic.c:282: return 0u;
	ld	l, #0x00
;apps/oneaddr_basic.c:283: }
	ret
;apps/oneaddr_basic.c:285: static unsigned int source_max_window_start(unsigned int len) {
;	---------------------------------
; Function source_max_window_start
; ---------------------------------
_source_max_window_start:
	push	ix
	ld	ix,#0
	add	ix,sp
	ld	hl, #-22
	add	hl, sp
	ld	sp, hl
;apps/oneaddr_basic.c:294: row_starts[0] = 0u;
	ld	hl, #0
	add	hl, sp
	ld	c,l
	ld	b,h
	xor	a, a
	ld	(hl), a
	inc	hl
	ld	(hl), a
;apps/oneaddr_basic.c:297: col = 0u;
	ld	-6 (ix), #0
;apps/oneaddr_basic.c:299: while (pos < len) {
	xor	a, a
	ld	-2 (ix), a
	ld	-1 (ix), a
	ld	de, #0x0000
00103$:
;apps/oneaddr_basic.c:303: ++row_no;
	ld	l, e
	ld	h, d
	inc	hl
	ld	-5 (ix), l
	ld	-4 (ix), h
;apps/oneaddr_basic.c:299: while (pos < len) {
	ld	a, -2 (ix)
	sub	a, 4 (ix)
	ld	a, -1 (ix)
	sbc	a, 5 (ix)
	jr	NC, 00105$
;apps/oneaddr_basic.c:300: ch = PROGRAM_TEXT[pos];
	ld	a, -2 (ix)
	add	a, #0x06
	ld	l, a
	ld	a, -1 (ix)
	adc	a, #0x60
	ld	h, a
	ld	a, (hl)
	ld	-3 (ix), a
;apps/oneaddr_basic.c:301: ++pos;
	inc	-2 (ix)
	jr	NZ, 00131$
	inc	-1 (ix)
00131$:
;apps/oneaddr_basic.c:302: if (source_starts_new_row(pos, len, ch, &col) != 0u) {
	ld	hl, #16
	add	hl, sp
	push	bc
	push	de
	push	hl
	ld	a, -3 (ix)
	push	af
	inc	sp
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	ld	l, -2 (ix)
	ld	h, -1 (ix)
	push	hl
	call	_source_starts_new_row
	pop	af
	pop	af
	pop	af
	ld	a, l
	inc	sp
	pop	de
	pop	bc
	or	a, a
	jr	Z, 00103$
;apps/oneaddr_basic.c:303: ++row_no;
	ld	e, -5 (ix)
	ld	d, -4 (ix)
;apps/oneaddr_basic.c:304: slot = (unsigned char)(row_no & 7u);
	ld	a, -5 (ix)
	and	a, #0x07
;apps/oneaddr_basic.c:305: row_starts[slot] = pos;
	ld	h, #0x00
	ld	l, a
	add	hl, hl
	add	hl, bc
	ld	a, -2 (ix)
	ld	(hl), a
	inc	hl
	ld	a, -1 (ix)
	ld	(hl), a
	jr	00103$
00105$:
;apps/oneaddr_basic.c:309: if ((unsigned int)(row_no + 1u) <= EDIT_TEXT_ROWS) {
	ld	a, #0x07
	cp	a, -5 (ix)
	ld	a, #0x00
	sbc	a, -4 (ix)
	jr	C, 00107$
;apps/oneaddr_basic.c:310: return 0u;
	ld	hl, #0x0000
	jr	00108$
00107$:
;apps/oneaddr_basic.c:313: target_row = (unsigned int)(row_no + 1u - EDIT_TEXT_ROWS);
	ld	a, e
	add	a, #0xfa
;apps/oneaddr_basic.c:314: slot = (unsigned char)(target_row & 7u);
	and	a, #0x07
;apps/oneaddr_basic.c:315: return row_starts[slot];
	ld	l, a
	ld	h, #0x00
	add	hl, hl
	add	hl, bc
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	ex	de, hl
00108$:
;apps/oneaddr_basic.c:316: }
	ld	sp, ix
	pop	ix
	ret
;apps/oneaddr_basic.c:318: static unsigned int source_prev_window_start(unsigned int start) {
;	---------------------------------
; Function source_prev_window_start
; ---------------------------------
_source_prev_window_start:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
	push	af
;apps/oneaddr_basic.c:325: len = program_len();
	call	_program_len
	ex	de, hl
;apps/oneaddr_basic.c:327: prev = 0u;
	xor	a, a
	ld	-3 (ix), a
	ld	-2 (ix), a
;apps/oneaddr_basic.c:328: col = 0u;
	ld	-4 (ix), #0
;apps/oneaddr_basic.c:330: while ((pos < len) && (pos < start)) {
	ld	bc, #0x0000
00106$:
	ld	a, c
	sub	a, e
	ld	a, b
	sbc	a, d
	jr	NC, 00108$
	ld	a, c
	sub	a, 4 (ix)
	ld	a, b
	sbc	a, 5 (ix)
	jr	NC, 00108$
;apps/oneaddr_basic.c:331: ch = PROGRAM_TEXT[pos];
	ld	hl, #0x6006
	add	hl, bc
	ld	a, (hl)
	ld	-1 (ix), a
;apps/oneaddr_basic.c:332: ++pos;
	inc	bc
;apps/oneaddr_basic.c:333: if (source_starts_new_row(pos, len, ch, &col) != 0u) {
	ld	hl, #0
	add	hl, sp
	push	bc
	push	de
	push	hl
	ld	a, -1 (ix)
	push	af
	inc	sp
	push	de
	push	bc
	call	_source_starts_new_row
	pop	af
	pop	af
	pop	af
	ld	a, l
	inc	sp
	pop	de
	pop	bc
	or	a, a
	jr	Z, 00106$
;apps/oneaddr_basic.c:334: if (pos >= start) {
	ld	a, c
	sub	a, 4 (ix)
	ld	a, b
	sbc	a, 5 (ix)
	jr	NC, 00108$
;apps/oneaddr_basic.c:337: prev = pos;
	ld	-3 (ix), c
	ld	-2 (ix), b
	jr	00106$
00108$:
;apps/oneaddr_basic.c:340: return prev;
	ld	l, -3 (ix)
	ld	h, -2 (ix)
;apps/oneaddr_basic.c:341: }
	ld	sp, ix
	pop	ix
	ret
;apps/oneaddr_basic.c:343: static unsigned int source_next_window_start(unsigned int start) {
;	---------------------------------
; Function source_next_window_start
; ---------------------------------
_source_next_window_start:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
	push	af
;apps/oneaddr_basic.c:350: len = program_len();
	call	_program_len
	ld	c, l
	ld	b, h
;apps/oneaddr_basic.c:351: max_start = source_max_window_start(len);
	push	bc
	push	bc
	call	_source_max_window_start
	pop	af
	ex	de, hl
	pop	bc
;apps/oneaddr_basic.c:353: col = 0u;
	ld	-4 (ix), #0
;apps/oneaddr_basic.c:355: while (pos < len) {
	xor	a, a
	ld	-2 (ix), a
	ld	-1 (ix), a
00107$:
	ld	a, -2 (ix)
	sub	a, c
	ld	a, -1 (ix)
	sbc	a, b
	jr	NC, 00109$
;apps/oneaddr_basic.c:356: ch = PROGRAM_TEXT[pos];
	ld	a, -2 (ix)
	add	a, #0x06
	ld	l, a
	ld	a, -1 (ix)
	adc	a, #0x60
	ld	h, a
	ld	a, (hl)
	ld	-3 (ix), a
;apps/oneaddr_basic.c:357: ++pos;
	inc	-2 (ix)
	jr	NZ, 00137$
	inc	-1 (ix)
00137$:
;apps/oneaddr_basic.c:358: if (source_starts_new_row(pos, len, ch, &col) != 0u) {
	ld	hl, #0
	add	hl, sp
	push	bc
	push	de
	push	hl
	ld	a, -3 (ix)
	push	af
	inc	sp
	push	bc
	ld	l, -2 (ix)
	ld	h, -1 (ix)
	push	hl
	call	_source_starts_new_row
	pop	af
	pop	af
	pop	af
	inc	sp
	ld	a, l
	pop	de
	pop	bc
	or	a, a
	jr	Z, 00107$
;apps/oneaddr_basic.c:359: if (pos > start) {
	ld	a, 4 (ix)
	sub	a, -2 (ix)
	ld	a, 5 (ix)
	sbc	a, -1 (ix)
	jr	NC, 00107$
;apps/oneaddr_basic.c:360: if (pos > max_start) {
	ld	a, e
	sub	a, -2 (ix)
	ld	a, d
	sbc	a, -1 (ix)
	jr	NC, 00102$
;apps/oneaddr_basic.c:361: return max_start;
	ex	de, hl
	jr	00110$
00102$:
;apps/oneaddr_basic.c:363: return pos;
	ld	l, -2 (ix)
	ld	h, -1 (ix)
	jr	00110$
00109$:
;apps/oneaddr_basic.c:367: return max_start;
	ex	de, hl
00110$:
;apps/oneaddr_basic.c:368: }
	ld	sp, ix
	pop	ix
	ret
;apps/oneaddr_basic.c:370: static void clamp_editor_view(void) {
;	---------------------------------
; Function clamp_editor_view
; ---------------------------------
_clamp_editor_view:
;apps/oneaddr_basic.c:373: max_start = source_max_window_start(program_len());
	call	_program_len
	push	hl
	call	_source_max_window_start
	pop	af
	ex	de, hl
;apps/oneaddr_basic.c:374: if (editor_view_start > max_start) {
	ld	a, e
	ld	iy, #_editor_view_start
	sub	a, 0 (iy)
	ld	a, d
	sbc	a, 1 (iy)
	ret	NC
;apps/oneaddr_basic.c:375: editor_view_start = max_start;
	ld	(_editor_view_start), de
;apps/oneaddr_basic.c:377: }
	ret
;apps/oneaddr_basic.c:379: static void editor_view_to_end(void) {
;	---------------------------------
; Function editor_view_to_end
; ---------------------------------
_editor_view_to_end:
;apps/oneaddr_basic.c:380: editor_view_start = source_max_window_start(program_len());
	call	_program_len
	push	hl
	call	_source_max_window_start
	pop	af
	ld	(_editor_view_start), hl
;apps/oneaddr_basic.c:381: }
	ret
;apps/oneaddr_basic.c:383: static void editor_scroll_up(void) {
;	---------------------------------
; Function editor_scroll_up
; ---------------------------------
_editor_scroll_up:
;apps/oneaddr_basic.c:384: editor_view_start = source_prev_window_start(editor_view_start);
	ld	hl, (_editor_view_start)
	push	hl
	call	_source_prev_window_start
	pop	af
	ld	(_editor_view_start), hl
;apps/oneaddr_basic.c:385: }
	ret
;apps/oneaddr_basic.c:387: static void editor_scroll_down(void) {
;	---------------------------------
; Function editor_scroll_down
; ---------------------------------
_editor_scroll_down:
;apps/oneaddr_basic.c:390: max_start = source_max_window_start(program_len());
	call	_program_len
	push	hl
	call	_source_max_window_start
	pop	af
	ex	de, hl
;apps/oneaddr_basic.c:391: if (editor_view_start < max_start) {
	ld	hl, (_editor_view_start)
	xor	a, a
	sbc	hl, de
	ret	NC
;apps/oneaddr_basic.c:392: editor_view_start = source_next_window_start(editor_view_start);
	ld	hl, (_editor_view_start)
	push	hl
	call	_source_next_window_start
	pop	af
	ld	(_editor_view_start), hl
;apps/oneaddr_basic.c:394: }
	ret
;apps/oneaddr_basic.c:396: static void draw_editor_scrollbar(unsigned int len) {
;	---------------------------------
; Function draw_editor_scrollbar
; ---------------------------------
_draw_editor_scrollbar:
	push	ix
	ld	ix,#0
	add	ix,sp
	dec	sp
;apps/oneaddr_basic.c:404: max_start = source_max_window_start(len);
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_source_max_window_start
	pop	af
	ld	c, l
	ld	b, h
;apps/oneaddr_basic.c:405: for (row = 0u; row < EDIT_TEXT_ROWS; ++row) {
	ld	d, #0x00
00116$:
;apps/oneaddr_basic.c:406: text_put((unsigned char)(EDIT_TEXT_FIRST_ROW + row), 15u, '.');
	inc	d
	push	bc
	push	de
	ld	hl, #0x2e0f
	push	hl
	push	de
	inc	sp
	call	_text_put
	pop	af
	inc	sp
	pop	de
	pop	bc
;apps/oneaddr_basic.c:405: for (row = 0u; row < EDIT_TEXT_ROWS; ++row) {
	ld	a, d
	sub	a, #0x07
	jr	C, 00116$
;apps/oneaddr_basic.c:409: if (max_start == 0u) {
	ld	a, b
;apps/oneaddr_basic.c:410: for (row = 0u; row < EDIT_TEXT_ROWS; ++row) {
	or	a,c
	jr	NZ, 00104$
	ld	b,a
00118$:
;apps/oneaddr_basic.c:411: text_put((unsigned char)(EDIT_TEXT_FIRST_ROW + row), 15u, '#');
	inc	b
	push	bc
	ld	de, #0x230f
	push	de
	push	bc
	inc	sp
	call	_text_put
	pop	af
	inc	sp
	pop	bc
;apps/oneaddr_basic.c:410: for (row = 0u; row < EDIT_TEXT_ROWS; ++row) {
	ld	a, b
	sub	a, #0x07
	jr	C, 00118$
;apps/oneaddr_basic.c:413: return;
	jr	00123$
00104$:
;apps/oneaddr_basic.c:416: if (editor_view_start == 0u) {
	ld	iy, #_editor_view_start
	ld	a, 1 (iy)
	or	a, 0 (iy)
	jr	NZ, 00114$
;apps/oneaddr_basic.c:417: knob_row = EDIT_TEXT_FIRST_ROW;
	ld	h, #0x01
	jr	00115$
00114$:
;apps/oneaddr_basic.c:418: } else if (editor_view_start >= max_start) {
	ld	hl, (_editor_view_start)
	xor	a, a
	sbc	hl, bc
	jr	C, 00111$
;apps/oneaddr_basic.c:419: knob_row = (unsigned char)(EDIT_TEXT_FIRST_ROW + EDIT_TEXT_ROWS - 1u);
	ld	h, #0x07
	jr	00115$
00111$:
;apps/oneaddr_basic.c:421: scaled = 0u;
	ld	de, #0x0000
;apps/oneaddr_basic.c:422: for (i = 0u; i < (unsigned char)(EDIT_TEXT_ROWS - 1u); ++i) {
	ld	-1 (ix), #0x06
00122$:
;apps/oneaddr_basic.c:423: scaled = (unsigned int)(scaled + editor_view_start);
	ld	a, e
	ld	hl, #_editor_view_start
	add	a, (hl)
	ld	e, a
	ld	a, d
	inc	hl
	adc	a, (hl)
	ld	d, a
	dec	-1 (ix)
;apps/oneaddr_basic.c:422: for (i = 0u; i < (unsigned char)(EDIT_TEXT_ROWS - 1u); ++i) {
	ld	a, -1 (ix)
;apps/oneaddr_basic.c:426: while ((scaled >= max_start) &&
	or	a,a
	jr	NZ, 00122$
	ld	l,a
00107$:
;apps/oneaddr_basic.c:429: ++knob_index;
	ld	h, l
	inc	h
;apps/oneaddr_basic.c:426: while ((scaled >= max_start) &&
	ld	a, e
	sub	a, c
	ld	a, d
	sbc	a, b
	jr	C, 00109$
;apps/oneaddr_basic.c:427: (knob_index < (unsigned char)(EDIT_TEXT_ROWS - 1u))) {
	ld	a, l
	sub	a, #0x06
	jr	NC, 00109$
;apps/oneaddr_basic.c:428: scaled = (unsigned int)(scaled - max_start);
	ld	a, e
	sub	a, c
	ld	e, a
	ld	a, d
	sbc	a, b
	ld	d, a
;apps/oneaddr_basic.c:429: ++knob_index;
	ld	l, h
	jr	00107$
00109$:
;apps/oneaddr_basic.c:431: knob_row = (unsigned char)(EDIT_TEXT_FIRST_ROW + knob_index);
00115$:
;apps/oneaddr_basic.c:433: text_put(knob_row, 15u, '#');
	ld	de, #0x230f
	push	de
	push	hl
	inc	sp
	call	_text_put
	pop	af
	inc	sp
00123$:
;apps/oneaddr_basic.c:434: }
	inc	sp
	pop	ix
	ret
;apps/oneaddr_basic.c:436: static void draw_source_preview(void) {
;	---------------------------------
; Function draw_source_preview
; ---------------------------------
_draw_source_preview:
	push	ix
	ld	ix,#0
	add	ix,sp
	ld	hl, #-6
	add	hl, sp
	ld	sp, hl
;apps/oneaddr_basic.c:443: for (row = 0u; row < EDIT_TEXT_ROWS; ++row) {
	ld	b, #0x00
00117$:
;apps/oneaddr_basic.c:444: text_clear_row((unsigned char)(EDIT_TEXT_FIRST_ROW + row));
	inc	b
	push	bc
	push	bc
	inc	sp
	call	_text_clear_row
	inc	sp
	pop	bc
;apps/oneaddr_basic.c:443: for (row = 0u; row < EDIT_TEXT_ROWS; ++row) {
	ld	a, b
	sub	a, #0x07
	jr	C, 00117$
;apps/oneaddr_basic.c:447: len = program_len();
	call	_program_len
	ex	(sp), hl
;apps/oneaddr_basic.c:448: clamp_editor_view();
	call	_clamp_editor_view
;apps/oneaddr_basic.c:449: if (len == 0u) {
	ld	a, -5 (ix)
	or	a, -6 (ix)
	jr	NZ, 00103$
;apps/oneaddr_basic.c:450: text_write(4u, 5u, "EMPTY");
	ld	hl, #___str_1
	push	hl
	ld	de, #0x0504
	push	de
	call	_text_write
	pop	af
	pop	af
;apps/oneaddr_basic.c:451: draw_editor_scrollbar(len);
	pop	hl
	push	hl
	push	hl
	call	_draw_editor_scrollbar
	pop	af
;apps/oneaddr_basic.c:452: return;
	jp	00119$
00103$:
;apps/oneaddr_basic.c:455: pos = editor_view_start;
	ld	hl, (_editor_view_start)
	ld	-3 (ix), l
	ld	-2 (ix), h
;apps/oneaddr_basic.c:456: row = EDIT_TEXT_FIRST_ROW;
	ld	c, #0x01
;apps/oneaddr_basic.c:457: col = 0u;
	ld	-1 (ix), #0
;apps/oneaddr_basic.c:459: while ((pos < len) &&
00114$:
	ld	a, -3 (ix)
	sub	a, -6 (ix)
	ld	a, -2 (ix)
	sbc	a, -5 (ix)
	jr	NC, 00116$
;apps/oneaddr_basic.c:460: (row < (unsigned char)(EDIT_TEXT_FIRST_ROW + EDIT_TEXT_ROWS))) {
	ld	a, c
	sub	a, #0x08
	jr	NC, 00116$
;apps/oneaddr_basic.c:461: ch = PROGRAM_TEXT[pos];
	ld	l, -3 (ix)
	ld	h, -2 (ix)
	ld	de, #0x6006
	add	hl, de
	ld	b, (hl)
;apps/oneaddr_basic.c:462: ++pos;
	inc	-3 (ix)
	jr	NZ, 00172$
	inc	-2 (ix)
00172$:
;apps/oneaddr_basic.c:444: text_clear_row((unsigned char)(EDIT_TEXT_FIRST_ROW + row));
	ld	a, c
	inc	a
	ld	-4 (ix), a
;apps/oneaddr_basic.c:464: if (ch == '\n') {
	ld	a, b
	sub	a, #0x0a
	jr	NZ, 00107$
;apps/oneaddr_basic.c:465: if (col < EDIT_VIEW_COLS) {
	ld	a, -1 (ix)
	sub	a, #0x0f
	jr	NC, 00105$
;apps/oneaddr_basic.c:466: text_put(row, col, '\\');
	ld	a, #0x5c
	push	af
	inc	sp
	ld	b, -1 (ix)
	push	bc
	call	_text_put
	pop	af
	inc	sp
00105$:
;apps/oneaddr_basic.c:468: ++row;
	ld	c, -4 (ix)
;apps/oneaddr_basic.c:469: col = 0u;
	ld	-1 (ix), #0
;apps/oneaddr_basic.c:470: continue;
	jr	00114$
00107$:
;apps/oneaddr_basic.c:472: if ((ch < 32u) || (ch > 126u)) {
	ld	a, b
	sub	a, #0x20
	jr	C, 00108$
	ld	a, #0x7e
	sub	a, b
	jr	NC, 00109$
00108$:
;apps/oneaddr_basic.c:473: ch = '?';
	ld	b, #0x3f
00109$:
;apps/oneaddr_basic.c:476: text_put(row, col, ch);
	push	bc
	push	bc
	inc	sp
	ld	b, -1 (ix)
	push	bc
	call	_text_put
	pop	af
	inc	sp
	pop	bc
;apps/oneaddr_basic.c:477: ++col;
	inc	-1 (ix)
;apps/oneaddr_basic.c:478: if (col >= EDIT_VIEW_COLS) {
	ld	a, -1 (ix)
	sub	a, #0x0f
	jr	C, 00114$
;apps/oneaddr_basic.c:479: col = 0u;
	ld	-1 (ix), #0
;apps/oneaddr_basic.c:480: ++row;
	ld	c, -4 (ix)
	jr	00114$
00116$:
;apps/oneaddr_basic.c:483: draw_editor_scrollbar(len);
	pop	hl
	push	hl
	push	hl
	call	_draw_editor_scrollbar
	pop	af
00119$:
;apps/oneaddr_basic.c:484: }
	ld	sp, ix
	pop	ix
	ret
___str_1:
	.ascii "EMPTY"
	.db 0x00
;apps/oneaddr_basic.c:486: static void draw_editor_header(unsigned char key_row,
;	---------------------------------
; Function draw_editor_header
; ---------------------------------
_draw_editor_header:
;apps/oneaddr_basic.c:491: len = program_len();
	call	_program_len
;apps/oneaddr_basic.c:492: text_clear_row(0u);
	push	hl
	xor	a, a
	push	af
	inc	sp
	call	_text_clear_row
	inc	sp
	xor	a, a
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_write_u16_3
	pop	af
;apps/oneaddr_basic.c:494: text_write(0u, 3u, "/512 KEY ");
	ld	hl, #___str_2
	ex	(sp),hl
	ld	a, #0x03
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_write
	pop	af
	pop	af
;apps/oneaddr_basic.c:495: draw_key_label(0u, 12u, key_at(key_row, key_col), 0u);
	ld	iy, #3
	add	iy, sp
	ld	h, 0 (iy)
	ld	l, -1 (iy)
	push	hl
	call	_key_at
	pop	af
	ld	a, l
	ld	h, #0x00
	push	hl
	inc	sp
	ld	d,a
	ld	e,#0x0c
	push	de
	xor	a, a
	push	af
	inc	sp
	call	_draw_key_label
	pop	af
	pop	af
;apps/oneaddr_basic.c:496: text_write(0u, 14u, status);
	ld	hl, #4
	add	hl, sp
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	push	bc
	ld	a, #0x0e
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_write
	pop	af
	pop	af
;apps/oneaddr_basic.c:497: }
	ret
___str_2:
	.ascii "/512 KEY "
	.db 0x00
;apps/oneaddr_basic.c:499: static void draw_editor_key(unsigned char key_row,
;	---------------------------------
; Function draw_editor_key
; ---------------------------------
_draw_editor_key:
;apps/oneaddr_basic.c:504: key_at(key_row, key_col),
	ld	iy, #3
	add	iy, sp
	ld	h, 0 (iy)
	ld	l, -1 (iy)
	push	hl
	call	_key_at
	pop	af
	ld	d, l
;apps/oneaddr_basic.c:503: (unsigned char)(key_col << 1),
	ld	iy, #3
	add	iy, sp
	ld	a, 0 (iy)
	add	a, a
	ld	b, a
;apps/oneaddr_basic.c:502: draw_key_label((unsigned char)(KEY_TOP + key_row),
	ld	a, -1 (iy)
	dec	iy
	add	a, #0x09
	ld	h, 2 (iy)
	ld	l, d
	push	hl
	push	bc
	inc	sp
	push	af
	inc	sp
	call	_draw_key_label
	pop	af
	pop	af
;apps/oneaddr_basic.c:506: }
	ret
;apps/oneaddr_basic.c:508: static void move_editor_key(unsigned char old_row,
;	---------------------------------
; Function move_editor_key
; ---------------------------------
_move_editor_key:
	push	ix
	ld	ix,#0
	add	ix,sp
;apps/oneaddr_basic.c:512: if ((old_row == new_row) && (old_col == new_col)) {
	ld	a, 4 (ix)
	sub	a, 6 (ix)
	jr	NZ, 00102$
	ld	a, 5 (ix)
	sub	a, 7 (ix)
	jr	Z, 00104$
;apps/oneaddr_basic.c:513: return;
	jr	00102$
00102$:
;apps/oneaddr_basic.c:515: draw_editor_key(old_row, old_col, 0u);
	xor	a, a
	push	af
	inc	sp
	ld	h, 5 (ix)
	ld	l, 4 (ix)
	push	hl
	call	_draw_editor_key
	pop	af
	inc	sp
;apps/oneaddr_basic.c:516: draw_editor_key(new_row, new_col, 1u);
	ld	h, #0x01
	ld	l, 7 (ix)
	push	hl
	ld	a, 6 (ix)
	push	af
	inc	sp
	call	_draw_editor_key
	pop	af
	inc	sp
00104$:
;apps/oneaddr_basic.c:517: }
	pop	ix
	ret
;apps/oneaddr_basic.c:519: static void draw_editor_shell(unsigned char key_row,
;	---------------------------------
; Function draw_editor_shell
; ---------------------------------
_draw_editor_shell:
;apps/oneaddr_basic.c:522: glic_prepare_screen(GLIC_BLACK);
	xor	a, a
	push	af
	inc	sp
	call	_glic_prepare_screen
	inc	sp
;apps/oneaddr_basic.c:523: draw_editor_header(key_row, key_col, status);
	ld	iy, #4
	add	iy, sp
	ld	l, 0 (iy)
	ld	h, 1 (iy)
	push	hl
	ld	a, -1 (iy)
	dec	iy
	push	af
	inc	sp
	ld	a, -1 (iy)
	push	af
	inc	sp
	call	_draw_editor_header
	pop	af
	pop	af
;apps/oneaddr_basic.c:524: draw_source_preview();
	call	_draw_source_preview
;apps/oneaddr_basic.c:525: text_write(8u, 0u, "B DEL C MENU");
	ld	hl, #___str_3
	push	hl
	xor	a, a
	ld	d,a
	ld	e,#0x08
	push	de
	call	_text_write
	pop	af
	pop	af
;apps/oneaddr_basic.c:526: draw_keyboard(key_row, key_col);
	ld	iy, #3
	add	iy, sp
	ld	h, 0 (iy)
	ld	l, -1 (iy)
	push	hl
	call	_draw_keyboard
;apps/oneaddr_basic.c:527: text_write(15u, 0u, "AA RUN A UD SCR");
	ld	hl, #___str_4
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x0f
	push	de
	call	_text_write
	pop	af
	pop	af
;apps/oneaddr_basic.c:528: }
	ret
___str_3:
	.ascii "B DEL C MENU"
	.db 0x00
___str_4:
	.ascii "AA RUN A UD SCR"
	.db 0x00
;apps/oneaddr_basic.c:530: static void insert_char(unsigned char key_row, unsigned char key_col) {
;	---------------------------------
; Function insert_char
; ---------------------------------
_insert_char:
;apps/oneaddr_basic.c:533: len = program_len();
	call	_program_len
	ex	de, hl
;apps/oneaddr_basic.c:534: if (len >= PROGRAM_CAPACITY) {
	ld	a, d
	sub	a, #0x02
;apps/oneaddr_basic.c:535: return;
	ret	NC
;apps/oneaddr_basic.c:538: PROGRAM_TEXT[len] = typed_key(key_row, key_col);
	ld	hl, #0x6006
	add	hl, de
	push	hl
	push	de
	ld	iy, #7
	add	iy, sp
	ld	h, 0 (iy)
	ld	l, -1 (iy)
	push	hl
	call	_typed_key
	pop	af
	ld	a, l
	pop	de
	pop	hl
	ld	(hl), a
;apps/oneaddr_basic.c:539: ++len;
	inc	de
;apps/oneaddr_basic.c:540: set_program_len(len);
	push	de
	push	de
	call	_set_program_len
	pop	af
	pop	de
;apps/oneaddr_basic.c:541: if (len < PROGRAM_CAPACITY) {
	ld	a, d
	sub	a, #0x02
	ret	NC
;apps/oneaddr_basic.c:542: PROGRAM_TEXT[len] = 0u;
	ld	hl, #0x6006
	add	hl, de
	ex	de, hl
	xor	a, a
	ld	(de), a
;apps/oneaddr_basic.c:544: }
	ret
;apps/oneaddr_basic.c:546: static void delete_char(void) {
;	---------------------------------
; Function delete_char
; ---------------------------------
_delete_char:
;apps/oneaddr_basic.c:549: len = program_len();
	call	_program_len
;apps/oneaddr_basic.c:550: if (len == 0u) {
	ld	a, h
	or	a, l
;apps/oneaddr_basic.c:551: return;
	ret	Z
;apps/oneaddr_basic.c:553: --len;
	dec	hl
;apps/oneaddr_basic.c:554: set_program_len(len);
	push	hl
	push	hl
	call	_set_program_len
	pop	af
	pop	hl
;apps/oneaddr_basic.c:555: PROGRAM_TEXT[len] = 0u;
	ld	bc, #0x6006
	add	hl, bc
	ld	(hl), #0x00
;apps/oneaddr_basic.c:556: }
	ret
;apps/oneaddr_basic.c:558: static void run_editor(void) {
;	---------------------------------
; Function run_editor
; ---------------------------------
_run_editor:
	push	ix
	ld	ix,#0
	add	ix,sp
	ld	hl, #-6
	add	hl, sp
	ld	sp, hl
;apps/oneaddr_basic.c:568: key_row = 0u;
	ld	-2 (ix), #0
;apps/oneaddr_basic.c:569: key_col = 0u;
	ld	-1 (ix), #0
;apps/oneaddr_basic.c:570: a_prefix = 0u;
	ld	-6 (ix), #0
;apps/oneaddr_basic.c:571: editor_view_to_end();
	call	_editor_view_to_end
;apps/oneaddr_basic.c:572: status = "";
;apps/oneaddr_basic.c:573: draw_editor_shell(key_row, key_col, status);
	ld	hl, #___str_5
	push	hl
	xor	a, a
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_draw_editor_shell
	pop	af
	pop	af
;apps/oneaddr_basic.c:574: last_buttons = GLIC_BUTTONS_NONE;
	ld	-4 (ix), #0xff
;apps/oneaddr_basic.c:576: while (1) {
00147$:
;apps/oneaddr_basic.c:577: buttons = glic_read_buttons();
	call	_glic_read_buttons
	ld	-5 (ix), l
;apps/oneaddr_basic.c:578: if (button_edge(buttons, last_buttons, GLIC_BTN_UP) != 0u) {
	ld	h, #0x02
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	-3 (ix), l
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00144$
;apps/oneaddr_basic.c:579: if (a_prefix != 0u) {
	ld	a, -6 (ix)
	or	a, a
	jr	Z, 00105$
;apps/oneaddr_basic.c:580: editor_scroll_up();
	call	_editor_scroll_up
;apps/oneaddr_basic.c:581: a_prefix = 0u;
	ld	-6 (ix), #0
;apps/oneaddr_basic.c:582: status = "SC";
;apps/oneaddr_basic.c:583: draw_editor_header(key_row, key_col, status);
	ld	hl, #___str_6
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	call	_draw_editor_header
	pop	af
	pop	af
;apps/oneaddr_basic.c:584: draw_source_preview();
	call	_draw_source_preview
	jp	00145$
00105$:
;apps/oneaddr_basic.c:586: old_row = key_row;
	ld	a, -2 (ix)
	ld	-4 (ix), a
;apps/oneaddr_basic.c:587: old_col = key_col;
	ld	a, -1 (ix)
	ld	-3 (ix), a
;apps/oneaddr_basic.c:588: if (key_row == 0u) {
	ld	a, -2 (ix)
	or	a, a
	jr	NZ, 00102$
;apps/oneaddr_basic.c:589: key_row = (unsigned char)(KEY_ROWS - 1u);
	ld	-2 (ix), #0x05
	jr	00103$
00102$:
;apps/oneaddr_basic.c:591: --key_row;
	dec	-2 (ix)
00103$:
;apps/oneaddr_basic.c:593: status = "";
;apps/oneaddr_basic.c:594: draw_editor_header(key_row, key_col, status);
	ld	hl, #___str_5
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	call	_draw_editor_header
	pop	af
	pop	af
;apps/oneaddr_basic.c:595: move_editor_key(old_row, old_col, key_row, key_col);
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	ld	h, -3 (ix)
	ld	l, -4 (ix)
	push	hl
	call	_move_editor_key
	pop	af
	pop	af
	jp	00145$
00144$:
;apps/oneaddr_basic.c:597: } else if (button_edge(buttons, last_buttons, GLIC_BTN_DOWN) != 0u) {
	ld	h, #0x08
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00141$
;apps/oneaddr_basic.c:598: if (a_prefix != 0u) {
	ld	a, -6 (ix)
	or	a, a
	jr	Z, 00110$
;apps/oneaddr_basic.c:599: editor_scroll_down();
	call	_editor_scroll_down
;apps/oneaddr_basic.c:600: a_prefix = 0u;
	ld	-6 (ix), #0
;apps/oneaddr_basic.c:601: status = "SC";
;apps/oneaddr_basic.c:602: draw_editor_header(key_row, key_col, status);
	ld	hl, #___str_6
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	call	_draw_editor_header
	pop	af
	pop	af
;apps/oneaddr_basic.c:603: draw_source_preview();
	call	_draw_source_preview
	jp	00145$
00110$:
;apps/oneaddr_basic.c:605: old_row = key_row;
	ld	a, -2 (ix)
	ld	-4 (ix), a
;apps/oneaddr_basic.c:606: old_col = key_col;
	ld	a, -1 (ix)
	ld	-3 (ix), a
;apps/oneaddr_basic.c:607: ++key_row;
	inc	-2 (ix)
;apps/oneaddr_basic.c:608: if (key_row >= KEY_ROWS) {
	ld	a, -2 (ix)
	sub	a, #0x06
	jr	C, 00108$
;apps/oneaddr_basic.c:609: key_row = 0u;
	ld	-2 (ix), #0
00108$:
;apps/oneaddr_basic.c:611: status = "";
;apps/oneaddr_basic.c:612: draw_editor_header(key_row, key_col, status);
	ld	hl, #___str_5
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	call	_draw_editor_header
	pop	af
	pop	af
;apps/oneaddr_basic.c:613: move_editor_key(old_row, old_col, key_row, key_col);
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	ld	h, -3 (ix)
	ld	l, -4 (ix)
	push	hl
	call	_move_editor_key
	pop	af
	pop	af
	jp	00145$
00141$:
;apps/oneaddr_basic.c:615: } else if (button_edge(buttons, last_buttons, GLIC_BTN_LEFT) != 0u) {
	ld	h, #0x10
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00138$
;apps/oneaddr_basic.c:616: a_prefix = 0u;
	ld	-6 (ix), #0
;apps/oneaddr_basic.c:617: old_row = key_row;
	ld	a, -2 (ix)
	ld	-3 (ix), a
;apps/oneaddr_basic.c:618: old_col = key_col;
	ld	c, -1 (ix)
;apps/oneaddr_basic.c:619: if (key_col == 0u) {
	ld	a, -1 (ix)
	or	a, a
	jr	NZ, 00113$
;apps/oneaddr_basic.c:620: key_col = (unsigned char)(KEY_COLS - 1u);
	ld	-1 (ix), #0x07
	jr	00114$
00113$:
;apps/oneaddr_basic.c:622: --key_col;
	dec	-1 (ix)
00114$:
;apps/oneaddr_basic.c:624: status = "";
;apps/oneaddr_basic.c:625: draw_editor_header(key_row, key_col, status);
	push	bc
	ld	hl, #___str_5
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	call	_draw_editor_header
	pop	af
	pop	af
	pop	bc
;apps/oneaddr_basic.c:626: move_editor_key(old_row, old_col, key_row, key_col);
	ld	a, -1 (ix)
	push	af
	inc	sp
	ld	b, -2 (ix)
	push	bc
	ld	a, -3 (ix)
	push	af
	inc	sp
	call	_move_editor_key
	pop	af
	pop	af
	jp	00145$
00138$:
;apps/oneaddr_basic.c:627: } else if (button_edge(buttons, last_buttons, GLIC_BTN_RIGHT) != 0u) {
	ld	h, #0x04
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00135$
;apps/oneaddr_basic.c:628: a_prefix = 0u;
	ld	-6 (ix), #0
;apps/oneaddr_basic.c:629: old_row = key_row;
	ld	b, -2 (ix)
;apps/oneaddr_basic.c:630: old_col = key_col;
	ld	d, -1 (ix)
;apps/oneaddr_basic.c:631: ++key_col;
	inc	-1 (ix)
;apps/oneaddr_basic.c:632: if (key_col >= KEY_COLS) {
	ld	a, -1 (ix)
	sub	a, #0x08
	jr	C, 00116$
;apps/oneaddr_basic.c:633: key_col = 0u;
	ld	-1 (ix), #0
00116$:
;apps/oneaddr_basic.c:635: status = "";
;apps/oneaddr_basic.c:636: draw_editor_header(key_row, key_col, status);
	push	bc
	push	de
	ld	hl, #___str_5
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	call	_draw_editor_header
	pop	af
	pop	af
	pop	de
	pop	bc
;apps/oneaddr_basic.c:637: move_editor_key(old_row, old_col, key_row, key_col);
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	ld	e, b
	push	de
	call	_move_editor_key
	pop	af
	pop	af
	jp	00145$
00135$:
;apps/oneaddr_basic.c:638: } else if (button_edge(buttons, last_buttons, GLIC_BTN_CENTER) != 0u) {
	ld	h, #0x01
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00132$
;apps/oneaddr_basic.c:639: a_prefix = 0u;
	ld	-6 (ix), #0
;apps/oneaddr_basic.c:640: if (program_len() < PROGRAM_CAPACITY) {
	call	_program_len
	ld	a, h
	sub	a, #0x02
	jr	NC, 00118$
;apps/oneaddr_basic.c:641: insert_char(key_row, key_col);
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	call	_insert_char
	pop	af
;apps/oneaddr_basic.c:642: editor_view_to_end();
	call	_editor_view_to_end
;apps/oneaddr_basic.c:643: status = "";
	ld	bc, #___str_5
	jr	00119$
00118$:
;apps/oneaddr_basic.c:645: status = "FL";
	ld	bc, #___str_7
00119$:
;apps/oneaddr_basic.c:647: draw_editor_header(key_row, key_col, status);
	push	bc
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	call	_draw_editor_header
	pop	af
	pop	af
;apps/oneaddr_basic.c:648: draw_source_preview();
	call	_draw_source_preview
	jp	00145$
00132$:
;apps/oneaddr_basic.c:649: } else if (button_edge(buttons, last_buttons, GLIC_BTN_B) != 0u) {
	ld	h, #0x40
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00129$
;apps/oneaddr_basic.c:650: a_prefix = 0u;
	ld	-6 (ix), #0
;apps/oneaddr_basic.c:651: delete_char();
	call	_delete_char
;apps/oneaddr_basic.c:652: editor_view_to_end();
	call	_editor_view_to_end
;apps/oneaddr_basic.c:653: status = "";
;apps/oneaddr_basic.c:654: draw_editor_header(key_row, key_col, status);
	ld	hl, #___str_5
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	call	_draw_editor_header
	pop	af
	pop	af
;apps/oneaddr_basic.c:655: draw_source_preview();
	call	_draw_source_preview
	jr	00145$
00129$:
;apps/oneaddr_basic.c:656: } else if (button_edge(buttons, last_buttons, GLIC_BTN_A) != 0u) {
	ld	h, #0x80
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00126$
;apps/oneaddr_basic.c:657: if (a_prefix != 0u) {
	ld	a, -6 (ix)
	or	a, a
	jr	Z, 00121$
;apps/oneaddr_basic.c:658: wait_for_release();
	call	_wait_for_release
;apps/oneaddr_basic.c:659: a_prefix = 0u;
	ld	-6 (ix), #0
;apps/oneaddr_basic.c:660: run_program();
	call	_run_program
;apps/oneaddr_basic.c:661: status = "";
;apps/oneaddr_basic.c:662: draw_editor_shell(key_row, key_col, status);
	ld	hl, #___str_5
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	call	_draw_editor_shell
	pop	af
	pop	af
;apps/oneaddr_basic.c:663: last_buttons = GLIC_BUTTONS_NONE;
	jr	00145$
00121$:
;apps/oneaddr_basic.c:665: a_prefix = 1u;
	ld	-6 (ix), #0x01
;apps/oneaddr_basic.c:666: status = "A?";
;apps/oneaddr_basic.c:667: draw_editor_header(key_row, key_col, status);
	ld	hl, #___str_8
	push	hl
	ld	h, -1 (ix)
	ld	l, -2 (ix)
	push	hl
	call	_draw_editor_header
	pop	af
	pop	af
	jr	00145$
00126$:
;apps/oneaddr_basic.c:669: } else if (button_edge(buttons, last_buttons, GLIC_BTN_C) != 0u) {
	ld	h, #0x20
	ld	l, -4 (ix)
	push	hl
	ld	a, -5 (ix)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00145$
;apps/oneaddr_basic.c:670: wait_for_release();
	call	_wait_for_release
;apps/oneaddr_basic.c:671: return;
	jr	00149$
00145$:
;apps/oneaddr_basic.c:674: last_buttons = buttons;
	ld	a, -5 (ix)
	ld	-4 (ix), a
;apps/oneaddr_basic.c:675: glic_delay(LOOP_DELAY);
	ld	hl, #0x0384
	push	hl
	call	_glic_delay
	pop	af
	jp	00147$
00149$:
;apps/oneaddr_basic.c:677: }
	ld	sp, ix
	pop	ix
	ret
___str_5:
	.db 0x00
___str_6:
	.ascii "SC"
	.db 0x00
___str_7:
	.ascii "FL"
	.db 0x00
___str_8:
	.ascii "A?"
	.db 0x00
;apps/oneaddr_basic.c:679: static void copy_text_row(unsigned char from_row, unsigned char to_row) {
;	---------------------------------
; Function copy_text_row
; ---------------------------------
_copy_text_row:
;apps/oneaddr_basic.c:682: for (col = 0u; col < 16u; ++col) {
	ld	b, #0x00
00102$:
;apps/oneaddr_basic.c:683: text_put(to_row, col, *text_cell(from_row, col));
	push	bc
	push	bc
	inc	sp
	ld	hl, #5
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_text_cell
	pop	af
	pop	bc
	ld	a, (hl)
	push	bc
	push	af
	inc	sp
	push	bc
	inc	sp
	ld	hl, #7
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
	pop	bc
;apps/oneaddr_basic.c:682: for (col = 0u; col < 16u; ++col) {
	inc	b
	ld	a, b
	sub	a, #0x10
	jr	C, 00102$
;apps/oneaddr_basic.c:685: }
	ret
;apps/oneaddr_basic.c:687: static void output_scroll(void) {
;	---------------------------------
; Function output_scroll
; ---------------------------------
_output_scroll:
;apps/oneaddr_basic.c:690: for (row = 1u; row < 14u; ++row) {
	ld	a, #0x01
00102$:
;apps/oneaddr_basic.c:691: copy_text_row((unsigned char)(row + 1u), row);
	ld	b, a
	inc	b
	push	bc
	push	af
	inc	sp
	push	bc
	inc	sp
	call	_copy_text_row
	pop	af
	pop	bc
;apps/oneaddr_basic.c:690: for (row = 1u; row < 14u; ++row) {
	ld	a, b
	cp	a, #0x0e
	jr	C, 00102$
;apps/oneaddr_basic.c:693: text_clear_row(14u);
	ld	a, #0x0e
	push	af
	inc	sp
	call	_text_clear_row
	inc	sp
;apps/oneaddr_basic.c:694: output_row = 14u;
	ld	a, #0x0e
	ld	(#_output_row), a
;apps/oneaddr_basic.c:695: output_col = 0u;
	ld	a, #0x00
	ld	(#_output_col), a
;apps/oneaddr_basic.c:696: }
	ret
;apps/oneaddr_basic.c:698: static void output_newline(void) {
;	---------------------------------
; Function output_newline
; ---------------------------------
_output_newline:
;apps/oneaddr_basic.c:699: output_col = 0u;
	ld	a, #0x00
	ld	(#_output_col), a
;apps/oneaddr_basic.c:700: if (output_row < 14u) {
	ld	iy, #_output_row
	ld	a, 0 (iy)
	sub	a, #0x0e
	jp	NC,_output_scroll
;apps/oneaddr_basic.c:701: ++output_row;
	inc	0 (iy)
;apps/oneaddr_basic.c:703: output_scroll();
	ret
;apps/oneaddr_basic.c:705: }
	ret
;apps/oneaddr_basic.c:707: static void output_char(unsigned char ch) {
;	---------------------------------
; Function output_char
; ---------------------------------
_output_char:
;apps/oneaddr_basic.c:708: if (ch == '\n') {
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	sub	a, #0x0a
	jp	Z,_output_newline
;apps/oneaddr_basic.c:709: output_newline();
;apps/oneaddr_basic.c:710: return;
	jr	00102$
00102$:
;apps/oneaddr_basic.c:712: if ((ch < 32u) || (ch > 126u)) {
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	sub	a, #0x20
	jr	C, 00103$
	ld	a, #0x7e
	sub	a, 0 (iy)
	jr	NC, 00104$
00103$:
;apps/oneaddr_basic.c:713: ch = ' ';
	ld	iy, #2
	add	iy, sp
	ld	0 (iy), #0x20
00104$:
;apps/oneaddr_basic.c:715: if (output_col >= 16u) {
	ld	a,(#_output_col + 0)
	sub	a, #0x10
	jr	C, 00107$
;apps/oneaddr_basic.c:716: output_newline();
	call	_output_newline
00107$:
;apps/oneaddr_basic.c:718: text_put(output_row, output_col, ch);
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	ld	a,(#_output_col + 0)
	push	af
	inc	sp
	ld	a,(#_output_row + 0)
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
;apps/oneaddr_basic.c:719: ++output_col;
	ld	iy, #_output_col
	inc	0 (iy)
;apps/oneaddr_basic.c:720: }
	ret
;apps/oneaddr_basic.c:722: static void output_text(const char *text) {
;	---------------------------------
; Function output_text
; ---------------------------------
_output_text:
;apps/oneaddr_basic.c:723: while (*text != 0) {
	pop	de
	pop	bc
	push	bc
	push	de
00101$:
	ld	a, (bc)
	or	a, a
	ret	Z
;apps/oneaddr_basic.c:724: output_char((unsigned char)*text);
	push	bc
	push	af
	inc	sp
	call	_output_char
	inc	sp
	pop	bc
;apps/oneaddr_basic.c:725: ++text;
	inc	bc
;apps/oneaddr_basic.c:727: }
	jr	00101$
;apps/oneaddr_basic.c:729: static unsigned int abs_to_unsigned(int value) {
;	---------------------------------
; Function abs_to_unsigned
; ---------------------------------
_abs_to_unsigned:
;apps/oneaddr_basic.c:731: return (unsigned int)(0u - (unsigned int)value);
	pop	bc
	pop	de
	push	de
	push	bc
;apps/oneaddr_basic.c:730: if (value < 0) {
	ld	iy, #2
	add	iy, sp
	ld	a, 1 (iy)
	bit	7, a
	jr	Z, 00102$
;apps/oneaddr_basic.c:731: return (unsigned int)(0u - (unsigned int)value);
	ld	hl, #0x0000
	cp	a, a
	sbc	hl, de
	ret
00102$:
;apps/oneaddr_basic.c:733: return (unsigned int)value;
	ex	de, hl
;apps/oneaddr_basic.c:734: }
	ret
;apps/oneaddr_basic.c:736: static int math_mul(int left, int right) {
;	---------------------------------
; Function math_mul
; ---------------------------------
_math_mul:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;apps/oneaddr_basic.c:742: negative = 0u;
	ld	l, #0x00
;apps/oneaddr_basic.c:745: a = (unsigned int)(0u - (unsigned int)left);
	ld	e, 4 (ix)
	ld	d, 5 (ix)
;apps/oneaddr_basic.c:743: if (left < 0) {
	bit	7, 5 (ix)
	jr	Z, 00102$
;apps/oneaddr_basic.c:744: negative = 1u;
	ld	l, #0x01
;apps/oneaddr_basic.c:745: a = (unsigned int)(0u - (unsigned int)left);
	xor	a, a
	sub	a, e
	ld	e, a
	ld	a, #0x00
	sbc	a, d
	ld	d, a
;apps/oneaddr_basic.c:747: a = (unsigned int)left;
00102$:
;apps/oneaddr_basic.c:751: b = (unsigned int)(0u - (unsigned int)right);
	ld	c, 6 (ix)
	ld	b, 7 (ix)
;apps/oneaddr_basic.c:749: if (right < 0) {
	bit	7, 7 (ix)
	jr	Z, 00105$
;apps/oneaddr_basic.c:750: negative ^= 1u;
	ld	a, l
	xor	a, #0x01
	ld	l, a
;apps/oneaddr_basic.c:751: b = (unsigned int)(0u - (unsigned int)right);
	xor	a, a
	sub	a, c
	ld	c, a
	ld	a, #0x00
	sbc	a, b
	ld	b, a
;apps/oneaddr_basic.c:753: b = (unsigned int)right;
00105$:
;apps/oneaddr_basic.c:756: result = 0u;
	xor	a, a
	ld	-2 (ix), a
	ld	-1 (ix), a
;apps/oneaddr_basic.c:757: while (b != 0u) {
00109$:
	ld	a, b
	or	a, c
	jr	Z, 00111$
;apps/oneaddr_basic.c:758: if ((b & 1u) != 0u) {
	bit	0, c
	jr	Z, 00108$
;apps/oneaddr_basic.c:759: result = (unsigned int)(result + a);
	ld	a, -2 (ix)
	add	a, e
	ld	-2 (ix), a
	ld	a, -1 (ix)
	adc	a, d
	ld	-1 (ix), a
00108$:
;apps/oneaddr_basic.c:761: a = (unsigned int)(a << 1);
	ex	de, hl
	add	hl, hl
	ex	de, hl
;apps/oneaddr_basic.c:762: b = (unsigned int)(b >> 1);
	srl	b
	rr	c
	jr	00109$
00111$:
;apps/oneaddr_basic.c:765: if (negative != 0u) {
	ld	a, l
	or	a, a
	jr	Z, 00113$
;apps/oneaddr_basic.c:766: return (int)(0u - result);
	xor	a, a
	sub	a, -2 (ix)
	ld	l, a
	ld	a, #0x00
	sbc	a, -1 (ix)
	ld	h, a
	jr	00114$
00113$:
;apps/oneaddr_basic.c:768: return (int)result;
	pop	de
	ex	de, hl
	push	hl
00114$:
;apps/oneaddr_basic.c:769: }
	ld	sp, ix
	pop	ix
	ret
;apps/oneaddr_basic.c:771: static unsigned int math_udiv(unsigned int numerator,
;	---------------------------------
; Function math_udiv
; ---------------------------------
_math_udiv:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;apps/oneaddr_basic.c:778: if (denominator == 0u) {
	ld	a, 7 (ix)
	or	a, 6 (ix)
	jr	NZ, 00102$
;apps/oneaddr_basic.c:779: return 0u;
	ld	hl, #0x0000
	jr	00114$
00102$:
;apps/oneaddr_basic.c:782: quotient = 0u;
	ld	bc, #0x0000
;apps/oneaddr_basic.c:783: shifted = denominator;
	ld	e, 6 (ix)
	ld	d, 7 (ix)
;apps/oneaddr_basic.c:784: bit = 1u;
	ld	hl, #0x0001
;apps/oneaddr_basic.c:786: while (shifted <= numerator) {
00106$:
	ld	a, 4 (ix)
	sub	a, e
	ld	a, 5 (ix)
	sbc	a, d
	jr	C, 00111$
;apps/oneaddr_basic.c:787: next_shifted = (unsigned int)(shifted << 1);
	inc	sp
	inc	sp
	push	de
	sla	-2 (ix)
	rl	-1 (ix)
;apps/oneaddr_basic.c:788: if ((next_shifted <= shifted) || (next_shifted > numerator)) {
	ld	a, e
	sub	a, -2 (ix)
	ld	a, d
	sbc	a, -1 (ix)
	jr	NC, 00111$
	ld	a, 4 (ix)
	sub	a, -2 (ix)
	ld	a, 5 (ix)
	sbc	a, -1 (ix)
	jr	C, 00111$
;apps/oneaddr_basic.c:791: shifted = next_shifted;
	pop	de
	push	de
;apps/oneaddr_basic.c:792: bit = (unsigned int)(bit << 1);
	add	hl, hl
	jr	00106$
;apps/oneaddr_basic.c:795: while (bit != 0u) {
00111$:
	ld	a, h
	or	a, l
	jr	Z, 00113$
;apps/oneaddr_basic.c:796: if (numerator >= shifted) {
	ld	a, 4 (ix)
	sub	a, e
	ld	a, 5 (ix)
	sbc	a, d
	jr	C, 00110$
;apps/oneaddr_basic.c:797: numerator = (unsigned int)(numerator - shifted);
	ld	a, 4 (ix)
	sub	a, e
	ld	4 (ix), a
	ld	a, 5 (ix)
	sbc	a, d
	ld	5 (ix), a
;apps/oneaddr_basic.c:798: quotient = (unsigned int)(quotient | bit);
	ld	a, c
	or	a, l
	ld	c, a
	ld	a, b
	or	a, h
	ld	b, a
00110$:
;apps/oneaddr_basic.c:800: shifted = (unsigned int)(shifted >> 1);
	srl	d
	rr	e
;apps/oneaddr_basic.c:801: bit = (unsigned int)(bit >> 1);
	srl	h
	rr	l
	jr	00111$
00113$:
;apps/oneaddr_basic.c:804: return quotient;
	ld	l, c
	ld	h, b
00114$:
;apps/oneaddr_basic.c:805: }
	ld	sp, ix
	pop	ix
	ret
;apps/oneaddr_basic.c:807: static int math_div(int left, int right) {
;	---------------------------------
; Function math_div
; ---------------------------------
_math_div:
	dec	sp
;apps/oneaddr_basic.c:811: negative = 0u;
	xor	a, a
	ld	iy, #0
	add	iy, sp
	ld	0 (iy), a
;apps/oneaddr_basic.c:812: if (left < 0) {
	inc	iy
	inc	iy
	inc	iy
	bit	7, 1 (iy)
	jr	Z, 00102$
;apps/oneaddr_basic.c:813: negative = 1u;
	dec	iy
	dec	iy
	dec	iy
	ld	0 (iy), #0x01
00102$:
;apps/oneaddr_basic.c:815: if (right < 0) {
	ld	hl, #5 + 1
	add	hl, sp
	bit	7, (hl)
	jr	Z, 00104$
;apps/oneaddr_basic.c:816: negative ^= 1u;
	ld	iy, #0
	add	iy, sp
	ld	a, 0 (iy)
	xor	a, #0x01
	ld	0 (iy), a
00104$:
;apps/oneaddr_basic.c:819: quotient = math_udiv(abs_to_unsigned(left), abs_to_unsigned(right));
	ld	hl, #5
	add	hl, sp
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	push	bc
	call	_abs_to_unsigned
	ex	(sp),hl
	ld	hl, #5
	add	hl, sp
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	push	de
	call	_abs_to_unsigned
	ex	(sp),hl
	call	_math_udiv
	pop	af
	pop	af
;apps/oneaddr_basic.c:820: if (negative != 0u) {
	ld	iy, #0
	add	iy, sp
	ld	a, 0 (iy)
	or	a, a
	jr	Z, 00106$
;apps/oneaddr_basic.c:821: return (int)(0u - quotient);
	xor	a, a
	sub	a, l
	ld	l, a
	ld	a, #0x00
	sbc	a, h
	ld	h, a
;apps/oneaddr_basic.c:823: return (int)quotient;
00106$:
;apps/oneaddr_basic.c:824: }
	inc	sp
	ret
;apps/oneaddr_basic.c:826: static void output_number(int value) {
;	---------------------------------
; Function output_number
; ---------------------------------
_output_number:
	push	ix
	ld	ix,#0
	add	ix,sp
	dec	sp
;apps/oneaddr_basic.c:835: if (value < 0) {
	bit	7, 5 (ix)
	jr	Z, 00102$
;apps/oneaddr_basic.c:836: output_char('-');
	ld	a, #0x2d
	push	af
	inc	sp
	call	_output_char
	inc	sp
;apps/oneaddr_basic.c:837: n = abs_to_unsigned(value);
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_abs_to_unsigned
	pop	af
	ex	de, hl
	jr	00103$
00102$:
;apps/oneaddr_basic.c:839: n = (unsigned int)value;
	ld	e, 4 (ix)
	ld	d, 5 (ix)
00103$:
;apps/oneaddr_basic.c:842: started = 0u;
;apps/oneaddr_basic.c:843: for (i = 0u; i < 5u; ++i) {
	ld	bc, #0x0000
;apps/oneaddr_basic.c:845: while (n >= places[i]) {
00118$:
	ld	-1 (ix), #0
00104$:
	ld	a, b
	ld	h, #0x00
	ld	l, a
	add	hl, hl
	push	de
	ld	de, #_output_number_places_65536_241
	add	hl, de
	pop	de
	ld	a, (hl)
	inc	hl
	ld	h, (hl)
	ld	l, a
	ld	a, e
	sub	a, l
	ld	a, d
	sbc	a, h
	jr	C, 00122$
;apps/oneaddr_basic.c:846: n = (unsigned int)(n - places[i]);
	ld	a, e
	sub	a, l
	ld	e, a
	ld	a, d
	sbc	a, h
	ld	d, a
;apps/oneaddr_basic.c:847: ++digit;
	inc	-1 (ix)
	jr	00104$
00122$:
	ld	l, -1 (ix)
;apps/oneaddr_basic.c:849: if ((digit != 0u) || (started != 0u) || (i == 4u)) {
	ld	a, -1 (ix)
	or	a,a
	jr	NZ, 00107$
	or	a,c
	jr	NZ, 00107$
	ld	a, b
	sub	a, #0x04
	jr	NZ, 00113$
00107$:
;apps/oneaddr_basic.c:850: output_char((unsigned char)('0' + digit));
	ld	a, l
	add	a, #0x30
	push	bc
	push	de
	push	af
	inc	sp
	call	_output_char
	inc	sp
	pop	de
	pop	bc
;apps/oneaddr_basic.c:851: started = 1u;
	ld	c, #0x01
00113$:
;apps/oneaddr_basic.c:843: for (i = 0u; i < 5u; ++i) {
	inc	b
	ld	a, b
	sub	a, #0x05
	jr	C, 00118$
;apps/oneaddr_basic.c:854: }
	inc	sp
	pop	ix
	ret
_output_number_places_65536_241:
	.dw #0x2710
	.dw #0x03e8
	.dw #0x0064
	.dw #0x000a
	.dw #0x0001
;apps/oneaddr_basic.c:856: static void output_clear(void) {
;	---------------------------------
; Function output_clear
; ---------------------------------
_output_clear:
;apps/oneaddr_basic.c:857: glic_prepare_screen(GLIC_BLACK);
	xor	a, a
	push	af
	inc	sp
	call	_glic_prepare_screen
	inc	sp
;apps/oneaddr_basic.c:858: text_write(0u, 0u, "RUN ONEADDR");
	ld	hl, #___str_9
	push	hl
	xor	a, a
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_write
	pop	af
;apps/oneaddr_basic.c:859: text_write(15u, 0u, "C STOP");
	ld	hl, #___str_10
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x0f
	push	de
	call	_text_write
	pop	af
	pop	af
;apps/oneaddr_basic.c:860: output_row = 1u;
	ld	a, #0x01
	ld	(#_output_row), a
;apps/oneaddr_basic.c:861: output_col = 0u;
	ld	a, #0x00
	ld	(#_output_col), a
;apps/oneaddr_basic.c:862: }
	ret
___str_9:
	.ascii "RUN ONEADDR"
	.db 0x00
___str_10:
	.ascii "C STOP"
	.db 0x00
;apps/oneaddr_basic.c:864: static unsigned char read_buttons_value(void) {
;	---------------------------------
; Function read_buttons_value
; ---------------------------------
_read_buttons_value:
;apps/oneaddr_basic.c:865: return (unsigned char)(~glic_read_buttons());
	call	_glic_read_buttons
	ld	a, l
	cpl
	ld	l, a
;apps/oneaddr_basic.c:866: }
	ret
;apps/oneaddr_basic.c:868: static unsigned char source_at(unsigned int pos) {
;	---------------------------------
; Function source_at
; ---------------------------------
_source_at:
;apps/oneaddr_basic.c:869: if (pos >= run_len) {
	ld	hl, #_run_len
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	sub	a, (hl)
	ld	a, 1 (iy)
	inc	hl
	sbc	a, (hl)
	jr	C, 00102$
;apps/oneaddr_basic.c:870: return 0u;
	ld	l, #0x00
	ret
00102$:
;apps/oneaddr_basic.c:872: return PROGRAM_TEXT[pos];
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	inc	hl
	ld	h, (hl)
	ld	l, a
	ld	de, #0x6006
	add	hl, de
	ld	l, (hl)
;apps/oneaddr_basic.c:873: }
	ret
;apps/oneaddr_basic.c:875: static unsigned char upper_char(unsigned char ch) {
;	---------------------------------
; Function upper_char
; ---------------------------------
_upper_char:
;apps/oneaddr_basic.c:876: if ((ch >= 'a') && (ch <= 'z')) {
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	sub	a, #0x61
	jr	C, 00102$
	ld	a, #0x7a
	sub	a, 0 (iy)
	jr	C, 00102$
;apps/oneaddr_basic.c:877: return (unsigned char)(ch - ('a' - 'A'));
	ld	a, 0 (iy)
	add	a, #0xe0
	ld	l, a
	ret
00102$:
;apps/oneaddr_basic.c:879: return ch;
	ld	iy, #2
	add	iy, sp
	ld	l, 0 (iy)
;apps/oneaddr_basic.c:880: }
	ret
;apps/oneaddr_basic.c:882: static unsigned char is_digit_char(unsigned char ch) {
;	---------------------------------
; Function is_digit_char
; ---------------------------------
_is_digit_char:
;apps/oneaddr_basic.c:883: return ((ch >= '0') && (ch <= '9'));
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	sub	a, #0x30
	jr	C, 00103$
	ld	a, #0x39
	sub	a, 0 (iy)
	jr	NC, 00104$
00103$:
	ld	l, #0x00
	ret
00104$:
	ld	l, #0x01
;apps/oneaddr_basic.c:884: }
	ret
;apps/oneaddr_basic.c:886: static unsigned char is_name_char(unsigned char ch) {
;	---------------------------------
; Function is_name_char
; ---------------------------------
_is_name_char:
;apps/oneaddr_basic.c:887: ch = upper_char(ch);
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_upper_char
	inc	sp
;apps/oneaddr_basic.c:888: return (((ch >= 'A') && (ch <= 'Z')) ||
	ld	a, l
	sub	a, #0x41
	jr	C, 00111$
	ld	a, #0x5a
	sub	a, l
	jr	NC, 00104$
00111$:
;apps/oneaddr_basic.c:889: ((ch >= '0') && (ch <= '9')) ||
	ld	a, l
	sub	a, #0x30
	jr	C, 00108$
	ld	a, #0x39
	sub	a, l
	jr	NC, 00104$
00108$:
;apps/oneaddr_basic.c:890: (ch == '_'));
	ld	a, l
	sub	a, #0x5f
	ld	l, #0x00
	ret	NZ
00104$:
	ld	l, #0x01
;apps/oneaddr_basic.c:891: }
	ret
;apps/oneaddr_basic.c:893: static void skip_spaces(unsigned int *pos) {
;	---------------------------------
; Function skip_spaces
; ---------------------------------
_skip_spaces:
	push	ix
	ld	ix,#0
	add	ix,sp
;apps/oneaddr_basic.c:896: while (*pos < run_len) {
00105$:
	ld	c, 4 (ix)
	ld	b, 5 (ix)
	ld	l, c
	ld	h, b
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	ld	hl, #_run_len
	ld	a, e
	sub	a, (hl)
	ld	a, d
	inc	hl
	sbc	a, (hl)
	jr	NC, 00108$
;apps/oneaddr_basic.c:897: ch = source_at(*pos);
	push	bc
	push	de
	call	_source_at
	pop	af
	ld	a, l
	pop	bc
;apps/oneaddr_basic.c:898: if ((ch != ' ') && (ch != '\t') && (ch != '\r')) {
	cp	a, #0x20
	jr	Z, 00102$
	cp	a, #0x09
	jr	Z, 00102$
	sub	a, #0x0d
;apps/oneaddr_basic.c:899: return;
	jr	NZ, 00108$
00102$:
;apps/oneaddr_basic.c:901: ++(*pos);
	ld	l, c
	ld	h, b
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	inc	de
	ld	a, e
	ld	(bc), a
	inc	bc
	ld	a, d
	ld	(bc), a
	jr	00105$
00108$:
;apps/oneaddr_basic.c:903: }
	pop	ix
	ret
;apps/oneaddr_basic.c:905: static void skip_statement_gaps(unsigned int *pos) {
;	---------------------------------
; Function skip_statement_gaps
; ---------------------------------
_skip_statement_gaps:
	push	ix
	ld	ix,#0
	add	ix,sp
;apps/oneaddr_basic.c:908: while (*pos < run_len) {
00106$:
	ld	c, 4 (ix)
	ld	b, 5 (ix)
	ld	l, c
	ld	h, b
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	ld	hl, #_run_len
	ld	a, e
	sub	a, (hl)
	ld	a, d
	inc	hl
	sbc	a, (hl)
	jr	NC, 00109$
;apps/oneaddr_basic.c:909: ch = source_at(*pos);
	push	bc
	push	de
	call	_source_at
	pop	af
	ld	a, l
	pop	bc
;apps/oneaddr_basic.c:910: if ((ch != ' ') && (ch != '\t') &&
	cp	a, #0x20
	jr	Z, 00102$
	cp	a, #0x09
	jr	Z, 00102$
;apps/oneaddr_basic.c:911: (ch != '\r') && (ch != '\n')) {
	cp	a, #0x0d
	jr	Z, 00102$
	sub	a, #0x0a
;apps/oneaddr_basic.c:912: return;
	jr	NZ, 00109$
00102$:
;apps/oneaddr_basic.c:914: ++(*pos);
	ld	l, c
	ld	h, b
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	inc	de
	ld	a, e
	ld	(bc), a
	inc	bc
	ld	a, d
	ld	(bc), a
	jr	00106$
00109$:
;apps/oneaddr_basic.c:916: }
	pop	ix
	ret
;apps/oneaddr_basic.c:918: static void skip_to_eol(unsigned int *pos) {
;	---------------------------------
; Function skip_to_eol
; ---------------------------------
_skip_to_eol:
	push	ix
	ld	ix,#0
	add	ix,sp
;apps/oneaddr_basic.c:921: while (*pos < run_len) {
00103$:
	ld	c, 4 (ix)
	ld	b, 5 (ix)
	ld	l, c
	ld	h, b
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	ld	hl, #_run_len
	ld	a, e
	sub	a, (hl)
	ld	a, d
	inc	hl
	sbc	a, (hl)
	jr	NC, 00106$
;apps/oneaddr_basic.c:922: ch = source_at(*pos);
	push	bc
	push	de
	call	_source_at
	pop	af
	ld	a, l
	pop	bc
;apps/oneaddr_basic.c:923: if (ch == '\n') {
	sub	a, #0x0a
	jr	Z, 00106$
;apps/oneaddr_basic.c:924: return;
	jr	00102$
00102$:
;apps/oneaddr_basic.c:926: ++(*pos);
	ld	l, c
	ld	h, b
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	inc	de
	ld	a, e
	ld	(bc), a
	inc	bc
	ld	a, d
	ld	(bc), a
	jr	00103$
00106$:
;apps/oneaddr_basic.c:928: }
	pop	ix
	ret
;apps/oneaddr_basic.c:930: static void skip_past_eol(unsigned int *pos) {
;	---------------------------------
; Function skip_past_eol
; ---------------------------------
_skip_past_eol:
	push	ix
	ld	ix,#0
	add	ix,sp
;apps/oneaddr_basic.c:931: skip_to_eol(pos);
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_skip_to_eol
	pop	af
;apps/oneaddr_basic.c:932: if (source_at(*pos) == '\n') {
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	dec	hl
	push	hl
	push	bc
	call	_source_at
	pop	af
	ld	a, l
	pop	hl
	sub	a, #0x0a
	jr	NZ, 00103$
;apps/oneaddr_basic.c:933: ++(*pos);
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	dec	hl
	inc	bc
	ld	a, b
	ld	(hl), c
	inc	hl
	ld	(hl), a
00103$:
;apps/oneaddr_basic.c:935: }
	pop	ix
	ret
;apps/oneaddr_basic.c:937: static unsigned char keyword_at(unsigned int pos,
;	---------------------------------
; Function keyword_at
; ---------------------------------
_keyword_at:
	push	ix
	ld	ix,#0
	add	ix,sp
;apps/oneaddr_basic.c:943: skip_spaces(&pos);
	ld	hl, #4
	add	hl, sp
	push	hl
	call	_skip_spaces
	pop	af
;apps/oneaddr_basic.c:944: while (*keyword != 0) {
	ld	c, 6 (ix)
	ld	b, 7 (ix)
	ld	e, 4 (ix)
	ld	d, 5 (ix)
00103$:
	ld	a, (bc)
	or	a, a
	jr	Z, 00105$
;apps/oneaddr_basic.c:945: ch = upper_char(source_at(pos));
	push	bc
	push	de
	push	de
	call	_source_at
	pop	af
	ld	a, l
	push	af
	inc	sp
	call	_upper_char
	inc	sp
	pop	de
	pop	bc
;apps/oneaddr_basic.c:946: want = (unsigned char)*keyword;
	ld	a, (bc)
;apps/oneaddr_basic.c:947: if (ch != want) {
	sub	a, l
	jr	Z, 00102$
;apps/oneaddr_basic.c:948: return 0u;
	ld	l, #0x00
	jr	00108$
00102$:
;apps/oneaddr_basic.c:950: ++keyword;
	inc	bc
;apps/oneaddr_basic.c:951: ++pos;
	inc	de
	ld	4 (ix), e
	ld	5 (ix), d
	jr	00103$
00105$:
;apps/oneaddr_basic.c:953: if (is_name_char(source_at(pos)) != 0u) {
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_source_at
	pop	af
	ld	a, l
	push	af
	inc	sp
	call	_is_name_char
	inc	sp
	ld	a, l
	or	a, a
	jr	Z, 00107$
;apps/oneaddr_basic.c:954: return 0u;
	ld	l, #0x00
	jr	00108$
00107$:
;apps/oneaddr_basic.c:956: *end_pos = pos;
	ld	c, 8 (ix)
	ld	b, 9 (ix)
	ld	a, 4 (ix)
	ld	(bc), a
	inc	bc
	ld	a, 5 (ix)
	ld	(bc), a
;apps/oneaddr_basic.c:957: return 1u;
	ld	l, #0x01
00108$:
;apps/oneaddr_basic.c:958: }
	pop	ix
	ret
;apps/oneaddr_basic.c:960: static unsigned char match_keyword(unsigned int *pos, const char *keyword) {
;	---------------------------------
; Function match_keyword
; ---------------------------------
_match_keyword:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;apps/oneaddr_basic.c:963: if (keyword_at(*pos, keyword, &end_pos) == 0u) {
	ld	hl, #0
	add	hl, sp
	ex	de, hl
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	dec	hl
	push	hl
	push	de
	ld	e, 6 (ix)
	ld	d, 7 (ix)
	push	de
	push	bc
	call	_keyword_at
	pop	af
	pop	af
	pop	af
	ld	a, l
	pop	hl
;apps/oneaddr_basic.c:964: return 0u;
	or	a,a
	jr	NZ, 00102$
	ld	l,a
	jr	00103$
00102$:
;apps/oneaddr_basic.c:966: *pos = end_pos;
	ld	a, -2 (ix)
	ld	(hl), a
	inc	hl
	ld	a, -1 (ix)
	ld	(hl), a
;apps/oneaddr_basic.c:967: return 1u;
	ld	l, #0x01
00103$:
;apps/oneaddr_basic.c:968: }
	ld	sp, ix
	pop	ix
	ret
;apps/oneaddr_basic.c:970: static unsigned char match_char(unsigned int *pos, unsigned char want) {
;	---------------------------------
; Function match_char
; ---------------------------------
_match_char:
	push	ix
	ld	ix,#0
	add	ix,sp
;apps/oneaddr_basic.c:971: skip_spaces(pos);
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_skip_spaces
	pop	af
;apps/oneaddr_basic.c:972: if (source_at(*pos) != want) {
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	dec	hl
	push	hl
	push	bc
	call	_source_at
	pop	af
	ld	c, l
	pop	hl
	ld	a, 6 (ix)
	sub	a, c
	jr	Z, 00102$
;apps/oneaddr_basic.c:973: return 0u;
	ld	l, #0x00
	jr	00103$
00102$:
;apps/oneaddr_basic.c:975: ++(*pos);
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	dec	hl
	inc	bc
	ld	a, b
	ld	(hl), c
	inc	hl
	ld	(hl), a
;apps/oneaddr_basic.c:976: return 1u;
	ld	l, #0x01
00103$:
;apps/oneaddr_basic.c:977: }
	pop	ix
	ret
;apps/oneaddr_basic.c:979: static void fail_run(const char *message) {
;	---------------------------------
; Function fail_run
; ---------------------------------
_fail_run:
;apps/oneaddr_basic.c:980: if (run_error == 0u) {
	ld	a,(#_run_error + 0)
	or	a, a
	ret	NZ
;apps/oneaddr_basic.c:981: output_newline();
	call	_output_newline
;apps/oneaddr_basic.c:982: output_text("ERR ");
	ld	hl, #___str_11
	push	hl
	call	_output_text
	pop	af
;apps/oneaddr_basic.c:983: output_text(message);
	pop	bc
	pop	hl
	push	hl
	push	bc
	push	hl
	call	_output_text
	pop	af
;apps/oneaddr_basic.c:984: run_error = 1u;
;apps/oneaddr_basic.c:985: run_stop = 1u;
	ld	a,#0x01
	ld	(#_run_error),a
	ld	(#_run_stop), a
;apps/oneaddr_basic.c:987: }
	ret
___str_11:
	.ascii "ERR "
	.db 0x00
;apps/oneaddr_basic.c:989: static unsigned char parse_variable(unsigned int *pos, unsigned char *var) {
;	---------------------------------
; Function parse_variable
; ---------------------------------
_parse_variable:
	push	ix
	ld	ix,#0
	add	ix,sp
;apps/oneaddr_basic.c:992: skip_spaces(pos);
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_skip_spaces
	pop	af
;apps/oneaddr_basic.c:993: ch = upper_char(source_at(*pos));
	ld	c, 4 (ix)
	ld	b, 5 (ix)
	ld	l, c
	ld	h, b
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	push	bc
	push	de
	call	_source_at
	pop	af
	ld	a, l
	push	af
	inc	sp
	call	_upper_char
	inc	sp
	pop	bc
;apps/oneaddr_basic.c:994: if ((ch < 'A') || (ch > 'Z')) {
	ld	a, l
	sub	a, #0x41
	jr	C, 00101$
	ld	a, #0x5a
	sub	a, l
	jr	NC, 00102$
00101$:
;apps/oneaddr_basic.c:995: fail_run("VAR");
	ld	hl, #___str_12
	push	hl
	call	_fail_run
	pop	af
;apps/oneaddr_basic.c:996: return 0u;
	ld	l, #0x00
	jr	00104$
00102$:
;apps/oneaddr_basic.c:998: *var = (unsigned char)(ch - 'A');
	ld	e, 6 (ix)
	ld	d, 7 (ix)
	ld	a, l
	add	a, #0xbf
	ld	(de), a
;apps/oneaddr_basic.c:999: ++(*pos);
	ld	l, c
	ld	h, b
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	inc	de
	ld	a, e
	ld	(bc), a
	inc	bc
	ld	a, d
	ld	(bc), a
;apps/oneaddr_basic.c:1000: return 1u;
	ld	l, #0x01
00104$:
;apps/oneaddr_basic.c:1001: }
	pop	ix
	ret
___str_12:
	.ascii "VAR"
	.db 0x00
;apps/oneaddr_basic.c:1005: static int parse_number(unsigned int *pos) {
;	---------------------------------
; Function parse_number
; ---------------------------------
_parse_number:
	push	ix
	ld	ix,#0
	add	ix,sp
	ld	hl, #-6
	add	hl, sp
	ld	sp, hl
;apps/oneaddr_basic.c:1009: skip_spaces(pos);
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_skip_spaces
	pop	af
;apps/oneaddr_basic.c:1010: value = 0;
	ld	hl, #0x0000
	ex	(sp), hl
;apps/oneaddr_basic.c:1011: ch = source_at(*pos);
	ld	a, 4 (ix)
	ld	-4 (ix), a
	ld	a, 5 (ix)
	ld	-3 (ix), a
	ld	l, -4 (ix)
	ld	h, -3 (ix)
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	push	bc
	call	_source_at
	pop	af
;apps/oneaddr_basic.c:1012: if (is_digit_char(ch) == 0u) {
	ld	-2 (ix), l
	ld	a, l
	push	af
	inc	sp
	call	_is_digit_char
	ld	-1 (ix), l
	ld	a, l
	inc	sp
	or	a, a
	jr	NZ, 00103$
;apps/oneaddr_basic.c:1013: fail_run("NUMBER");
	ld	hl, #___str_13
	push	hl
	call	_fail_run
	pop	af
;apps/oneaddr_basic.c:1014: return 0;
	ld	hl, #0x0000
	jr	00106$
;apps/oneaddr_basic.c:1016: while (is_digit_char(ch) != 0u) {
00103$:
	ld	a, -2 (ix)
	push	af
	inc	sp
	call	_is_digit_char
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00105$
;apps/oneaddr_basic.c:1017: value = (int)(((int)(value << 3)) +
	ld	l, -6 (ix)
	ld	h, -5 (ix)
	add	hl, hl
	add	hl, hl
	add	hl, hl
	ex	de,hl
;apps/oneaddr_basic.c:1018: ((int)(value << 1)) +
	pop	hl
	push	hl
	add	hl, hl
	add	hl, de
;apps/oneaddr_basic.c:1019: (ch - '0'));
	ld	a, -2 (ix)
	ld	b, #0x00
	add	a, #0xd0
	ld	c, a
	ld	a, b
	adc	a, #0xff
	ld	b, a
	add	hl, bc
	ex	(sp), hl
;apps/oneaddr_basic.c:1020: ++(*pos);
	ld	l, -4 (ix)
	ld	h, -3 (ix)
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	inc	bc
	pop	de
	pop	hl
	push	hl
	push	de
	ld	(hl), c
	inc	hl
	ld	(hl), b
;apps/oneaddr_basic.c:1021: ch = source_at(*pos);
	push	bc
	call	_source_at
	pop	af
	ld	-2 (ix), l
	jr	00103$
00105$:
;apps/oneaddr_basic.c:1023: return value;
	ld	l, -6 (ix)
	ld	h, -5 (ix)
00106$:
;apps/oneaddr_basic.c:1024: }
	ld	sp, ix
	pop	ix
	ret
___str_13:
	.ascii "NUMBER"
	.db 0x00
;apps/oneaddr_basic.c:1026: static int parse_factor(unsigned int *pos) {
;	---------------------------------
; Function parse_factor
; ---------------------------------
_parse_factor:
	push	ix
	ld	ix,#0
	add	ix,sp
	dec	sp
;apps/oneaddr_basic.c:1030: skip_spaces(pos);
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_skip_spaces
;apps/oneaddr_basic.c:1031: if (match_char(pos, '+') != 0u) {
	ld	h,#0x2b
	ex	(sp),hl
	inc	sp
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_match_char
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00102$
;apps/oneaddr_basic.c:1032: return parse_factor(pos);
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_parse_factor
	pop	af
	jp	00115$
00102$:
;apps/oneaddr_basic.c:1034: if (match_char(pos, '-') != 0u) {
	ld	a, #0x2d
	push	af
	inc	sp
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_match_char
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00104$
;apps/oneaddr_basic.c:1035: return (int)(0 - parse_factor(pos));
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_parse_factor
	pop	af
	ex	de, hl
	ld	hl, #0x0000
	cp	a, a
	sbc	hl, de
	jp	00115$
00104$:
;apps/oneaddr_basic.c:1037: if (match_char(pos, '(') != 0u) {
	ld	a, #0x28
	push	af
	inc	sp
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_match_char
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00108$
;apps/oneaddr_basic.c:1038: value = parse_expr(pos);
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_parse_expr
;apps/oneaddr_basic.c:1039: if (match_char(pos, ')') == 0u) {
	ex	(sp),hl
	ld	a, #0x29
	push	af
	inc	sp
	ld	e, 4 (ix)
	ld	d, 5 (ix)
	push	de
	call	_match_char
	pop	af
	ld	a, l
	inc	sp
	pop	hl
	or	a, a
	jr	NZ, 00115$
;apps/oneaddr_basic.c:1040: fail_run(")");
	ld	bc, #___str_14+0
	push	hl
	push	bc
	call	_fail_run
	pop	af
	pop	hl
;apps/oneaddr_basic.c:1042: return value;
	jr	00115$
00108$:
;apps/oneaddr_basic.c:1044: if (match_keyword(pos, "BTN") != 0u) {
	ld	hl, #___str_15
	push	hl
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	Z, 00110$
;apps/oneaddr_basic.c:1045: return (int)read_buttons_value();
	call	_read_buttons_value
	ld	h, #0x00
	jr	00115$
00110$:
;apps/oneaddr_basic.c:1047: if (is_digit_char(source_at(*pos)) != 0u) {
	ld	c, 4 (ix)
	ld	b, 5 (ix)
	ld	l, c
	ld	h, b
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	push	bc
	push	de
	call	_source_at
	pop	af
	ld	a, l
	push	af
	inc	sp
	call	_is_digit_char
	ld	a, l
	inc	sp
	pop	bc
	or	a, a
	jr	Z, 00112$
;apps/oneaddr_basic.c:1048: return parse_number(pos);
	push	bc
	call	_parse_number
	pop	af
	jr	00115$
00112$:
;apps/oneaddr_basic.c:1050: if (parse_variable(pos, &var) == 0u) {
	ld	hl, #0
	add	hl, sp
	push	hl
	push	bc
	call	_parse_variable
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	NZ, 00114$
;apps/oneaddr_basic.c:1051: return 0;
	ld	hl, #0x0000
	jr	00115$
00114$:
;apps/oneaddr_basic.c:1053: return variables[var];
	ld	l, -1 (ix)
	ld	h, #0x00
	add	hl, hl
	ld	de, #_variables
	add	hl, de
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	ex	de, hl
00115$:
;apps/oneaddr_basic.c:1054: }
	inc	sp
	pop	ix
	ret
___str_14:
	.ascii ")"
	.db 0x00
___str_15:
	.ascii "BTN"
	.db 0x00
;apps/oneaddr_basic.c:1056: static int parse_term(unsigned int *pos) {
;	---------------------------------
; Function parse_term
; ---------------------------------
_parse_term:
	push	ix
	ld	ix,#0
	add	ix,sp
	dec	sp
;apps/oneaddr_basic.c:1061: value = parse_factor(pos);
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_parse_factor
	pop	af
	ex	de, hl
;apps/oneaddr_basic.c:1062: while (run_error == 0u) {
00109$:
	ld	a,(#_run_error + 0)
	or	a, a
	jr	NZ, 00111$
;apps/oneaddr_basic.c:1063: skip_spaces(pos);
	push	de
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_skip_spaces
	pop	af
	pop	de
;apps/oneaddr_basic.c:1064: op = source_at(*pos);
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	dec	hl
	push	hl
	push	de
	push	bc
	call	_source_at
	pop	af
	ld	a, l
	pop	de
	pop	hl
;apps/oneaddr_basic.c:1065: if ((op != '*') && (op != '/')) {
	ld	c, a
	sub	a, #0x2a
	ld	a, #0x01
	jr	Z, 00140$
	xor	a, a
00140$:
	ld	-1 (ix), a
	bit	0, -1 (ix)
	jr	NZ, 00102$
	ld	a, c
	sub	a, #0x2f
	jr	NZ, 00111$
;apps/oneaddr_basic.c:1066: break;
00102$:
;apps/oneaddr_basic.c:1068: ++(*pos);
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	dec	hl
	inc	bc
	ld	a, b
	ld	(hl), c
	inc	hl
	ld	(hl), a
	dec	hl
;apps/oneaddr_basic.c:1069: rhs = parse_factor(pos);
	push	de
	push	hl
	call	_parse_factor
	pop	af
	pop	de
;apps/oneaddr_basic.c:1070: if (op == '*') {
	ld	a, -1 (ix)
	or	a, a
	jr	Z, 00107$
;apps/oneaddr_basic.c:1071: value = math_mul(value, rhs);
	push	hl
	push	de
	call	_math_mul
	pop	af
	pop	af
	ex	de, hl
	jr	00109$
00107$:
;apps/oneaddr_basic.c:1073: if (rhs == 0) {
	ld	a, h
	or	a, l
	jr	NZ, 00105$
;apps/oneaddr_basic.c:1074: fail_run("DIV0");
	ld	hl, #___str_16
	push	hl
	call	_fail_run
	pop	af
;apps/oneaddr_basic.c:1075: return 0;
	ld	hl, #0x0000
	jr	00112$
00105$:
;apps/oneaddr_basic.c:1077: value = math_div(value, rhs);
	push	hl
	push	de
	call	_math_div
	pop	af
	pop	af
	ex	de, hl
	jr	00109$
00111$:
;apps/oneaddr_basic.c:1080: return value;
	ex	de, hl
00112$:
;apps/oneaddr_basic.c:1081: }
	inc	sp
	pop	ix
	ret
___str_16:
	.ascii "DIV0"
	.db 0x00
;apps/oneaddr_basic.c:1083: static int parse_sum(unsigned int *pos) {
;	---------------------------------
; Function parse_sum
; ---------------------------------
_parse_sum:
	push	ix
	ld	ix,#0
	add	ix,sp
	dec	sp
;apps/oneaddr_basic.c:1088: value = parse_term(pos);
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_parse_term
	pop	af
	ld	c, l
	ld	b, h
;apps/oneaddr_basic.c:1089: while (run_error == 0u) {
00107$:
	ld	a,(#_run_error + 0)
	or	a, a
	jr	NZ, 00109$
;apps/oneaddr_basic.c:1090: skip_spaces(pos);
	push	bc
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_skip_spaces
	pop	af
	pop	bc
;apps/oneaddr_basic.c:1091: op = source_at(*pos);
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	dec	hl
	push	hl
	push	bc
	push	de
	call	_source_at
	pop	af
	ld	a, l
	pop	bc
	pop	hl
;apps/oneaddr_basic.c:1092: if ((op != '+') && (op != '-')) {
	ld	e, a
	sub	a, #0x2b
	ld	a, #0x01
	jr	Z, 00133$
	xor	a, a
00133$:
	ld	-1 (ix), a
	bit	0, -1 (ix)
	jr	NZ, 00102$
	ld	a, e
	sub	a, #0x2d
	jr	NZ, 00109$
;apps/oneaddr_basic.c:1093: break;
00102$:
;apps/oneaddr_basic.c:1095: ++(*pos);
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	dec	hl
	inc	de
	ld	a, d
	ld	(hl), e
	inc	hl
	ld	(hl), a
	dec	hl
;apps/oneaddr_basic.c:1096: rhs = parse_term(pos);
	push	bc
	push	hl
	call	_parse_term
	pop	af
	pop	bc
;apps/oneaddr_basic.c:1097: if (op == '+') {
	ld	a, -1 (ix)
	or	a, a
	jr	Z, 00105$
;apps/oneaddr_basic.c:1098: value = (int)(value + rhs);
	add	hl, bc
	ld	c, l
	ld	b, h
	jr	00107$
00105$:
;apps/oneaddr_basic.c:1100: value = (int)(value - rhs);
	ld	a, c
	sub	a, l
	ld	c, a
	ld	a, b
	sbc	a, h
	ld	b, a
	jr	00107$
00109$:
;apps/oneaddr_basic.c:1103: return value;
	ld	l, c
	ld	h, b
;apps/oneaddr_basic.c:1104: }
	inc	sp
	pop	ix
	ret
;apps/oneaddr_basic.c:1106: static int parse_expr(unsigned int *pos) {
;	---------------------------------
; Function parse_expr
; ---------------------------------
_parse_expr:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
	push	af
;apps/oneaddr_basic.c:1111: left = parse_sum(pos);
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_parse_sum
	pop	af
	ld	c, l
	ld	b, h
;apps/oneaddr_basic.c:1112: if (run_error != 0u) {
	ld	a,(#_run_error + 0)
	or	a, a
	jr	Z, 00102$
;apps/oneaddr_basic.c:1113: return 0;
	ld	hl, #0x0000
	jp	00117$
00102$:
;apps/oneaddr_basic.c:1116: skip_spaces(pos);
	push	bc
	ld	l, 4 (ix)
	ld	h, 5 (ix)
	push	hl
	call	_skip_spaces
	pop	af
	pop	bc
;apps/oneaddr_basic.c:1117: op = source_at(*pos);
	ld	a, 4 (ix)
	ld	-4 (ix), a
	ld	a, 5 (ix)
	ld	-3 (ix), a
	pop	hl
	push	hl
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	push	bc
	push	de
	call	_source_at
	pop	af
	pop	bc
;apps/oneaddr_basic.c:1118: if ((op != '=') && (op != '<') && (op != '>')) {
	ld	a, l
	sub	a, #0x3c
	ld	a, #0x01
	jr	Z, 00207$
	xor	a, a
00207$:
	ld	-2 (ix), a
	ld	a, l
	sub	a, #0x3e
	ld	a, #0x01
	jr	Z, 00209$
	xor	a, a
00209$:
	ld	-1 (ix), a
	ld	a, l
	sub	a, #0x3d
	jr	Z, 00104$
	bit	0, -2 (ix)
	jr	NZ, 00104$
	bit	0, -1 (ix)
	jr	NZ, 00104$
;apps/oneaddr_basic.c:1119: return left;
	ld	l, c
	ld	h, b
	jp	00117$
00104$:
;apps/oneaddr_basic.c:1122: ++(*pos);
	pop	hl
	push	hl
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	inc	de
	pop	hl
	push	hl
	ld	(hl), e
	inc	hl
	ld	(hl), d
;apps/oneaddr_basic.c:1123: if (op == '<') {
	ld	a, -2 (ix)
	or	a, a
	jp	Z, 00112$
;apps/oneaddr_basic.c:1124: if (source_at(*pos) == '=') {
	pop	hl
	push	hl
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	push	bc
	push	de
	call	_source_at
	pop	af
	ld	e, l
	pop	bc
;apps/oneaddr_basic.c:1117: op = source_at(*pos);
	pop	hl
	push	hl
	ld	a, (hl)
	ld	-2 (ix), a
	inc	hl
	ld	a, (hl)
	ld	-1 (ix), a
;apps/oneaddr_basic.c:1124: if (source_at(*pos) == '=') {
	ld	a, e
	sub	a, #0x3d
	jr	NZ, 00108$
;apps/oneaddr_basic.c:1125: ++(*pos);
	ld	e, -2 (ix)
	ld	d, -1 (ix)
	inc	de
	pop	hl
	push	hl
	ld	(hl), e
	inc	hl
	ld	(hl), d
;apps/oneaddr_basic.c:1126: right = parse_sum(pos);
	ld	e, c
	ld	d, b
	pop	hl
	push	hl
	push	de
	push	hl
	call	_parse_sum
	pop	af
	pop	bc
;apps/oneaddr_basic.c:1127: return (left <= right) ? 1 : 0;
	ld	a, l
	sub	a, c
	ld	a, h
	sbc	a, b
	jp	PO, 00213$
	xor	a, #0x80
00213$:
	jp	M, 00119$
	ld	hl, #0x0001
	jp	00117$
00119$:
	ld	hl, #0x0000
	jp	00117$
00108$:
;apps/oneaddr_basic.c:1129: if (source_at(*pos) == '>') {
	push	bc
	ld	l, -2 (ix)
	ld	h, -1 (ix)
	push	hl
	call	_source_at
	pop	af
	ld	a, l
	pop	bc
	sub	a, #0x3e
	jr	NZ, 00110$
;apps/oneaddr_basic.c:1130: ++(*pos);
	pop	hl
	push	hl
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	inc	de
	pop	hl
	push	hl
	ld	(hl), e
	inc	hl
	ld	(hl), d
;apps/oneaddr_basic.c:1131: right = parse_sum(pos);
	ld	e, c
	ld	d, b
	pop	hl
	push	hl
	push	de
	push	hl
	call	_parse_sum
	pop	af
	pop	bc
;apps/oneaddr_basic.c:1132: return (left != right) ? 1 : 0;
	cp	a, a
	sbc	hl, bc
	jr	Z, 00121$
	ld	hl, #0x0001
	jp	00117$
00121$:
	ld	hl, #0x0000
	jp	00117$
00110$:
;apps/oneaddr_basic.c:1134: right = parse_sum(pos);
	ld	e, c
	ld	d, b
	pop	hl
	push	hl
	push	de
	push	hl
	call	_parse_sum
	pop	af
	pop	bc
;apps/oneaddr_basic.c:1135: return (left < right) ? 1 : 0;
	ld	a, c
	sub	a, l
	ld	a, b
	sbc	a, h
	jp	PO, 00217$
	xor	a, #0x80
00217$:
	jp	P, 00123$
	ld	hl, #0x0001
	jp	00117$
00123$:
	ld	hl, #0x0000
	jr	00117$
00112$:
;apps/oneaddr_basic.c:1137: if (op == '>') {
	ld	a, -1 (ix)
	or	a, a
	jr	Z, 00116$
;apps/oneaddr_basic.c:1138: if (source_at(*pos) == '=') {
	pop	hl
	push	hl
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	push	bc
	push	de
	call	_source_at
	pop	af
	ld	a, l
	pop	bc
	sub	a, #0x3d
	jr	NZ, 00114$
;apps/oneaddr_basic.c:1139: ++(*pos);
	pop	hl
	push	hl
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	inc	de
	pop	hl
	push	hl
	ld	(hl), e
	inc	hl
	ld	(hl), d
;apps/oneaddr_basic.c:1140: right = parse_sum(pos);
	ld	e, c
	ld	d, b
	pop	hl
	push	hl
	push	de
	push	hl
	call	_parse_sum
	pop	af
	pop	bc
;apps/oneaddr_basic.c:1141: return (left >= right) ? 1 : 0;
	ld	a, c
	sub	a, l
	ld	a, b
	sbc	a, h
	jp	PO, 00220$
	xor	a, #0x80
00220$:
	jp	M, 00125$
	ld	hl, #0x0001
	jr	00117$
00125$:
	ld	hl, #0x0000
	jr	00117$
00114$:
;apps/oneaddr_basic.c:1143: right = parse_sum(pos);
	ld	e, c
	ld	d, b
	pop	hl
	push	hl
	push	de
	push	hl
	call	_parse_sum
	pop	af
	pop	bc
;apps/oneaddr_basic.c:1144: return (left > right) ? 1 : 0;
	ld	a, l
	sub	a, c
	ld	a, h
	sbc	a, b
	jp	PO, 00221$
	xor	a, #0x80
00221$:
	jp	P, 00127$
	ld	hl, #0x0001
	jr	00117$
00127$:
	ld	hl, #0x0000
	jr	00117$
00116$:
;apps/oneaddr_basic.c:1147: right = parse_sum(pos);
	ld	e, c
	ld	d, b
	pop	hl
	push	hl
	push	de
	push	hl
	call	_parse_sum
	pop	af
	pop	bc
;apps/oneaddr_basic.c:1148: return (left == right) ? 1 : 0;
	cp	a, a
	sbc	hl, bc
	jr	NZ, 00129$
	ld	hl, #0x0001
	jr	00130$
00129$:
	ld	hl, #0x0000
00130$:
00117$:
;apps/oneaddr_basic.c:1149: }
	ld	sp, ix
	pop	ix
	ret
;apps/oneaddr_basic.c:1151: static unsigned char loop_in_range(int value, int limit, int step) {
;	---------------------------------
; Function loop_in_range
; ---------------------------------
_loop_in_range:
;apps/oneaddr_basic.c:1152: if (step > 0) {
	xor	a, a
	ld	iy, #6
	add	iy, sp
	cp	a, 0 (iy)
	sbc	a, 1 (iy)
	jp	PO, 00124$
	xor	a, #0x80
00124$:
	jp	P, 00102$
;apps/oneaddr_basic.c:1153: return (value <= limit) ? 1u : 0u;
	ld	hl, #4
	add	hl, sp
	ld	a, (hl)
	ld	iy, #2
	add	iy, sp
	sub	a, 0 (iy)
	inc	hl
	ld	a, (hl)
	sbc	a, 1 (iy)
	jp	PO, 00125$
	xor	a, #0x80
00125$:
	jp	M, 00105$
	ld	hl, #0x0001
	ret
00105$:
	ld	hl, #0x0000
	ret
00102$:
;apps/oneaddr_basic.c:1155: return (value >= limit) ? 1u : 0u;
	ld	hl, #4
	add	hl, sp
	ld	iy, #2
	add	iy, sp
	ld	a, 0 (iy)
	sub	a, (hl)
	ld	a, 1 (iy)
	inc	hl
	sbc	a, (hl)
	jp	PO, 00126$
	xor	a, #0x80
00126$:
	jp	M, 00107$
	ld	hl, #0x0001
	ret
00107$:
	ld	hl, #0x0000
;apps/oneaddr_basic.c:1156: }
	ret
;apps/oneaddr_basic.c:1158: static unsigned char find_matching_next(unsigned int start,
;	---------------------------------
; Function find_matching_next
; ---------------------------------
_find_matching_next:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
	push	af
	dec	sp
;apps/oneaddr_basic.c:1164: pos = start;
	ld	a, 4 (ix)
	ld	-5 (ix), a
	ld	a, 5 (ix)
	ld	-4 (ix), a
;apps/oneaddr_basic.c:1165: depth = 0u;
	ld	-1 (ix), #0
;apps/oneaddr_basic.c:1166: while (pos < run_len) {
00110$:
	ld	hl, #_run_len
	ld	a, -5 (ix)
	sub	a, (hl)
	ld	a, -4 (ix)
	inc	hl
	sbc	a, (hl)
	jp	NC, 00112$
;apps/oneaddr_basic.c:1167: skip_statement_gaps(&pos);
	ld	hl, #0
	add	hl, sp
	ex	de, hl
	ld	c, e
	ld	b, d
	push	de
	push	bc
	call	_skip_statement_gaps
	pop	af
	pop	de
;apps/oneaddr_basic.c:1168: if (pos >= run_len) {
	ld	hl, #_run_len
	ld	a, -5 (ix)
	sub	a, (hl)
	ld	a, -4 (ix)
	inc	hl
	sbc	a, (hl)
	jr	NC, 00112$
;apps/oneaddr_basic.c:1172: check = pos;
	ld	a, -5 (ix)
	ld	-3 (ix), a
	ld	a, -4 (ix)
	ld	-2 (ix), a
;apps/oneaddr_basic.c:1173: if (match_keyword(&check, "FOR") != 0u) {
	ld	hl, #2
	add	hl, sp
	push	de
	ld	bc, #___str_17
	push	bc
	push	hl
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	pop	de
	or	a, a
	jr	Z, 00108$
;apps/oneaddr_basic.c:1174: ++depth;
	inc	-1 (ix)
	jr	00109$
00108$:
;apps/oneaddr_basic.c:1176: check = pos;
	ld	a, -5 (ix)
	ld	-3 (ix), a
	ld	a, -4 (ix)
	ld	-2 (ix), a
;apps/oneaddr_basic.c:1177: if (match_keyword(&check, "NEXT") != 0u) {
	ld	hl, #2
	add	hl, sp
	ld	c, l
	ld	b, h
	push	hl
	push	de
	ld	hl, #___str_18
	push	hl
	push	bc
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	pop	de
	pop	hl
	or	a, a
	jr	Z, 00109$
;apps/oneaddr_basic.c:1178: if (depth == 0u) {
	ld	a, -1 (ix)
	or	a, a
	jr	NZ, 00104$
;apps/oneaddr_basic.c:1179: skip_past_eol(&check);
	push	hl
	call	_skip_past_eol
	pop	af
;apps/oneaddr_basic.c:1180: *after_next = check;
	ld	c, 6 (ix)
	ld	b, 7 (ix)
	ld	a, -3 (ix)
	ld	(bc), a
	inc	bc
	ld	a, -2 (ix)
	ld	(bc), a
;apps/oneaddr_basic.c:1181: return 1u;
	ld	l, #0x01
	jr	00113$
00104$:
;apps/oneaddr_basic.c:1183: --depth;
	dec	-1 (ix)
00109$:
;apps/oneaddr_basic.c:1186: skip_past_eol(&pos);
	push	de
	call	_skip_past_eol
	pop	af
	jp	00110$
00112$:
;apps/oneaddr_basic.c:1188: return 0u;
	ld	l, #0x00
00113$:
;apps/oneaddr_basic.c:1189: }
	ld	sp, ix
	pop	ix
	ret
___str_17:
	.ascii "FOR"
	.db 0x00
___str_18:
	.ascii "NEXT"
	.db 0x00
;apps/oneaddr_basic.c:1193: static void exec_print(void) {
;	---------------------------------
; Function exec_print
; ---------------------------------
_exec_print:
;apps/oneaddr_basic.c:1197: skip_spaces(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_skip_spaces
	pop	af
;apps/oneaddr_basic.c:1198: if (source_at(run_pc) == '"') {
	ld	hl, (_run_pc)
	push	hl
	call	_source_at
	pop	af
	ld	a, l
	sub	a, #0x22
	jr	NZ, 00111$
;apps/oneaddr_basic.c:1199: ++run_pc;
	ld	hl, (_run_pc)
	inc	hl
	ld	(_run_pc), hl
;apps/oneaddr_basic.c:1200: while ((run_pc < run_len) && (source_at(run_pc) != '"') &&
00103$:
	ld	hl, #_run_len
	ld	iy, #_run_pc
	ld	a, 0 (iy)
	sub	a, (hl)
	ld	a, 1 (iy)
	inc	hl
	sbc	a, (hl)
	jr	NC, 00105$
	ld	hl, (_run_pc)
	push	hl
	call	_source_at
	pop	af
	ld	a, l
	sub	a, #0x22
	jr	Z, 00105$
;apps/oneaddr_basic.c:1201: (source_at(run_pc) != '\n')) {
	ld	hl, (_run_pc)
	push	hl
	call	_source_at
	pop	af
	ld	a, l
	sub	a, #0x0a
	jr	Z, 00105$
;apps/oneaddr_basic.c:1202: ch = source_at(run_pc);
	ld	hl, (_run_pc)
	push	hl
	call	_source_at
	pop	af
	ld	a, l
;apps/oneaddr_basic.c:1203: output_char(ch);
	push	af
	inc	sp
	call	_output_char
	inc	sp
;apps/oneaddr_basic.c:1204: ++run_pc;
	ld	hl, (_run_pc)
	inc	hl
	ld	(_run_pc), hl
	jr	00103$
00105$:
;apps/oneaddr_basic.c:1206: if (source_at(run_pc) != '"') {
	ld	hl, (_run_pc)
	push	hl
	call	_source_at
	pop	af
	ld	a, l
	sub	a, #0x22
	jr	Z, 00107$
;apps/oneaddr_basic.c:1207: fail_run("QUOTE");
	ld	hl, #___str_19
	push	hl
	call	_fail_run
	pop	af
;apps/oneaddr_basic.c:1208: return;
	ret
00107$:
;apps/oneaddr_basic.c:1210: ++run_pc;
	ld	hl, (_run_pc)
	inc	hl
	ld	(_run_pc), hl
	jr	00112$
00111$:
;apps/oneaddr_basic.c:1212: value = parse_expr(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_parse_expr
	pop	af
	ex	de, hl
;apps/oneaddr_basic.c:1213: if (run_error != 0u) {
	ld	a,(#_run_error + 0)
	or	a, a
;apps/oneaddr_basic.c:1214: return;
	ret	NZ
;apps/oneaddr_basic.c:1216: output_number(value);
	push	de
	call	_output_number
	pop	af
00112$:
;apps/oneaddr_basic.c:1218: output_newline();
	call	_output_newline
;apps/oneaddr_basic.c:1219: skip_to_eol(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_skip_to_eol
	pop	af
;apps/oneaddr_basic.c:1220: }
	ret
___str_19:
	.ascii "QUOTE"
	.db 0x00
;apps/oneaddr_basic.c:1222: static void exec_assignment(unsigned char var) {
;	---------------------------------
; Function exec_assignment
; ---------------------------------
_exec_assignment:
	push	ix
	ld	ix,#0
	add	ix,sp
	push	af
;apps/oneaddr_basic.c:1225: if (match_char(&run_pc, '=') == 0u) {
	ld	a, #0x3d
	push	af
	inc	sp
	ld	hl, #_run_pc
	push	hl
	call	_match_char
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	NZ, 00102$
;apps/oneaddr_basic.c:1226: fail_run("=");
	ld	hl, #___str_20
	push	hl
	call	_fail_run
	pop	af
;apps/oneaddr_basic.c:1227: return;
	jr	00105$
00102$:
;apps/oneaddr_basic.c:1229: value = parse_expr(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_parse_expr
	pop	af
	ex	(sp), hl
;apps/oneaddr_basic.c:1230: if (run_error != 0u) {
	ld	a,(#_run_error + 0)
	or	a, a
;apps/oneaddr_basic.c:1231: return;
	jr	NZ, 00105$
;apps/oneaddr_basic.c:1233: variables[var] = value;
	ld	l, 4 (ix)
	ld	h, #0x00
	add	hl, hl
	ld	de, #_variables
	add	hl, de
	ld	a, -2 (ix)
	ld	(hl), a
	inc	hl
	ld	a, -1 (ix)
	ld	(hl), a
;apps/oneaddr_basic.c:1234: skip_to_eol(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_skip_to_eol
	pop	af
00105$:
;apps/oneaddr_basic.c:1235: }
	ld	sp, ix
	pop	ix
	ret
___str_20:
	.ascii "="
	.db 0x00
;apps/oneaddr_basic.c:1237: static void exec_let(void) {
;	---------------------------------
; Function exec_let
; ---------------------------------
_exec_let:
	push	ix
	ld	ix,#0
	add	ix,sp
	dec	sp
;apps/oneaddr_basic.c:1240: if (parse_variable(&run_pc, &var) == 0u) {
	ld	hl, #0
	add	hl, sp
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_parse_variable
	pop	af
	pop	af
	ld	a, l
	or	a, a
;apps/oneaddr_basic.c:1241: return;
	jr	Z, 00103$
;apps/oneaddr_basic.c:1243: exec_assignment(var);
	ld	a, -1 (ix)
	push	af
	inc	sp
	call	_exec_assignment
	inc	sp
00103$:
;apps/oneaddr_basic.c:1244: }
	inc	sp
	pop	ix
	ret
;apps/oneaddr_basic.c:1246: static void exec_if(void) {
;	---------------------------------
; Function exec_if
; ---------------------------------
_exec_if:
;apps/oneaddr_basic.c:1249: condition = parse_expr(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_parse_expr
	pop	af
	ex	de, hl
;apps/oneaddr_basic.c:1250: if (run_error != 0u) {
	ld	a,(#_run_error + 0)
	or	a, a
;apps/oneaddr_basic.c:1251: return;
	ret	NZ
;apps/oneaddr_basic.c:1253: if (match_keyword(&run_pc, "THEN") == 0u) {
	push	de
	ld	hl, #___str_21
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	pop	de
	or	a, a
	jr	NZ, 00104$
;apps/oneaddr_basic.c:1254: fail_run("THEN");
	ld	hl, #___str_21
	push	hl
	call	_fail_run
	pop	af
;apps/oneaddr_basic.c:1255: return;
	ret
00104$:
;apps/oneaddr_basic.c:1257: if (condition != 0) {
	ld	a, d
	or	a, e
;apps/oneaddr_basic.c:1258: exec_statement();
	jp	NZ,_exec_statement
;apps/oneaddr_basic.c:1260: skip_to_eol(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_skip_to_eol
	pop	af
;apps/oneaddr_basic.c:1262: }
	ret
___str_21:
	.ascii "THEN"
	.db 0x00
;apps/oneaddr_basic.c:1264: static void exec_for(void) {
;	---------------------------------
; Function exec_for
; ---------------------------------
_exec_for:
	push	ix
	ld	ix,#0
	add	ix,sp
	ld	hl, #-9
	add	hl, sp
	ld	sp, hl
;apps/oneaddr_basic.c:1272: if (parse_variable(&run_pc, &var) == 0u) {
	ld	hl, #0
	add	hl, sp
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_parse_variable
	pop	af
	pop	af
	ld	a, l
	or	a, a
;apps/oneaddr_basic.c:1273: return;
	jp	Z,00124$
;apps/oneaddr_basic.c:1275: if (match_char(&run_pc, '=') == 0u) {
	ld	a, #0x3d
	push	af
	inc	sp
	ld	hl, #_run_pc
	push	hl
	call	_match_char
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	NZ, 00104$
;apps/oneaddr_basic.c:1276: fail_run("=");
	ld	hl, #___str_22
	push	hl
	call	_fail_run
	pop	af
;apps/oneaddr_basic.c:1277: return;
	jp	00124$
00104$:
;apps/oneaddr_basic.c:1279: start_value = parse_expr(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_parse_expr
	pop	af
	ld	-4 (ix), l
	ld	-3 (ix), h
;apps/oneaddr_basic.c:1280: if (run_error != 0u) {
	ld	a,(#_run_error + 0)
	or	a, a
;apps/oneaddr_basic.c:1281: return;
	jp	NZ,00124$
;apps/oneaddr_basic.c:1283: if (match_keyword(&run_pc, "TO") == 0u) {
	ld	hl, #___str_23
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	NZ, 00108$
;apps/oneaddr_basic.c:1284: fail_run("TO");
	ld	hl, #___str_23
	push	hl
	call	_fail_run
	pop	af
;apps/oneaddr_basic.c:1285: return;
	jp	00124$
00108$:
;apps/oneaddr_basic.c:1287: limit_value = parse_expr(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_parse_expr
	pop	af
	ld	-2 (ix), l
	ld	-1 (ix), h
;apps/oneaddr_basic.c:1288: if (run_error != 0u) {
	ld	a,(#_run_error + 0)
	or	a, a
;apps/oneaddr_basic.c:1289: return;
	jp	NZ,00124$
;apps/oneaddr_basic.c:1291: if (match_keyword(&run_pc, "STEP") != 0u) {
	ld	hl, #___str_24
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	Z, 00114$
;apps/oneaddr_basic.c:1292: step_value = parse_expr(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_parse_expr
	pop	af
	ex	de, hl
;apps/oneaddr_basic.c:1293: if (run_error != 0u) {
	ld	a,(#_run_error + 0)
	or	a, a
	jr	Z, 00115$
;apps/oneaddr_basic.c:1294: return;
	jp	00124$
00114$:
;apps/oneaddr_basic.c:1297: step_value = 1;
	ld	de, #0x0001
00115$:
;apps/oneaddr_basic.c:1299: if (step_value == 0) {
	ld	a, d
	or	a, e
	jr	NZ, 00117$
;apps/oneaddr_basic.c:1300: fail_run("STEP");
	ld	bc, #___str_24
	push	bc
	call	_fail_run
	pop	af
;apps/oneaddr_basic.c:1301: return;
	jp	00124$
00117$:
;apps/oneaddr_basic.c:1304: variables[var] = start_value;
	ld	l, -9 (ix)
	ld	h, #0x00
	add	hl, hl
	ld	bc, #_variables
	add	hl, bc
	ld	a, -4 (ix)
	ld	(hl), a
	inc	hl
	ld	a, -3 (ix)
	ld	(hl), a
;apps/oneaddr_basic.c:1305: body_pc = run_pc;
	ld	hl, (_run_pc)
	ld	-8 (ix), l
	ld	-7 (ix), h
;apps/oneaddr_basic.c:1306: skip_past_eol(&body_pc);
	ld	hl, #1
	add	hl, sp
	ld	c, l
	ld	b, h
	push	hl
	push	de
	push	bc
	call	_skip_past_eol
	pop	af
	pop	de
	pop	hl
;apps/oneaddr_basic.c:1307: skip_statement_gaps(&body_pc);
	push	de
	push	hl
	call	_skip_statement_gaps
	pop	af
	pop	de
;apps/oneaddr_basic.c:1309: if (loop_in_range(start_value, limit_value, step_value) == 0u) {
	push	de
	push	de
	ld	l, -2 (ix)
	ld	h, -1 (ix)
	push	hl
	ld	l, -4 (ix)
	ld	h, -3 (ix)
	push	hl
	call	_loop_in_range
	pop	af
	pop	af
	pop	af
	ld	a, l
	pop	de
	or	a, a
	jr	NZ, 00121$
;apps/oneaddr_basic.c:1310: if (find_matching_next(body_pc, &after_next) == 0u) {
	ld	hl, #3
	add	hl, sp
	push	hl
	ld	l, -8 (ix)
	ld	h, -7 (ix)
	push	hl
	call	_find_matching_next
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	NZ, 00119$
;apps/oneaddr_basic.c:1311: fail_run("NEXT");
	ld	hl, #___str_25
	push	hl
	call	_fail_run
	pop	af
;apps/oneaddr_basic.c:1312: return;
	jp	00124$
00119$:
;apps/oneaddr_basic.c:1314: run_pc = after_next;
	ld	l, -6 (ix)
	ld	h, -5 (ix)
	ld	(_run_pc), hl
;apps/oneaddr_basic.c:1315: return;
	jr	00124$
00121$:
;apps/oneaddr_basic.c:1318: if (loop_depth >= MAX_LOOP_STACK) {
	ld	a,(#_loop_depth + 0)
	sub	a, #0x04
	jr	C, 00123$
;apps/oneaddr_basic.c:1319: fail_run("LOOP");
	ld	hl, #___str_26
	push	hl
	call	_fail_run
	pop	af
;apps/oneaddr_basic.c:1320: return;
	jr	00124$
00123$:
;apps/oneaddr_basic.c:1322: loop_stack[loop_depth].var = var;
	ld	bc, #_loop_stack+0
	push	de
	ld	de, (_loop_depth)
	ld	d, #0x00
	ld	l, e
	ld	h, d
	add	hl, hl
	add	hl, de
	add	hl, hl
	add	hl, de
	pop	de
	add	hl, bc
	ld	a, -9 (ix)
	ld	(hl), a
;apps/oneaddr_basic.c:1323: loop_stack[loop_depth].limit = limit_value;
	push	de
	ld	de, (_loop_depth)
	ld	d, #0x00
	ld	l, e
	ld	h, d
	add	hl, hl
	add	hl, de
	add	hl, hl
	add	hl, de
	pop	de
	add	hl, bc
	inc	hl
	ld	a, -2 (ix)
	ld	(hl), a
	inc	hl
	ld	a, -1 (ix)
	ld	(hl), a
;apps/oneaddr_basic.c:1324: loop_stack[loop_depth].step = step_value;
	push	de
	ld	de, (_loop_depth)
	ld	d, #0x00
	ld	l, e
	ld	h, d
	add	hl, hl
	add	hl, de
	add	hl, hl
	add	hl, de
	pop	de
	add	hl, bc
	inc	hl
	inc	hl
	inc	hl
	ld	(hl), e
	inc	hl
	ld	(hl), d
;apps/oneaddr_basic.c:1325: loop_stack[loop_depth].body_pc = body_pc;
	ld	de, (_loop_depth)
	ld	d, #0x00
	ld	l, e
	ld	h, d
	add	hl, hl
	add	hl, de
	add	hl, hl
	add	hl, de
	add	hl, bc
	ld	bc, #0x0005
	add	hl, bc
	ld	a, -8 (ix)
	ld	(hl), a
	inc	hl
	ld	a, -7 (ix)
	ld	(hl), a
;apps/oneaddr_basic.c:1326: ++loop_depth;
	ld	hl, #_loop_depth
	inc	(hl)
;apps/oneaddr_basic.c:1327: run_pc = body_pc;
	ld	l, -8 (ix)
	ld	h, -7 (ix)
	ld	(_run_pc), hl
00124$:
;apps/oneaddr_basic.c:1328: }
	ld	sp, ix
	pop	ix
	ret
___str_22:
	.ascii "="
	.db 0x00
___str_23:
	.ascii "TO"
	.db 0x00
___str_24:
	.ascii "STEP"
	.db 0x00
___str_25:
	.ascii "NEXT"
	.db 0x00
___str_26:
	.ascii "LOOP"
	.db 0x00
;apps/oneaddr_basic.c:1330: static void exec_next(void) {
;	---------------------------------
; Function exec_next
; ---------------------------------
_exec_next:
	push	ix
	ld	ix,#0
	add	ix,sp
	ld	hl, #-7
	add	hl, sp
	ld	sp, hl
;apps/oneaddr_basic.c:1337: if (loop_depth == 0u) {
	ld	a,(#_loop_depth + 0)
	or	a, a
	jr	NZ, 00102$
;apps/oneaddr_basic.c:1338: fail_run("FOR");
	ld	hl, #___str_27
	push	hl
	call	_fail_run
	pop	af
;apps/oneaddr_basic.c:1339: return;
	jp	00113$
00102$:
;apps/oneaddr_basic.c:1342: var = 0u;
	ld	-7 (ix), #0
;apps/oneaddr_basic.c:1343: have_var = 0u;
	ld	-1 (ix), #0
;apps/oneaddr_basic.c:1344: skip_spaces(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_skip_spaces
	pop	af
;apps/oneaddr_basic.c:1345: if (is_name_char(source_at(run_pc)) != 0u) {
	ld	hl, (_run_pc)
	push	hl
	call	_source_at
	pop	af
	ld	a, l
	push	af
	inc	sp
	call	_is_name_char
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00106$
;apps/oneaddr_basic.c:1346: if (parse_variable(&run_pc, &var) == 0u) {
	ld	hl, #0
	add	hl, sp
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_parse_variable
	pop	af
	pop	af
	ld	a, l
	or	a, a
;apps/oneaddr_basic.c:1347: return;
	jp	Z,00113$
;apps/oneaddr_basic.c:1349: have_var = 1u;
	ld	-1 (ix), #0x01
00106$:
;apps/oneaddr_basic.c:1352: frame = &loop_stack[(unsigned char)(loop_depth - 1u)];
	ld	a, (#_loop_depth + 0)
	ld	c, a
	dec	c
	ld	b, #0x00
	ld	l, c
	ld	h, b
	add	hl, hl
	add	hl, bc
	add	hl, hl
	add	hl, bc
	ld	de, #_loop_stack
	add	hl, de
	ld	-4 (ix), l
	ld	-3 (ix), h
;apps/oneaddr_basic.c:1353: if ((have_var != 0u) && (var != frame->var)) {
	ld	a, -1 (ix)
	or	a, a
	jr	Z, 00108$
	ld	l, -4 (ix)
	ld	h, -3 (ix)
	ld	a,-7 (ix)
	sub	a,(hl)
	jr	Z, 00108$
;apps/oneaddr_basic.c:1354: fail_run("VAR");
	ld	hl, #___str_28
	push	hl
	call	_fail_run
	pop	af
;apps/oneaddr_basic.c:1355: return;
	jp	00113$
00108$:
;apps/oneaddr_basic.c:1358: after_next = run_pc;
	ld	hl, (_run_pc)
	ld	-6 (ix), l
	ld	-5 (ix), h
;apps/oneaddr_basic.c:1359: skip_past_eol(&after_next);
	ld	hl, #1
	add	hl, sp
	push	hl
	call	_skip_past_eol
	pop	af
;apps/oneaddr_basic.c:1361: value = (int)(variables[frame->var] + frame->step);
	ld	bc, #_variables+0
	ld	l, -4 (ix)
	ld	h, -3 (ix)
	ld	l, (hl)
	ld	h, #0x00
	add	hl, hl
	add	hl, bc
	ld	-2 (ix), l
	ld	-1 (ix), h
	ld	e, (hl)
	inc	hl
	ld	d, (hl)
	ld	c, -4 (ix)
	ld	b, -3 (ix)
	inc	bc
	inc	bc
	inc	bc
	ld	l, c
	ld	h, b
	ld	a, (hl)
	inc	hl
	ld	h, (hl)
	ld	l, a
	add	hl, de
	ex	de, hl
;apps/oneaddr_basic.c:1362: variables[frame->var] = value;
	ld	l, -2 (ix)
	ld	h, -1 (ix)
	ld	(hl), e
	inc	hl
	ld	(hl), d
;apps/oneaddr_basic.c:1363: if (loop_in_range(value, frame->limit, frame->step) != 0u) {
	ld	l, c
	ld	h, b
	ld	c, (hl)
	inc	hl
	ld	b, (hl)
	ld	l, -4 (ix)
	ld	h, -3 (ix)
	inc	hl
	ld	a, (hl)
	inc	hl
	ld	h, (hl)
	ld	l, a
	push	bc
	push	hl
	push	de
	call	_loop_in_range
	pop	af
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	Z, 00111$
;apps/oneaddr_basic.c:1364: run_pc = frame->body_pc;
	ld	c, -4 (ix)
	ld	b, -3 (ix)
	ld	hl, #5
	add	hl, bc
	ld	a, (hl)
	ld	(_run_pc+0), a
	inc	hl
	ld	a, (hl)
	ld	(_run_pc+1), a
	jr	00113$
00111$:
;apps/oneaddr_basic.c:1366: --loop_depth;
	ld	hl, #_loop_depth
	dec	(hl)
;apps/oneaddr_basic.c:1367: run_pc = after_next;
	ld	l, -6 (ix)
	ld	h, -5 (ix)
	ld	(_run_pc), hl
00113$:
;apps/oneaddr_basic.c:1369: }
	ld	sp, ix
	pop	ix
	ret
___str_27:
	.ascii "FOR"
	.db 0x00
___str_28:
	.ascii "VAR"
	.db 0x00
;apps/oneaddr_basic.c:1371: static void exec_input(void) {
;	---------------------------------
; Function exec_input
; ---------------------------------
_exec_input:
	push	ix
	ld	ix,#0
	add	ix,sp
	dec	sp
;apps/oneaddr_basic.c:1374: if (parse_variable(&run_pc, &var) == 0u) {
	ld	hl, #0
	add	hl, sp
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_parse_variable
	pop	af
	pop	af
	ld	a, l
	or	a, a
;apps/oneaddr_basic.c:1375: return;
	jr	Z, 00103$
;apps/oneaddr_basic.c:1377: variables[var] = (int)read_buttons_value();
	ld	l, -1 (ix)
	ld	h, #0x00
	add	hl, hl
	ld	de, #_variables
	add	hl, de
	push	hl
	call	_read_buttons_value
	ld	a, l
	pop	hl
	ld	c, #0x00
	ld	(hl), a
	inc	hl
	ld	(hl), c
;apps/oneaddr_basic.c:1378: skip_to_eol(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_skip_to_eol
	pop	af
00103$:
;apps/oneaddr_basic.c:1379: }
	inc	sp
	pop	ix
	ret
;apps/oneaddr_basic.c:1381: static void exec_wait(void) {
;	---------------------------------
; Function exec_wait
; ---------------------------------
_exec_wait:
;apps/oneaddr_basic.c:1384: count = parse_expr(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_parse_expr
	pop	af
	ex	de, hl
;apps/oneaddr_basic.c:1385: if (run_error != 0u) {
	ld	a,(#_run_error + 0)
	or	a, a
;apps/oneaddr_basic.c:1386: return;
	ret	NZ
;apps/oneaddr_basic.c:1388: if (count < 0) {
	bit	7, d
	jr	Z, 00118$
;apps/oneaddr_basic.c:1389: count = 0;
	ld	de, #0x0000
;apps/oneaddr_basic.c:1391: while ((count != 0) && (run_stop == 0u)) {
00118$:
00108$:
	ld	a, d
	or	a, e
	jr	Z, 00110$
	ld	a,(#_run_stop + 0)
	or	a, a
	jr	NZ, 00110$
;apps/oneaddr_basic.c:1392: if (glic_button_pressed(glic_read_buttons(), GLIC_BTN_C) != 0u) {
	push	de
	call	_glic_read_buttons
	ld	a, l
	ld	h, #0x20
	push	hl
	inc	sp
	push	af
	inc	sp
	call	_glic_button_pressed
	pop	af
	ld	a, l
	pop	de
	or	a, a
	jr	Z, 00106$
;apps/oneaddr_basic.c:1393: run_break = 1u;
;apps/oneaddr_basic.c:1394: run_stop = 1u;
	ld	a,#0x01
	ld	(#_run_break),a
	ld	(#_run_stop), a
;apps/oneaddr_basic.c:1395: return;
	ret
00106$:
;apps/oneaddr_basic.c:1397: glic_delay(400u);
	push	de
	ld	hl, #0x0190
	push	hl
	call	_glic_delay
	pop	af
	pop	de
;apps/oneaddr_basic.c:1398: --count;
	dec	de
	jr	00108$
00110$:
;apps/oneaddr_basic.c:1400: skip_to_eol(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_skip_to_eol
	pop	af
;apps/oneaddr_basic.c:1401: }
	ret
;apps/oneaddr_basic.c:1403: static void exec_statement(void) {
;	---------------------------------
; Function exec_statement
; ---------------------------------
_exec_statement:
	push	ix
	ld	ix,#0
	add	ix,sp
	dec	sp
;apps/oneaddr_basic.c:1406: skip_spaces(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_skip_spaces
	pop	af
;apps/oneaddr_basic.c:1407: if ((run_pc >= run_len) || (source_at(run_pc) == '\n')) {
	ld	hl, #_run_len
	ld	iy, #_run_pc
	ld	a, 0 (iy)
	sub	a, (hl)
	ld	a, 1 (iy)
	inc	hl
	sbc	a, (hl)
	jp	NC,00136$
	ld	hl, (_run_pc)
	push	hl
	call	_source_at
	pop	af
	ld	a, l
	sub	a, #0x0a
;apps/oneaddr_basic.c:1408: return;
	jp	Z,00136$
;apps/oneaddr_basic.c:1411: if (match_keyword(&run_pc, "REM") != 0u) {
	ld	hl, #___str_29
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	Z, 00134$
;apps/oneaddr_basic.c:1412: skip_to_eol(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_skip_to_eol
	pop	af
	jp	00136$
00134$:
;apps/oneaddr_basic.c:1413: } else if (match_keyword(&run_pc, "PRINT") != 0u) {
	ld	hl, #___str_30
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	Z, 00131$
;apps/oneaddr_basic.c:1414: exec_print();
	call	_exec_print
	jp	00136$
00131$:
;apps/oneaddr_basic.c:1415: } else if (match_keyword(&run_pc, "LET") != 0u) {
	ld	hl, #___str_31
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	Z, 00128$
;apps/oneaddr_basic.c:1416: exec_let();
	call	_exec_let
	jp	00136$
00128$:
;apps/oneaddr_basic.c:1417: } else if (match_keyword(&run_pc, "IF") != 0u) {
	ld	hl, #___str_32
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	Z, 00125$
;apps/oneaddr_basic.c:1418: exec_if();
	call	_exec_if
	jp	00136$
00125$:
;apps/oneaddr_basic.c:1419: } else if (match_keyword(&run_pc, "FOR") != 0u) {
	ld	hl, #___str_33
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	Z, 00122$
;apps/oneaddr_basic.c:1420: exec_for();
	call	_exec_for
	jp	00136$
00122$:
;apps/oneaddr_basic.c:1421: } else if (match_keyword(&run_pc, "NEXT") != 0u) {
	ld	hl, #___str_34
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	Z, 00119$
;apps/oneaddr_basic.c:1422: exec_next();
	call	_exec_next
	jp	00136$
00119$:
;apps/oneaddr_basic.c:1423: } else if (match_keyword(&run_pc, "INPUT") != 0u) {
	ld	hl, #___str_35
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	Z, 00116$
;apps/oneaddr_basic.c:1424: exec_input();
	call	_exec_input
	jr	00136$
00116$:
;apps/oneaddr_basic.c:1425: } else if (match_keyword(&run_pc, "WAIT") != 0u) {
	ld	hl, #___str_36
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	Z, 00113$
;apps/oneaddr_basic.c:1426: exec_wait();
	call	_exec_wait
	jr	00136$
00113$:
;apps/oneaddr_basic.c:1427: } else if (match_keyword(&run_pc, "CLS") != 0u) {
	ld	hl, #___str_37
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	Z, 00110$
;apps/oneaddr_basic.c:1428: output_clear();
	call	_output_clear
;apps/oneaddr_basic.c:1429: skip_to_eol(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_skip_to_eol
	pop	af
	jr	00136$
00110$:
;apps/oneaddr_basic.c:1430: } else if (match_keyword(&run_pc, "END") != 0u) {
	ld	hl, #___str_38
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_match_keyword
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	Z, 00107$
;apps/oneaddr_basic.c:1431: run_done = 1u;
	ld	hl, #_run_done
	ld	(hl), #0x01
;apps/oneaddr_basic.c:1432: run_stop = 1u;
	ld	hl, #_run_stop
	ld	(hl), #0x01
;apps/oneaddr_basic.c:1433: skip_to_eol(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_skip_to_eol
	pop	af
	jr	00136$
00107$:
;apps/oneaddr_basic.c:1434: } else if (parse_variable(&run_pc, &var) != 0u) {
	ld	hl, #0
	add	hl, sp
	push	hl
	ld	hl, #_run_pc
	push	hl
	call	_parse_variable
	pop	af
	pop	af
	ld	a, l
	or	a, a
	jr	Z, 00136$
;apps/oneaddr_basic.c:1435: exec_assignment(var);
	ld	a, -1 (ix)
	push	af
	inc	sp
	call	_exec_assignment
	inc	sp
00136$:
;apps/oneaddr_basic.c:1437: }
	inc	sp
	pop	ix
	ret
___str_29:
	.ascii "REM"
	.db 0x00
___str_30:
	.ascii "PRINT"
	.db 0x00
___str_31:
	.ascii "LET"
	.db 0x00
___str_32:
	.ascii "IF"
	.db 0x00
___str_33:
	.ascii "FOR"
	.db 0x00
___str_34:
	.ascii "NEXT"
	.db 0x00
___str_35:
	.ascii "INPUT"
	.db 0x00
___str_36:
	.ascii "WAIT"
	.db 0x00
___str_37:
	.ascii "CLS"
	.db 0x00
___str_38:
	.ascii "END"
	.db 0x00
;apps/oneaddr_basic.c:1439: static void poll_run_stop(void) {
;	---------------------------------
; Function poll_run_stop
; ---------------------------------
_poll_run_stop:
;apps/oneaddr_basic.c:1440: if ((run_stop == 0u) &&
	ld	a,(#_run_stop + 0)
	or	a, a
	ret	NZ
;apps/oneaddr_basic.c:1441: (glic_button_pressed(glic_read_buttons(), GLIC_BTN_C) != 0u)) {
	call	_glic_read_buttons
	ld	a, l
	ld	h, #0x20
	push	hl
	inc	sp
	push	af
	inc	sp
	call	_glic_button_pressed
	pop	af
	ld	a, l
	or	a, a
	ret	Z
;apps/oneaddr_basic.c:1442: run_break = 1u;
;apps/oneaddr_basic.c:1443: run_stop = 1u;
	ld	a,#0x01
	ld	(#_run_break),a
	ld	(#_run_stop), a
;apps/oneaddr_basic.c:1445: }
	ret
;apps/oneaddr_basic.c:1447: static void run_program(void) {
;	---------------------------------
; Function run_program
; ---------------------------------
_run_program:
;apps/oneaddr_basic.c:1452: wait_for_release();
	call	_wait_for_release
;apps/oneaddr_basic.c:1453: run_len = program_len();
	call	_program_len
	ld	(_run_len), hl
;apps/oneaddr_basic.c:1454: run_pc = 0u;
	ld	hl, #0x0000
	ld	(_run_pc), hl
;apps/oneaddr_basic.c:1455: run_error = 0u;
	ld	hl, #_run_error
	ld	(hl), #0x00
;apps/oneaddr_basic.c:1456: run_stop = 0u;
	ld	hl, #_run_stop
	ld	(hl), #0x00
;apps/oneaddr_basic.c:1457: run_break = 0u;
	ld	hl, #_run_break
	ld	(hl), #0x00
;apps/oneaddr_basic.c:1458: run_done = 0u;
	ld	hl, #_run_done
	ld	(hl), #0x00
;apps/oneaddr_basic.c:1459: loop_depth = 0u;
	ld	hl, #_loop_depth
	ld	(hl), #0x00
;apps/oneaddr_basic.c:1461: for (i = 0u; i < 26u; ++i) {
	ld	bc, #_variables+0
	ld	e, #0x00
00125$:
;apps/oneaddr_basic.c:1462: variables[i] = 0;
	ld	l, e
	ld	h, #0x00
	add	hl, hl
	add	hl, bc
	xor	a, a
	ld	(hl), a
	inc	hl
	ld	(hl), a
;apps/oneaddr_basic.c:1461: for (i = 0u; i < 26u; ++i) {
	inc	e
	ld	a, e
	sub	a, #0x1a
	jr	C, 00125$
;apps/oneaddr_basic.c:1465: output_clear();
	call	_output_clear
;apps/oneaddr_basic.c:1466: if (run_len == 0u) {
	ld	iy, #_run_len
	ld	a, 1 (iy)
	or	a, 0 (iy)
	jr	NZ, 00109$
;apps/oneaddr_basic.c:1467: output_text("EMPTY");
	ld	hl, #___str_39
	push	hl
	call	_output_text
	pop	af
;apps/oneaddr_basic.c:1468: output_newline();
	call	_output_newline
;apps/oneaddr_basic.c:1469: run_done = 1u;
	ld	hl, #_run_done
	ld	(hl), #0x01
;apps/oneaddr_basic.c:1472: while ((run_stop == 0u) && (run_done == 0u)) {
00109$:
	ld	a,(#_run_stop + 0)
	or	a, a
	jr	NZ, 00111$
	ld	a,(#_run_done + 0)
	or	a, a
	jr	NZ, 00111$
;apps/oneaddr_basic.c:1473: skip_statement_gaps(&run_pc);
	ld	hl, #_run_pc
	push	hl
	call	_skip_statement_gaps
	pop	af
;apps/oneaddr_basic.c:1474: if (run_pc >= run_len) {
	ld	hl, #_run_len
	ld	iy, #_run_pc
	ld	a, 0 (iy)
	sub	a, (hl)
	ld	a, 1 (iy)
	inc	hl
	sbc	a, (hl)
	jr	C, 00105$
;apps/oneaddr_basic.c:1475: run_done = 1u;
	ld	hl, #_run_done
	ld	(hl), #0x01
;apps/oneaddr_basic.c:1476: break;
	jr	00111$
00105$:
;apps/oneaddr_basic.c:1478: exec_statement();
	call	_exec_statement
;apps/oneaddr_basic.c:1479: if (run_error != 0u) {
	ld	a,(#_run_error + 0)
	or	a, a
	jr	NZ, 00111$
;apps/oneaddr_basic.c:1482: poll_run_stop();
	call	_poll_run_stop
	jr	00109$
00111$:
;apps/oneaddr_basic.c:1485: if (run_error == 0u) {
	ld	a,(#_run_error + 0)
	or	a, a
	jr	NZ, 00116$
;apps/oneaddr_basic.c:1486: output_newline();
	call	_output_newline
;apps/oneaddr_basic.c:1487: if (run_break != 0u) {
	ld	a,(#_run_break + 0)
	or	a, a
	jr	Z, 00113$
;apps/oneaddr_basic.c:1488: output_text("STOP");
	ld	hl, #___str_40
	push	hl
	call	_output_text
	pop	af
	jr	00116$
00113$:
;apps/oneaddr_basic.c:1490: output_text("DONE");
	ld	hl, #___str_41
	push	hl
	call	_output_text
	pop	af
00116$:
;apps/oneaddr_basic.c:1494: text_clear_row(15u);
	ld	a, #0x0f
	push	af
	inc	sp
	call	_text_clear_row
	inc	sp
;apps/oneaddr_basic.c:1495: text_write(15u, 0u, "C MENU CTR EXIT");
	ld	hl, #___str_42
	push	hl
	xor	a, a
	ld	d,a
	ld	e,#0x0f
	push	de
	call	_text_write
	pop	af
	pop	af
;apps/oneaddr_basic.c:1496: last_buttons = GLIC_BUTTONS_NONE;
	ld	h, #0xff
;apps/oneaddr_basic.c:1497: while (1) {
00123$:
;apps/oneaddr_basic.c:1498: buttons = glic_read_buttons();
	push	hl
	call	_glic_read_buttons
	pop	af
	ld	h, a
;apps/oneaddr_basic.c:1499: if ((button_edge(buttons, last_buttons, GLIC_BTN_C) != 0u) ||
	push	hl
	ld	a, #0x20
	push	af
	inc	sp
	push	hl
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	hl
	or	a, a
	jp	NZ,_wait_for_release
;apps/oneaddr_basic.c:1500: (button_edge(buttons, last_buttons, GLIC_BTN_CENTER) != 0u) ||
	push	hl
	ld	a, #0x01
	push	af
	inc	sp
	push	hl
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	hl
	or	a, a
	jp	NZ,_wait_for_release
;apps/oneaddr_basic.c:1501: (button_edge(buttons, last_buttons, GLIC_BTN_B) != 0u) ||
	push	hl
	ld	a, #0x40
	push	af
	inc	sp
	push	hl
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	hl
	or	a, a
	jp	NZ,_wait_for_release
;apps/oneaddr_basic.c:1502: (button_edge(buttons, last_buttons, GLIC_BTN_A) != 0u)) {
	push	hl
	ld	a, #0x80
	push	af
	inc	sp
	push	hl
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	hl
	or	a, a
;apps/oneaddr_basic.c:1503: wait_for_release();
;apps/oneaddr_basic.c:1504: return;
	jp	NZ,_wait_for_release
;apps/oneaddr_basic.c:1506: last_buttons = buttons;
	ld	h, l
;apps/oneaddr_basic.c:1507: glic_delay(LOOP_DELAY);
	push	hl
	ld	de, #0x0384
	push	de
	call	_glic_delay
	pop	af
	pop	hl
;apps/oneaddr_basic.c:1509: }
	jr	00123$
___str_39:
	.ascii "EMPTY"
	.db 0x00
___str_40:
	.ascii "STOP"
	.db 0x00
___str_41:
	.ascii "DONE"
	.db 0x00
___str_42:
	.ascii "C MENU CTR EXIT"
	.db 0x00
;apps/oneaddr_basic.c:1511: static void draw_menu(void) {
;	---------------------------------
; Function draw_menu
; ---------------------------------
_draw_menu:
;apps/oneaddr_basic.c:1514: len = program_len();
	call	_program_len
;apps/oneaddr_basic.c:1515: glic_prepare_screen(GLIC_BLACK);
	push	hl
	xor	a, a
	push	af
	inc	sp
	call	_glic_prepare_screen
	inc	sp
	pop	hl
;apps/oneaddr_basic.c:1516: text_write(0u, 1u, "ONEADDR BASIC");
	ld	bc, #___str_43+0
	push	hl
	push	bc
	ld	a, #0x01
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_write
	pop	af
	pop	af
	pop	hl
;apps/oneaddr_basic.c:1517: text_write(2u, 0u, "SRC 000/512");
	ld	bc, #___str_44+0
	push	hl
	push	bc
	xor	a, a
	ld	d,a
	ld	e,#0x02
	push	de
	call	_text_write
	pop	af
	pop	af
	ld	de, #0x0402
	push	de
	call	_text_write_u16_3
	pop	af
;apps/oneaddr_basic.c:1519: text_write(5u, 1u, "CENTER EDIT");
	ld	hl, #___str_45
	ex	(sp),hl
	ld	de, #0x0105
	push	de
	call	_text_write
	pop	af
;apps/oneaddr_basic.c:1520: text_write(7u, 1u, "A RUN");
	ld	hl, #___str_46
	ex	(sp),hl
	ld	de, #0x0107
	push	de
	call	_text_write
	pop	af
;apps/oneaddr_basic.c:1521: text_write(9u, 1u, "B LOAD DEMO");
	ld	hl, #___str_47
	ex	(sp),hl
	ld	de, #0x0109
	push	de
	call	_text_write
	pop	af
;apps/oneaddr_basic.c:1522: text_write(11u, 1u, "C INSTRUCTIONS");
	ld	hl, #___str_48
	ex	(sp),hl
	ld	de, #0x010b
	push	de
	call	_text_write
	pop	af
;apps/oneaddr_basic.c:1523: text_write(15u, 0u, "TEXT AT 0x6006");
	ld	hl, #___str_49
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x0f
	push	de
	call	_text_write
	pop	af
	pop	af
;apps/oneaddr_basic.c:1524: }
	ret
___str_43:
	.ascii "ONEADDR BASIC"
	.db 0x00
___str_44:
	.ascii "SRC 000/512"
	.db 0x00
___str_45:
	.ascii "CENTER EDIT"
	.db 0x00
___str_46:
	.ascii "A RUN"
	.db 0x00
___str_47:
	.ascii "B LOAD DEMO"
	.db 0x00
___str_48:
	.ascii "C INSTRUCTIONS"
	.db 0x00
___str_49:
	.ascii "TEXT AT 0x6006"
	.db 0x00
;apps/oneaddr_basic.c:1526: static const char *instruction_name(unsigned char index) {
;	---------------------------------
; Function instruction_name
; ---------------------------------
_instruction_name:
;apps/oneaddr_basic.c:1527: switch (index) {
	ld	a, #0x0a
	ld	iy, #2
	add	iy, sp
	sub	a, 0 (iy)
	jr	C, 00112$
	ld	c, 0 (iy)
	ld	b, #0x00
	ld	hl, #00121$
	add	hl, bc
	add	hl, bc
	add	hl, bc
	jp	(hl)
00121$:
	jp	00101$
	jp	00102$
	jp	00103$
	jp	00104$
	jp	00105$
	jp	00106$
	jp	00107$
	jp	00108$
	jp	00109$
	jp	00110$
	jp	00111$
;apps/oneaddr_basic.c:1528: case 0u:
00101$:
;apps/oneaddr_basic.c:1529: return "PRINT";
	ld	hl, #___str_50
	ret
;apps/oneaddr_basic.c:1530: case 1u:
00102$:
;apps/oneaddr_basic.c:1531: return "LET";
	ld	hl, #___str_51
	ret
;apps/oneaddr_basic.c:1532: case 2u:
00103$:
;apps/oneaddr_basic.c:1533: return "IF";
	ld	hl, #___str_52
	ret
;apps/oneaddr_basic.c:1534: case 3u:
00104$:
;apps/oneaddr_basic.c:1535: return "FOR";
	ld	hl, #___str_53
	ret
;apps/oneaddr_basic.c:1536: case 4u:
00105$:
;apps/oneaddr_basic.c:1537: return "NEXT";
	ld	hl, #___str_54
	ret
;apps/oneaddr_basic.c:1538: case 5u:
00106$:
;apps/oneaddr_basic.c:1539: return "INPUT";
	ld	hl, #___str_55
	ret
;apps/oneaddr_basic.c:1540: case 6u:
00107$:
;apps/oneaddr_basic.c:1541: return "BTN";
	ld	hl, #___str_56
	ret
;apps/oneaddr_basic.c:1542: case 7u:
00108$:
;apps/oneaddr_basic.c:1543: return "WAIT";
	ld	hl, #___str_57
	ret
;apps/oneaddr_basic.c:1544: case 8u:
00109$:
;apps/oneaddr_basic.c:1545: return "CLS";
	ld	hl, #___str_58
	ret
;apps/oneaddr_basic.c:1546: case 9u:
00110$:
;apps/oneaddr_basic.c:1547: return "REM";
	ld	hl, #___str_59
	ret
;apps/oneaddr_basic.c:1548: case 10u:
00111$:
;apps/oneaddr_basic.c:1549: return "END";
	ld	hl, #___str_60
	ret
;apps/oneaddr_basic.c:1550: default:
00112$:
;apps/oneaddr_basic.c:1551: return "EXPR";
	ld	hl, #___str_61
;apps/oneaddr_basic.c:1552: }
;apps/oneaddr_basic.c:1553: }
	ret
___str_50:
	.ascii "PRINT"
	.db 0x00
___str_51:
	.ascii "LET"
	.db 0x00
___str_52:
	.ascii "IF"
	.db 0x00
___str_53:
	.ascii "FOR"
	.db 0x00
___str_54:
	.ascii "NEXT"
	.db 0x00
___str_55:
	.ascii "INPUT"
	.db 0x00
___str_56:
	.ascii "BTN"
	.db 0x00
___str_57:
	.ascii "WAIT"
	.db 0x00
___str_58:
	.ascii "CLS"
	.db 0x00
___str_59:
	.ascii "REM"
	.db 0x00
___str_60:
	.ascii "END"
	.db 0x00
___str_61:
	.ascii "EXPR"
	.db 0x00
;apps/oneaddr_basic.c:1555: static const char *instruction_detail(unsigned char index,
;	---------------------------------
; Function instruction_detail
; ---------------------------------
_instruction_detail:
	push	ix
	ld	ix,#0
	add	ix,sp
;apps/oneaddr_basic.c:1557: switch (index) {
	ld	a, #0x0a
	sub	a, 4 (ix)
	jp	C, 00174$
	ld	c, 4 (ix)
	ld	b, #0x00
	ld	hl, #00409$
	add	hl, bc
	add	hl, bc
	add	hl, bc
	jp	(hl)
00409$:
	jp	00101$
	jp	00108$
	jp	00114$
	jp	00121$
	jp	00128$
	jp	00135$
	jp	00142$
	jp	00149$
	jp	00155$
	jp	00162$
	jp	00168$
;apps/oneaddr_basic.c:1558: case 0u:
00101$:
;apps/oneaddr_basic.c:1559: switch (line) {
	ld	a, 5 (ix)
	or	a, a
	jr	Z, 00102$
	ld	a, 5 (ix)
	dec	a
	jr	Z, 00103$
	ld	a, 5 (ix)
	sub	a, #0x02
	jr	Z, 00104$
	ld	a, 5 (ix)
	sub	a, #0x03
	jr	Z, 00105$
	jr	00106$
;apps/oneaddr_basic.c:1560: case 0u:
00102$:
;apps/oneaddr_basic.c:1561: return "PRINT X";
	ld	hl, #___str_62
	jp	00182$
;apps/oneaddr_basic.c:1562: case 1u:
00103$:
;apps/oneaddr_basic.c:1563: return "SHOWS A NUMBER";
	ld	hl, #___str_63
	jp	00182$
;apps/oneaddr_basic.c:1564: case 2u:
00104$:
;apps/oneaddr_basic.c:1565: return "PRINT \"HI\"";
	ld	hl, #___str_64
	jp	00182$
;apps/oneaddr_basic.c:1566: case 3u:
00105$:
;apps/oneaddr_basic.c:1567: return "SHOWS TEXT";
	ld	hl, #___str_65
	jp	00182$
;apps/oneaddr_basic.c:1568: default:
00106$:
;apps/oneaddr_basic.c:1569: return "";
	ld	hl, #___str_66
	jp	00182$
;apps/oneaddr_basic.c:1571: case 1u:
00108$:
;apps/oneaddr_basic.c:1572: switch (line) {
	ld	a, 5 (ix)
	or	a, a
	jr	Z, 00109$
	ld	a, 5 (ix)
	dec	a
	jr	Z, 00110$
	ld	a, 5 (ix)
	sub	a, #0x02
	jr	Z, 00111$
	jr	00112$
;apps/oneaddr_basic.c:1573: case 0u:
00109$:
;apps/oneaddr_basic.c:1574: return "LET A=1";
	ld	hl, #___str_67
	jp	00182$
;apps/oneaddr_basic.c:1575: case 1u:
00110$:
;apps/oneaddr_basic.c:1576: return "A=A+1 ALSO OK";
	ld	hl, #___str_68
	jp	00182$
;apps/oneaddr_basic.c:1577: case 2u:
00111$:
;apps/oneaddr_basic.c:1578: return "VARS ARE A-Z";
	ld	hl, #___str_69
	jp	00182$
;apps/oneaddr_basic.c:1579: default:
00112$:
;apps/oneaddr_basic.c:1580: return "";
	ld	hl, #___str_66
	jp	00182$
;apps/oneaddr_basic.c:1582: case 2u:
00114$:
;apps/oneaddr_basic.c:1583: switch (line) {
	ld	a, 5 (ix)
	or	a, a
	jr	Z, 00115$
	ld	a, 5 (ix)
	dec	a
	jr	Z, 00116$
	ld	a, 5 (ix)
	sub	a, #0x02
	jr	Z, 00117$
	ld	a, 5 (ix)
	sub	a, #0x03
	jr	Z, 00118$
	jr	00119$
;apps/oneaddr_basic.c:1584: case 0u:
00115$:
;apps/oneaddr_basic.c:1585: return "IF X THEN CMD";
	ld	hl, #___str_70
	jp	00182$
;apps/oneaddr_basic.c:1586: case 1u:
00116$:
;apps/oneaddr_basic.c:1587: return "RUNS CMD WHEN";
	ld	hl, #___str_71
	jp	00182$
;apps/oneaddr_basic.c:1588: case 2u:
00117$:
;apps/oneaddr_basic.c:1589: return "X IS NOT ZERO";
	ld	hl, #___str_72
	jp	00182$
;apps/oneaddr_basic.c:1590: case 3u:
00118$:
;apps/oneaddr_basic.c:1591: return "COMPARE = < >";
	ld	hl, #___str_73
	jp	00182$
;apps/oneaddr_basic.c:1592: default:
00119$:
;apps/oneaddr_basic.c:1593: return "";
	ld	hl, #___str_66
	jp	00182$
;apps/oneaddr_basic.c:1595: case 3u:
00121$:
;apps/oneaddr_basic.c:1596: switch (line) {
	ld	a, 5 (ix)
	or	a, a
	jr	Z, 00122$
	ld	a, 5 (ix)
	dec	a
	jr	Z, 00123$
	ld	a, 5 (ix)
	sub	a, #0x02
	jr	Z, 00124$
	ld	a, 5 (ix)
	sub	a, #0x03
	jr	Z, 00125$
	jr	00126$
;apps/oneaddr_basic.c:1597: case 0u:
00122$:
;apps/oneaddr_basic.c:1598: return "FOR I=1 TO 5";
	ld	hl, #___str_74
	jp	00182$
;apps/oneaddr_basic.c:1599: case 1u:
00123$:
;apps/oneaddr_basic.c:1600: return "REPEATS LINES";
	ld	hl, #___str_75
	jp	00182$
;apps/oneaddr_basic.c:1601: case 2u:
00124$:
;apps/oneaddr_basic.c:1602: return "UNTIL NEXT";
	ld	hl, #___str_76
	jp	00182$
;apps/oneaddr_basic.c:1603: case 3u:
00125$:
;apps/oneaddr_basic.c:1604: return "STEP OPTIONAL";
	ld	hl, #___str_77
	jp	00182$
;apps/oneaddr_basic.c:1605: default:
00126$:
;apps/oneaddr_basic.c:1606: return "";
	ld	hl, #___str_66
	jp	00182$
;apps/oneaddr_basic.c:1608: case 4u:
00128$:
;apps/oneaddr_basic.c:1609: switch (line) {
	ld	a, 5 (ix)
	or	a, a
	jr	Z, 00129$
	ld	a, 5 (ix)
	dec	a
	jr	Z, 00130$
	ld	a, 5 (ix)
	sub	a, #0x02
	jr	Z, 00131$
	ld	a, 5 (ix)
	sub	a, #0x03
	jr	Z, 00132$
	jr	00133$
;apps/oneaddr_basic.c:1610: case 0u:
00129$:
;apps/oneaddr_basic.c:1611: return "NEXT";
	ld	hl, #___str_78
	jp	00182$
;apps/oneaddr_basic.c:1612: case 1u:
00130$:
;apps/oneaddr_basic.c:1613: return "ENDS FOR LOOP";
	ld	hl, #___str_79
	jp	00182$
;apps/oneaddr_basic.c:1614: case 2u:
00131$:
;apps/oneaddr_basic.c:1615: return "NEXT I CHECKS";
	ld	hl, #___str_80
	jp	00182$
;apps/oneaddr_basic.c:1616: case 3u:
00132$:
;apps/oneaddr_basic.c:1617: return "THE VARIABLE";
	ld	hl, #___str_81
	jp	00182$
;apps/oneaddr_basic.c:1618: default:
00133$:
;apps/oneaddr_basic.c:1619: return "";
	ld	hl, #___str_66
	jp	00182$
;apps/oneaddr_basic.c:1621: case 5u:
00135$:
;apps/oneaddr_basic.c:1622: switch (line) {
	ld	a, 5 (ix)
	or	a, a
	jr	Z, 00136$
	ld	a, 5 (ix)
	dec	a
	jr	Z, 00137$
	ld	a, 5 (ix)
	sub	a, #0x02
	jr	Z, 00138$
	ld	a, 5 (ix)
	sub	a, #0x03
	jr	Z, 00139$
	jr	00140$
;apps/oneaddr_basic.c:1623: case 0u:
00136$:
;apps/oneaddr_basic.c:1624: return "INPUT A";
	ld	hl, #___str_82
	jp	00182$
;apps/oneaddr_basic.c:1625: case 1u:
00137$:
;apps/oneaddr_basic.c:1626: return "READS BUTTONS";
	ld	hl, #___str_83
	jp	00182$
;apps/oneaddr_basic.c:1627: case 2u:
00138$:
;apps/oneaddr_basic.c:1628: return "A GETS A MASK";
	ld	hl, #___str_84
	jp	00182$
;apps/oneaddr_basic.c:1629: case 3u:
00139$:
;apps/oneaddr_basic.c:1630: return "0 MEANS NONE";
	ld	hl, #___str_85
	jp	00182$
;apps/oneaddr_basic.c:1631: default:
00140$:
;apps/oneaddr_basic.c:1632: return "";
	ld	hl, #___str_66
	jp	00182$
;apps/oneaddr_basic.c:1634: case 6u:
00142$:
;apps/oneaddr_basic.c:1635: switch (line) {
	ld	a, 5 (ix)
	or	a, a
	jr	Z, 00143$
	ld	a, 5 (ix)
	dec	a
	jr	Z, 00144$
	ld	a, 5 (ix)
	sub	a, #0x02
	jr	Z, 00145$
	ld	a, 5 (ix)
	sub	a, #0x03
	jr	Z, 00146$
	jr	00147$
;apps/oneaddr_basic.c:1636: case 0u:
00143$:
;apps/oneaddr_basic.c:1637: return "BTN";
	ld	hl, #___str_86
	jp	00182$
;apps/oneaddr_basic.c:1638: case 1u:
00144$:
;apps/oneaddr_basic.c:1639: return "CURRENT BUTTON";
	ld	hl, #___str_87
	jp	00182$
;apps/oneaddr_basic.c:1640: case 2u:
00145$:
;apps/oneaddr_basic.c:1641: return "MASK IN EXPR";
	ld	hl, #___str_88
	jp	00182$
;apps/oneaddr_basic.c:1642: case 3u:
00146$:
;apps/oneaddr_basic.c:1643: return "IF BTN THEN...";
	ld	hl, #___str_89
	jp	00182$
;apps/oneaddr_basic.c:1644: default:
00147$:
;apps/oneaddr_basic.c:1645: return "";
	ld	hl, #___str_66
	jp	00182$
;apps/oneaddr_basic.c:1647: case 7u:
00149$:
;apps/oneaddr_basic.c:1648: switch (line) {
	ld	a, 5 (ix)
	or	a, a
	jr	Z, 00150$
	ld	a, 5 (ix)
	dec	a
	jr	Z, 00151$
	ld	a, 5 (ix)
	sub	a, #0x02
	jr	Z, 00152$
	jr	00153$
;apps/oneaddr_basic.c:1649: case 0u:
00150$:
;apps/oneaddr_basic.c:1650: return "WAIT 50";
	ld	hl, #___str_90
	jp	00182$
;apps/oneaddr_basic.c:1651: case 1u:
00151$:
;apps/oneaddr_basic.c:1652: return "PAUSES A BIT";
	ld	hl, #___str_91
	jp	00182$
;apps/oneaddr_basic.c:1653: case 2u:
00152$:
;apps/oneaddr_basic.c:1654: return "C CAN STOP IT";
	ld	hl, #___str_92
	jp	00182$
;apps/oneaddr_basic.c:1655: default:
00153$:
;apps/oneaddr_basic.c:1656: return "";
	ld	hl, #___str_66
	jp	00182$
;apps/oneaddr_basic.c:1658: case 8u:
00155$:
;apps/oneaddr_basic.c:1659: switch (line) {
	ld	a, 5 (ix)
	or	a, a
	jr	Z, 00156$
	ld	a, 5 (ix)
	dec	a
	jr	Z, 00157$
	ld	a, 5 (ix)
	sub	a, #0x02
	jr	Z, 00158$
	ld	a, 5 (ix)
	sub	a, #0x03
	jr	Z, 00159$
	jr	00160$
;apps/oneaddr_basic.c:1660: case 0u:
00156$:
;apps/oneaddr_basic.c:1661: return "CLS";
	ld	hl, #___str_93
	jp	00182$
;apps/oneaddr_basic.c:1662: case 1u:
00157$:
;apps/oneaddr_basic.c:1663: return "CLEARS OUTPUT";
	ld	hl, #___str_94
	jp	00182$
;apps/oneaddr_basic.c:1664: case 2u:
00158$:
;apps/oneaddr_basic.c:1665: return "PROGRAM KEEPS";
	ld	hl, #___str_95
	jp	00182$
;apps/oneaddr_basic.c:1666: case 3u:
00159$:
;apps/oneaddr_basic.c:1667: return "RUNNING";
	ld	hl, #___str_96
	jp	00182$
;apps/oneaddr_basic.c:1668: default:
00160$:
;apps/oneaddr_basic.c:1669: return "";
	ld	hl, #___str_66
	jp	00182$
;apps/oneaddr_basic.c:1671: case 9u:
00162$:
;apps/oneaddr_basic.c:1672: switch (line) {
	ld	a, 5 (ix)
	or	a, a
	jr	Z, 00163$
	ld	a, 5 (ix)
	dec	a
	jr	Z, 00164$
	ld	a, 5 (ix)
	sub	a, #0x02
	jr	Z, 00165$
	jr	00166$
;apps/oneaddr_basic.c:1673: case 0u:
00163$:
;apps/oneaddr_basic.c:1674: return "REM COMMENT";
	ld	hl, #___str_97
	jr	00182$
;apps/oneaddr_basic.c:1675: case 1u:
00164$:
;apps/oneaddr_basic.c:1676: return "IGNORED LINE";
	ld	hl, #___str_98
	jr	00182$
;apps/oneaddr_basic.c:1677: case 2u:
00165$:
;apps/oneaddr_basic.c:1678: return "USE FOR NOTES";
	ld	hl, #___str_99
	jr	00182$
;apps/oneaddr_basic.c:1679: default:
00166$:
;apps/oneaddr_basic.c:1680: return "";
	ld	hl, #___str_66
	jr	00182$
;apps/oneaddr_basic.c:1682: case 10u:
00168$:
;apps/oneaddr_basic.c:1683: switch (line) {
	ld	a, 5 (ix)
	or	a, a
	jr	Z, 00169$
	ld	a, 5 (ix)
	dec	a
	jr	Z, 00170$
	ld	a, 5 (ix)
	sub	a, #0x02
	jr	Z, 00171$
	jr	00172$
;apps/oneaddr_basic.c:1684: case 0u:
00169$:
;apps/oneaddr_basic.c:1685: return "END";
	ld	hl, #___str_100
	jr	00182$
;apps/oneaddr_basic.c:1686: case 1u:
00170$:
;apps/oneaddr_basic.c:1687: return "STOPS PROGRAM";
	ld	hl, #___str_101
	jr	00182$
;apps/oneaddr_basic.c:1688: case 2u:
00171$:
;apps/oneaddr_basic.c:1689: return "DONE IS SHOWN";
	ld	hl, #___str_102
	jr	00182$
;apps/oneaddr_basic.c:1690: default:
00172$:
;apps/oneaddr_basic.c:1691: return "";
	ld	hl, #___str_66
	jr	00182$
;apps/oneaddr_basic.c:1693: default:
00174$:
;apps/oneaddr_basic.c:1694: switch (line) {
	ld	a, 5 (ix)
	or	a, a
	jr	Z, 00175$
	ld	a, 5 (ix)
	dec	a
	jr	Z, 00176$
	ld	a, 5 (ix)
	sub	a, #0x02
	jr	Z, 00177$
	ld	a, 5 (ix)
	sub	a, #0x03
	jr	Z, 00178$
	jr	00179$
;apps/oneaddr_basic.c:1695: case 0u:
00175$:
;apps/oneaddr_basic.c:1696: return "A+2*(B-1)";
	ld	hl, #___str_103
	jr	00182$
;apps/oneaddr_basic.c:1697: case 1u:
00176$:
;apps/oneaddr_basic.c:1698: return "+ - * / ( )";
	ld	hl, #___str_104
	jr	00182$
;apps/oneaddr_basic.c:1699: case 2u:
00177$:
;apps/oneaddr_basic.c:1700: return "COMPARES MAKE";
	ld	hl, #___str_105
	jr	00182$
;apps/oneaddr_basic.c:1701: case 3u:
00178$:
;apps/oneaddr_basic.c:1702: return "0 FALSE 1 TRUE";
	ld	hl, #___str_106
	jr	00182$
;apps/oneaddr_basic.c:1703: default:
00179$:
;apps/oneaddr_basic.c:1704: return "";
	ld	hl, #___str_66
;apps/oneaddr_basic.c:1706: }
00182$:
;apps/oneaddr_basic.c:1707: }
	pop	ix
	ret
___str_62:
	.ascii "PRINT X"
	.db 0x00
___str_63:
	.ascii "SHOWS A NUMBER"
	.db 0x00
___str_64:
	.ascii "PRINT "
	.db 0x22
	.ascii "HI"
	.db 0x22
	.db 0x00
___str_65:
	.ascii "SHOWS TEXT"
	.db 0x00
___str_66:
	.db 0x00
___str_67:
	.ascii "LET A=1"
	.db 0x00
___str_68:
	.ascii "A=A+1 ALSO OK"
	.db 0x00
___str_69:
	.ascii "VARS ARE A-Z"
	.db 0x00
___str_70:
	.ascii "IF X THEN CMD"
	.db 0x00
___str_71:
	.ascii "RUNS CMD WHEN"
	.db 0x00
___str_72:
	.ascii "X IS NOT ZERO"
	.db 0x00
___str_73:
	.ascii "COMPARE = < >"
	.db 0x00
___str_74:
	.ascii "FOR I=1 TO 5"
	.db 0x00
___str_75:
	.ascii "REPEATS LINES"
	.db 0x00
___str_76:
	.ascii "UNTIL NEXT"
	.db 0x00
___str_77:
	.ascii "STEP OPTIONAL"
	.db 0x00
___str_78:
	.ascii "NEXT"
	.db 0x00
___str_79:
	.ascii "ENDS FOR LOOP"
	.db 0x00
___str_80:
	.ascii "NEXT I CHECKS"
	.db 0x00
___str_81:
	.ascii "THE VARIABLE"
	.db 0x00
___str_82:
	.ascii "INPUT A"
	.db 0x00
___str_83:
	.ascii "READS BUTTONS"
	.db 0x00
___str_84:
	.ascii "A GETS A MASK"
	.db 0x00
___str_85:
	.ascii "0 MEANS NONE"
	.db 0x00
___str_86:
	.ascii "BTN"
	.db 0x00
___str_87:
	.ascii "CURRENT BUTTON"
	.db 0x00
___str_88:
	.ascii "MASK IN EXPR"
	.db 0x00
___str_89:
	.ascii "IF BTN THEN..."
	.db 0x00
___str_90:
	.ascii "WAIT 50"
	.db 0x00
___str_91:
	.ascii "PAUSES A BIT"
	.db 0x00
___str_92:
	.ascii "C CAN STOP IT"
	.db 0x00
___str_93:
	.ascii "CLS"
	.db 0x00
___str_94:
	.ascii "CLEARS OUTPUT"
	.db 0x00
___str_95:
	.ascii "PROGRAM KEEPS"
	.db 0x00
___str_96:
	.ascii "RUNNING"
	.db 0x00
___str_97:
	.ascii "REM COMMENT"
	.db 0x00
___str_98:
	.ascii "IGNORED LINE"
	.db 0x00
___str_99:
	.ascii "USE FOR NOTES"
	.db 0x00
___str_100:
	.ascii "END"
	.db 0x00
___str_101:
	.ascii "STOPS PROGRAM"
	.db 0x00
___str_102:
	.ascii "DONE IS SHOWN"
	.db 0x00
___str_103:
	.ascii "A+2*(B-1)"
	.db 0x00
___str_104:
	.ascii "+ - * / ( )"
	.db 0x00
___str_105:
	.ascii "COMPARES MAKE"
	.db 0x00
___str_106:
	.ascii "0 FALSE 1 TRUE"
	.db 0x00
;apps/oneaddr_basic.c:1709: static void draw_instruction_list(unsigned char selected) {
;	---------------------------------
; Function draw_instruction_list
; ---------------------------------
_draw_instruction_list:
;apps/oneaddr_basic.c:1713: glic_prepare_screen(GLIC_BLACK);
	xor	a, a
	push	af
	inc	sp
	call	_glic_prepare_screen
	inc	sp
;apps/oneaddr_basic.c:1714: text_write(0u, 1u, "INSTRUCTIONS");
	ld	hl, #___str_107
	push	hl
	ld	a, #0x01
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_write
	pop	af
;apps/oneaddr_basic.c:1715: text_write(1u, 0u, "CTR OPENS ITEM");
	ld	hl, #___str_108
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x01
	push	de
	call	_text_write
	pop	af
	pop	af
;apps/oneaddr_basic.c:1717: for (i = 0u; i < INSTRUCTION_COUNT; ++i) {
	ld	c, #0x00
00102$:
;apps/oneaddr_basic.c:1718: row = (unsigned char)(2u + i);
	ld	b, c
	inc	b
	inc	b
;apps/oneaddr_basic.c:1719: text_put(row, 0u, (i == selected) ? '>' : ' ');
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	sub	a, c
	jr	NZ, 00106$
	ld	de, #0x003e
	jr	00107$
00106$:
	ld	de, #0x0020
00107$:
	ld	a, e
	push	bc
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	push	bc
	inc	sp
	call	_text_put
	pop	af
	inc	sp
	pop	bc
;apps/oneaddr_basic.c:1720: text_write(row, 2u, instruction_name(i));
	push	bc
	ld	a, c
	push	af
	inc	sp
	call	_instruction_name
	inc	sp
	pop	bc
	push	bc
	push	hl
	ld	a, #0x02
	push	af
	inc	sp
	push	bc
	inc	sp
	call	_text_write
	pop	af
	pop	af
	pop	bc
;apps/oneaddr_basic.c:1717: for (i = 0u; i < INSTRUCTION_COUNT; ++i) {
	inc	c
	ld	a, c
	sub	a, #0x0c
	jr	C, 00102$
;apps/oneaddr_basic.c:1723: text_write(14u, 0u, "UP/DN PICK");
	ld	hl, #___str_109
	push	hl
	xor	a, a
	ld	d,a
	ld	e,#0x0e
	push	de
	call	_text_write
	pop	af
;apps/oneaddr_basic.c:1724: text_write(15u, 0u, "CTR VIEW C BACK");
	ld	hl, #___str_110
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x0f
	push	de
	call	_text_write
	pop	af
	pop	af
;apps/oneaddr_basic.c:1725: }
	ret
___str_107:
	.ascii "INSTRUCTIONS"
	.db 0x00
___str_108:
	.ascii "CTR OPENS ITEM"
	.db 0x00
___str_109:
	.ascii "UP/DN PICK"
	.db 0x00
___str_110:
	.ascii "CTR VIEW C BACK"
	.db 0x00
;apps/oneaddr_basic.c:1727: static void draw_instruction_detail(unsigned char selected) {
;	---------------------------------
; Function draw_instruction_detail
; ---------------------------------
_draw_instruction_detail:
;apps/oneaddr_basic.c:1730: glic_prepare_screen(GLIC_BLACK);
	xor	a, a
	push	af
	inc	sp
	call	_glic_prepare_screen
	inc	sp
;apps/oneaddr_basic.c:1731: text_put(0u, 0u, '>');
	ld	a, #0x3e
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_put
	pop	af
	inc	sp
;apps/oneaddr_basic.c:1732: text_write(0u, 2u, instruction_name(selected));
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_instruction_name
	inc	sp
	push	hl
	ld	a, #0x02
	push	af
	inc	sp
	xor	a, a
	push	af
	inc	sp
	call	_text_write
	pop	af
	pop	af
;apps/oneaddr_basic.c:1734: for (line = 0u; line < 9u; ++line) {
	ld	b, #0x00
00102$:
;apps/oneaddr_basic.c:1737: instruction_detail(selected, line));
	push	bc
	push	bc
	inc	sp
	ld	hl, #5
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_instruction_detail
	pop	af
	pop	bc
;apps/oneaddr_basic.c:1735: text_write((unsigned char)(2u + line),
	ld	a, b
	inc	a
	inc	a
	push	bc
	push	hl
	ld	h, #0x00
	push	hl
	inc	sp
	push	af
	inc	sp
	call	_text_write
	pop	af
	pop	af
	pop	bc
;apps/oneaddr_basic.c:1734: for (line = 0u; line < 9u; ++line) {
	inc	b
	ld	a, b
	sub	a, #0x09
	jr	C, 00102$
;apps/oneaddr_basic.c:1740: text_write(13u, 0u, "< > NEXT ITEM");
	ld	hl, #___str_111
	push	hl
	xor	a, a
	ld	d,a
	ld	e,#0x0d
	push	de
	call	_text_write
	pop	af
;apps/oneaddr_basic.c:1741: text_write(14u, 0u, "UP/DN ALSO");
	ld	hl, #___str_112
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x0e
	push	de
	call	_text_write
	pop	af
;apps/oneaddr_basic.c:1742: text_write(15u, 0u, "C LIST CTR LIST");
	ld	hl, #___str_113
	ex	(sp),hl
	xor	a, a
	ld	d,a
	ld	e,#0x0f
	push	de
	call	_text_write
	pop	af
	pop	af
;apps/oneaddr_basic.c:1743: }
	ret
___str_111:
	.ascii "< > NEXT ITEM"
	.db 0x00
___str_112:
	.ascii "UP/DN ALSO"
	.db 0x00
___str_113:
	.ascii "C LIST CTR LIST"
	.db 0x00
;apps/oneaddr_basic.c:1745: static void show_instruction_detail(unsigned char *selected) {
;	---------------------------------
; Function show_instruction_detail
; ---------------------------------
_show_instruction_detail:
;apps/oneaddr_basic.c:1749: draw_instruction_detail(*selected);
	pop	de
	pop	bc
	push	bc
	push	de
	ld	a, (bc)
	push	bc
	push	af
	inc	sp
	call	_draw_instruction_detail
	inc	sp
	pop	bc
;apps/oneaddr_basic.c:1750: last_buttons = GLIC_BUTTONS_NONE;
	ld	h, #0xff
;apps/oneaddr_basic.c:1751: while (1) {
00118$:
;apps/oneaddr_basic.c:1752: buttons = glic_read_buttons();
	push	hl
	push	bc
	call	_glic_read_buttons
	ld	a, l
	pop	bc
	pop	hl
	ld	d, a
;apps/oneaddr_basic.c:1753: if ((button_edge(buttons, last_buttons, GLIC_BTN_C) != 0u) ||
	push	hl
	push	bc
	push	de
	ld	a, #0x20
	push	af
	inc	sp
	ld	l, d
	push	hl
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	de
	pop	bc
	pop	hl
	or	a, a
	jp	NZ,_wait_for_release
;apps/oneaddr_basic.c:1754: (button_edge(buttons, last_buttons, GLIC_BTN_CENTER) != 0u)) {
	push	hl
	push	bc
	push	de
	ld	a, #0x01
	push	af
	inc	sp
	ld	l, d
	push	hl
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	de
	pop	bc
	pop	hl
	or	a, a
;apps/oneaddr_basic.c:1755: wait_for_release();
;apps/oneaddr_basic.c:1756: return;
	jp	NZ,_wait_for_release
;apps/oneaddr_basic.c:1757: } else if ((button_edge(buttons, last_buttons, GLIC_BTN_RIGHT) != 0u) ||
	push	hl
	push	bc
	push	de
	ld	a, #0x04
	push	af
	inc	sp
	ld	l, d
	push	hl
	call	_button_edge
	pop	af
	ld	e, l
	inc	sp
	pop	af
	ld	d, a
	pop	bc
	pop	hl
	ld	a, e
	or	a, a
	jr	NZ, 00109$
;apps/oneaddr_basic.c:1758: (button_edge(buttons, last_buttons, GLIC_BTN_DOWN) != 0u)) {
	push	hl
	push	bc
	push	de
	ld	a, #0x08
	push	af
	inc	sp
	ld	l, d
	push	hl
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	de
	pop	bc
	pop	hl
	or	a, a
	jr	Z, 00110$
00109$:
;apps/oneaddr_basic.c:1759: ++(*selected);
	ld	a, (bc)
	inc	a
	ld	(bc), a
;apps/oneaddr_basic.c:1760: if (*selected >= INSTRUCTION_COUNT) {
	sub	a, #0x0c
	jr	C, 00102$
;apps/oneaddr_basic.c:1761: *selected = 0u;
	xor	a, a
	ld	(bc), a
00102$:
;apps/oneaddr_basic.c:1763: draw_instruction_detail(*selected);
	ld	a, (bc)
	push	bc
	push	de
	push	af
	inc	sp
	call	_draw_instruction_detail
	inc	sp
	pop	de
	pop	bc
	jr	00115$
00110$:
;apps/oneaddr_basic.c:1764: } else if ((button_edge(buttons, last_buttons, GLIC_BTN_LEFT) != 0u) ||
	push	hl
	push	bc
	push	de
	ld	a, #0x10
	push	af
	inc	sp
	ld	l, d
	push	hl
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	de
	pop	bc
	pop	hl
	or	a, a
	jr	NZ, 00106$
;apps/oneaddr_basic.c:1765: (button_edge(buttons, last_buttons, GLIC_BTN_UP) != 0u)) {
	push	bc
	push	de
	ld	a, #0x02
	push	af
	inc	sp
	ld	l, d
	push	hl
	call	_button_edge
	pop	af
	inc	sp
	ld	a, l
	pop	de
	pop	bc
	or	a, a
	jr	Z, 00115$
00106$:
;apps/oneaddr_basic.c:1766: if (*selected == 0u) {
	ld	a, (bc)
	or	a, a
	jr	NZ, 00104$
;apps/oneaddr_basic.c:1767: *selected = (unsigned char)(INSTRUCTION_COUNT - 1u);
	ld	a, #0x0b
	ld	(bc), a
	jr	00105$
00104$:
;apps/oneaddr_basic.c:1769: --(*selected);
	dec	a
	ld	(bc), a
00105$:
;apps/oneaddr_basic.c:1771: draw_instruction_detail(*selected);
	ld	a, (bc)
	push	bc
	push	de
	push	af
	inc	sp
	call	_draw_instruction_detail
	inc	sp
	pop	de
	pop	bc
00115$:
;apps/oneaddr_basic.c:1773: last_buttons = buttons;
	ld	h, d
;apps/oneaddr_basic.c:1774: glic_delay(LOOP_DELAY);
	push	hl
	push	bc
	ld	de, #0x0384
	push	de
	call	_glic_delay
	pop	af
	pop	bc
	pop	hl
;apps/oneaddr_basic.c:1776: }
	jp	00118$
;apps/oneaddr_basic.c:1778: static void show_instructions(void) {
;	---------------------------------
; Function show_instructions
; ---------------------------------
_show_instructions:
	push	af
;apps/oneaddr_basic.c:1783: selected = 0u;
	xor	a, a
	ld	iy, #1
	add	iy, sp
	ld	0 (iy), a
;apps/oneaddr_basic.c:1784: draw_instruction_list(selected);
	xor	a, a
	push	af
	inc	sp
	call	_draw_instruction_list
	inc	sp
;apps/oneaddr_basic.c:1785: last_buttons = GLIC_BUTTONS_NONE;
	ld	c, #0xff
;apps/oneaddr_basic.c:1787: while (1) {
00120$:
;apps/oneaddr_basic.c:1788: buttons = glic_read_buttons();
	push	bc
	call	_glic_read_buttons
	pop	bc
	ld	a, l
	ld	iy, #0
	add	iy, sp
	ld	0 (iy), a
;apps/oneaddr_basic.c:1789: if (button_edge(buttons, last_buttons, GLIC_BTN_UP) != 0u) {
	push	bc
	ld	b, #0x02
	push	bc
	ld	a, 0 (iy)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	bc
	or	a, a
	jr	Z, 00117$
;apps/oneaddr_basic.c:1790: if (selected == 0u) {
	ld	iy, #1
	add	iy, sp
	ld	a, 0 (iy)
	or	a, a
	jr	NZ, 00102$
;apps/oneaddr_basic.c:1791: selected = (unsigned char)(INSTRUCTION_COUNT - 1u);
	ld	0 (iy), #0x0b
	jr	00103$
00102$:
;apps/oneaddr_basic.c:1793: --selected;
	ld	iy, #1
	add	iy, sp
	dec	0 (iy)
00103$:
;apps/oneaddr_basic.c:1795: draw_instruction_list(selected);
	ld	hl, #1
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_draw_instruction_list
	inc	sp
	jp	00118$
00117$:
;apps/oneaddr_basic.c:1796: } else if (button_edge(buttons, last_buttons, GLIC_BTN_DOWN) != 0u) {
	push	bc
	ld	b, #0x08
	push	bc
	ld	hl, #4
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	bc
	or	a, a
	jr	Z, 00114$
;apps/oneaddr_basic.c:1797: ++selected;
	ld	iy, #1
	add	iy, sp
	inc	0 (iy)
;apps/oneaddr_basic.c:1798: if (selected >= INSTRUCTION_COUNT) {
	ld	a, 0 (iy)
	sub	a, #0x0c
	jr	C, 00105$
;apps/oneaddr_basic.c:1799: selected = 0u;
	ld	0 (iy), #0
00105$:
;apps/oneaddr_basic.c:1801: draw_instruction_list(selected);
	ld	hl, #1
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_draw_instruction_list
	inc	sp
	jr	00118$
00114$:
;apps/oneaddr_basic.c:1802: } else if ((button_edge(buttons, last_buttons, GLIC_BTN_CENTER) != 0u) ||
	push	bc
	ld	b, #0x01
	push	bc
	ld	hl, #4
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	bc
	or	a, a
	jr	NZ, 00109$
;apps/oneaddr_basic.c:1803: (button_edge(buttons, last_buttons, GLIC_BTN_A) != 0u)) {
	push	bc
	ld	b, #0x80
	push	bc
	ld	hl, #4
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	bc
	or	a, a
	jr	Z, 00110$
00109$:
;apps/oneaddr_basic.c:1804: wait_for_release();
	call	_wait_for_release
;apps/oneaddr_basic.c:1805: show_instruction_detail(&selected);
	ld	hl, #1
	add	hl, sp
	push	hl
	call	_show_instruction_detail
	pop	af
;apps/oneaddr_basic.c:1806: draw_instruction_list(selected);
	ld	hl, #1
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_draw_instruction_list
	inc	sp
;apps/oneaddr_basic.c:1807: last_buttons = GLIC_BUTTONS_NONE;
	jr	00118$
00110$:
;apps/oneaddr_basic.c:1808: } else if ((button_edge(buttons, last_buttons, GLIC_BTN_C) != 0u) ||
	push	bc
	ld	b, #0x20
	push	bc
	ld	hl, #4
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	bc
	or	a, a
	jr	NZ, 00106$
;apps/oneaddr_basic.c:1809: (button_edge(buttons, last_buttons, GLIC_BTN_B) != 0u)) {
	ld	b, #0x40
	push	bc
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00118$
00106$:
;apps/oneaddr_basic.c:1810: wait_for_release();
	call	_wait_for_release
;apps/oneaddr_basic.c:1811: return;
	jr	00122$
00118$:
;apps/oneaddr_basic.c:1814: last_buttons = buttons;
	ld	hl, #0
	add	hl, sp
	ld	c, (hl)
;apps/oneaddr_basic.c:1815: glic_delay(LOOP_DELAY);
	push	bc
	ld	hl, #0x0384
	push	hl
	call	_glic_delay
	pop	af
	pop	bc
	jp	00120$
00122$:
;apps/oneaddr_basic.c:1817: }
	pop	af
	ret
;apps/oneaddr_basic.c:1819: static void confirm_load_demo(void) {
;	---------------------------------
; Function confirm_load_demo
; ---------------------------------
_confirm_load_demo:
;apps/oneaddr_basic.c:1823: glic_prepare_screen(GLIC_BLACK);
	xor	a, a
	push	af
	inc	sp
	call	_glic_prepare_screen
	inc	sp
;apps/oneaddr_basic.c:1824: text_write(3u, 2u, "LOAD DEMO?");
	ld	hl, #___str_114
	push	hl
	ld	de, #0x0203
	push	de
	call	_text_write
	pop	af
;apps/oneaddr_basic.c:1825: text_write(6u, 2u, "A YES");
	ld	hl, #___str_115
	ex	(sp),hl
	ld	de, #0x0206
	push	de
	call	_text_write
	pop	af
;apps/oneaddr_basic.c:1826: text_write(8u, 2u, "B/C NO");
	ld	hl, #___str_116
	ex	(sp),hl
	ld	de, #0x0208
	push	de
	call	_text_write
	pop	af
	pop	af
;apps/oneaddr_basic.c:1828: last_buttons = GLIC_BUTTONS_NONE;
	ld	h, #0xff
;apps/oneaddr_basic.c:1829: while (1) {
00108$:
;apps/oneaddr_basic.c:1830: buttons = glic_read_buttons();
	push	hl
	call	_glic_read_buttons
	pop	af
	ld	h, a
;apps/oneaddr_basic.c:1831: if (button_edge(buttons, last_buttons, GLIC_BTN_A) != 0u) {
	push	hl
	ld	a, #0x80
	push	af
	inc	sp
	push	hl
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	hl
	or	a, a
	jr	Z, 00102$
;apps/oneaddr_basic.c:1832: load_demo_program();
	call	_load_demo_program
;apps/oneaddr_basic.c:1833: wait_for_release();
;apps/oneaddr_basic.c:1834: return;
	jp	_wait_for_release
00102$:
;apps/oneaddr_basic.c:1836: if ((button_edge(buttons, last_buttons, GLIC_BTN_B) != 0u) ||
	push	hl
	ld	a, #0x40
	push	af
	inc	sp
	push	hl
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	hl
	or	a, a
	jp	NZ,_wait_for_release
;apps/oneaddr_basic.c:1837: (button_edge(buttons, last_buttons, GLIC_BTN_C) != 0u) ||
	push	hl
	ld	a, #0x20
	push	af
	inc	sp
	push	hl
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	hl
	or	a, a
	jp	NZ,_wait_for_release
;apps/oneaddr_basic.c:1838: (button_edge(buttons, last_buttons, GLIC_BTN_CENTER) != 0u)) {
	push	hl
	ld	a, #0x01
	push	af
	inc	sp
	push	hl
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	hl
	or	a, a
;apps/oneaddr_basic.c:1839: wait_for_release();
;apps/oneaddr_basic.c:1840: return;
	jp	NZ,_wait_for_release
;apps/oneaddr_basic.c:1842: last_buttons = buttons;
	ld	h, l
;apps/oneaddr_basic.c:1843: glic_delay(LOOP_DELAY);
	push	hl
	ld	de, #0x0384
	push	de
	call	_glic_delay
	pop	af
	pop	hl
;apps/oneaddr_basic.c:1845: }
	jr	00108$
___str_114:
	.ascii "LOAD DEMO?"
	.db 0x00
___str_115:
	.ascii "A YES"
	.db 0x00
___str_116:
	.ascii "B/C NO"
	.db 0x00
;apps/oneaddr_basic.c:1847: static unsigned char run_menu(void) {
;	---------------------------------
; Function run_menu
; ---------------------------------
_run_menu:
	dec	sp
;apps/oneaddr_basic.c:1851: draw_menu();
	call	_draw_menu
;apps/oneaddr_basic.c:1852: last_buttons = GLIC_BUTTONS_NONE;
	ld	b, #0xff
;apps/oneaddr_basic.c:1854: while (1) {
00111$:
;apps/oneaddr_basic.c:1855: buttons = glic_read_buttons();
	push	bc
	call	_glic_read_buttons
	pop	bc
	ld	a, l
	ld	iy, #0
	add	iy, sp
	ld	0 (iy), a
;apps/oneaddr_basic.c:1856: if (button_edge(buttons, last_buttons, GLIC_BTN_CENTER) != 0u) {
	push	bc
	ld	a, #0x01
	push	af
	inc	sp
	push	bc
	inc	sp
	ld	a, 0 (iy)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	bc
	or	a, a
	jr	Z, 00102$
;apps/oneaddr_basic.c:1857: wait_for_release();
	call	_wait_for_release
;apps/oneaddr_basic.c:1858: return ACTION_EDIT;
	ld	l, #0x01
	jr	00113$
00102$:
;apps/oneaddr_basic.c:1860: if (button_edge(buttons, last_buttons, GLIC_BTN_A) != 0u) {
	push	bc
	ld	a, #0x80
	push	af
	inc	sp
	push	bc
	inc	sp
	ld	hl, #4
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	bc
	or	a, a
	jr	Z, 00104$
;apps/oneaddr_basic.c:1861: wait_for_release();
	call	_wait_for_release
;apps/oneaddr_basic.c:1862: return ACTION_RUN;
	ld	l, #0x02
	jr	00113$
00104$:
;apps/oneaddr_basic.c:1864: if (button_edge(buttons, last_buttons, GLIC_BTN_B) != 0u) {
	push	bc
	ld	a, #0x40
	push	af
	inc	sp
	push	bc
	inc	sp
	ld	hl, #4
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	pop	bc
	or	a, a
	jr	Z, 00108$
;apps/oneaddr_basic.c:1865: wait_for_release();
	call	_wait_for_release
;apps/oneaddr_basic.c:1866: confirm_load_demo();
	call	_confirm_load_demo
;apps/oneaddr_basic.c:1867: draw_menu();
	call	_draw_menu
;apps/oneaddr_basic.c:1868: last_buttons = GLIC_BUTTONS_NONE;
	jr	00109$
00108$:
;apps/oneaddr_basic.c:1869: } else if (button_edge(buttons, last_buttons, GLIC_BTN_C) != 0u) {
	ld	a, #0x20
	push	af
	inc	sp
	push	bc
	inc	sp
	ld	hl, #2
	add	hl, sp
	ld	a, (hl)
	push	af
	inc	sp
	call	_button_edge
	pop	af
	ld	a, l
	inc	sp
	or	a, a
	jr	Z, 00109$
;apps/oneaddr_basic.c:1870: wait_for_release();
	call	_wait_for_release
;apps/oneaddr_basic.c:1871: show_instructions();
	call	_show_instructions
;apps/oneaddr_basic.c:1872: draw_menu();
	call	_draw_menu
;apps/oneaddr_basic.c:1873: last_buttons = GLIC_BUTTONS_NONE;
00109$:
;apps/oneaddr_basic.c:1876: last_buttons = buttons;
	ld	hl, #0
	add	hl, sp
	ld	b, (hl)
;apps/oneaddr_basic.c:1877: glic_delay(LOOP_DELAY);
	push	bc
	ld	hl, #0x0384
	push	hl
	call	_glic_delay
	pop	af
	pop	bc
	jp	00111$
00113$:
;apps/oneaddr_basic.c:1879: }
	inc	sp
	ret
;apps/oneaddr_basic.c:1881: void main(void) {
;	---------------------------------
; Function main
; ---------------------------------
_main::
;apps/oneaddr_basic.c:1884: storage_init();
	call	_storage_init
;apps/oneaddr_basic.c:1885: while (1) {
00107$:
;apps/oneaddr_basic.c:1886: action = run_menu();
	call	_run_menu
	ld	a, l
;apps/oneaddr_basic.c:1887: if (action == ACTION_EDIT) {
	cp	a, #0x01
	jr	NZ, 00104$
;apps/oneaddr_basic.c:1888: run_editor();
	call	_run_editor
	jr	00107$
00104$:
;apps/oneaddr_basic.c:1889: } else if (action == ACTION_RUN) {
	sub	a, #0x02
	jr	NZ, 00107$
;apps/oneaddr_basic.c:1890: run_program();
	call	_run_program
;apps/oneaddr_basic.c:1893: }
	jr	00107$
	.area _CODE
	.area _INITIALIZER
	.area _CABS (ABS)
